# Report — `P2-XI-QM2-SCOPE-01` v2: nine inputs enumerated and classified

    Task ID     P2-XI-QM2-SCOPE-01
    Version     v2
    Spec        specs/2026-08-24T1500Z_xi-qm2-scope-01_v2.md
    Review      reviews/chatgpt/2026-08-24T1500Z_xi-qm2-scope-01_v2.md
    Base        main @ 0c01fc7f26e91dd84b032dccde0feac61f61d8ea
    Branch      science/xi-qm2-scope-01
    Outcome     COMPLETE through M6. Nine inputs enumerated, each
                carrying exactly one of the four outcomes. Five are
                returned to the PI. Q-M2 remains OPEN.

This report records `M0` through `M6` and nothing later. It names the tested
tree `T` and **is itself the next commit on `T`**; it does not state its own
commit SHA. `M7b`'s measurements are post-commit and are recorded outside this
file.

    T = f2766d60fdd7ec7ca0191ee9ef90b5e259050f42

**MEASUREMENT UNITS.** Every length and byte-identity comparison in this task is
performed and reported in bytes. **No normalization was applied to either side
of any byte comparison**, and none is claimed.

---

## 0. Execution location and worktree identity (Amendment D step 0)

    host                     vm
    working directory        /home/user/2-emergent-gravity
    worktree top level       /home/user/2-emergent-gravity
    shallow repository       false
    HEAD before the branch   d55b6350a015d124f723d1fceb75b77cdcc112a9
                             (science/xi-qm3-dep-01)
    working tree             clean before the branch was cut

    git checkout -b science/xi-qm2-scope-01 \
        0c01fc7f26e91dd84b032dccde0feac61f61d8ea

## 0a. Binding, before any write

**Rule 18 and Amendment N.** The review carries the line
`**Reviewed specification SHA-256:**` — PRESENT — at its lines 4 and 174,
identically, and

    680248e8deada3e1d77df13ee1f5f8899ddb3084e76db71f4c052ce7ff07fb87

is the only 64-hex string it contains. Measured before the specification commit
and re-measured over the committed bytes afterwards:

    sha256 of the spec file, before the commit
      680248e8deada3e1d77df13ee1f5f8899ddb3084e76db71f4c052ce7ff07fb87
    the digest the review declares itself bound to
      680248e8deada3e1d77df13ee1f5f8899ddb3084e76db71f4c052ce7ff07fb87
    sha256 of the committed bytes
      680248e8deada3e1d77df13ee1f5f8899ddb3084e76db71f4c052ce7ff07fb87

The review artifact has no pre-committed hash. Its sha256 is recorded at commit
as its first recorded digest,
`f22397a0d5d52fdca3ff828e113e66b107f64c245f2af772b3d3bb849bf566cb`, provenance
transmitted by the PI in session. Verdict `APPROVE FOR EXECUTION`.

**Reviewer determinations, recorded as issued and not restated:**

    Four-outcome architecture         CONCUR
    DET-01 status/value distinction   CONCUR
    Scope boundary                    PRESERVED
    PI authority boundary             PRESERVED
    Protocol closure                  PASS

Commit order, spec then review, nothing interleaved:

    38b65276bb251fc4d04a86a830e717ab9c2ddfdf  spec(P2-XI-QM2-SCOPE-01): v2, ...
    73862217a5a822909636cff53e910e83fb22097f  review(P2-XI-QM2-SCOPE-01): ...

**The Reviewer's §15 execution note** — that an all-fourth-outcome result would
be a constructive terminal outcome after the artifact, provenance and report
steps rather than an abort suppressing the Rule 22 deliverable — **did not need
to be applied**: not every enumerated input carries the fourth outcome. See §4.

## 1. `M0` — precondition and provenance

    origin/main                       0c01fc7f26e91dd84b032dccde0feac61f61d8ea
    the Base this specification pins  0c01fc7f26e91dd84b032dccde0feac61f61d8ea
                                      EQUAL, full string

    (a) decisions/P2-XI-RULINGS-02.issued.md
        measured ab2e90ddb6fa8c24c9b913a26b4b455809ca358d82cff2d2256f3526957ebbf5
        required ab2e90ddb6fa8c24c9b913a26b4b455809ca358d82cff2d2256f3526957ebbf5

    (b) derivations/P2-XI-LEDGER-01_conditional-analytic-ledger.md
        present; sha256 aa0c79e21568b09d6efed64ec538c1ee9b4892ebc65653cb76deecfbd25f1454
        the Q-M2 row at :305, verbatim:

            condensate scalar's own                 —                            —     OPEN(Q-M2)
              fluctuation loop

        status OPEN(Q-M2), with an em-dash in both numeric cells

The repository is non-shallow. **`A0` did not fire. `C0` PASS.**

## 2. `M1` — provenance extraction, before any classification

Every node was extracted with
`git cat-file blob <Base>:<path> | sed -n 'A,Bp'` and appears in the artifact
inside a fenced block. Each was verified as a byte-substring of the artifact's
bytes; **no normalization was applied on either side.**

    node                                                          bytes  sha256 (first 16)
    ---------------------------------------------------------------------------------------
    decisions/P2-XI-RULINGS-02.issued.md:41-53   Ruling 3          824  8adf68d6e7a64f9c
    decisions/P2-XI-RULINGS-02.issued.md:55-59   Ruling 4          214  d224a19f5bac68d2
    P2-XI-B0a_induced-xi-scope-assessment.md:615-618  Q-M2         290  4f6390804ca1ec42
    P2-NORM-01_normalization_chain.md:23-27      the chain         148  1abfed23bb5257c5
    P2-SRC-B0_source-side-scope.md:186-188       DET-01            254  3b4bf05c20934790
    results/recovered-2026/session_log_full.md:101  the carrier    895  67994a152037769f
    P2-XI-B0a...:311-314   the landed rendering of that carrier    276  e3b2ab95c0852ca6
    GATES.md:665-668       the carrier's recorded status           316  c37ac0c6943782fb
    P2-XI-B0a...:601       "This artifact proposes nothing."        32  73e0bfea8cf8d2b7
    P2-XI-B0a...:607-608   "None is answered here…"                134  c70dcda9adccf804
    CONVENTIONS.md:17      scalar curvature-coupling convention    195  b043c43b305d0c12
    CONVENTIONS.md:20      Z, normalized per unit 4N               430  8e51adf7d50a63ce
    CONVENTIONS.md:21      β_s and the p_s prefactor rule          410  27b928ef5a4b8097
    CONVENTIONS.md:22      the L definition                        254  e80feb3e35927215
    P2-HK-01_heat_kernel_species.md:59-68  the bundle table        659  1688a1d28eba4b93
    P2-XI-LEDGER-01...:68-82   both Z conventions                  520  2a7dd51586ae2a39
    P2-XI-LEDGER-01...:299-303 the explicit-ξR row                 373  4beca3793592ff14
    P2-XI-B0a...:358-369   the landed non-overlap statement        691  be5c5a02d8f2581a
    P2-FIERZSUM-01.md:447-449  double counting above O(G²)         205  98271a7538d26e8f
    P2-XI-B0a...:610-613   Q-M1                                    266  39271a688ad52e93
    P2-XI-B0a...:626-628   Q-M4                                    223  c1e3aa21ad12594f
    P2-PHASE-01_fierz_verification...:326-330  OPEN-AC-2           341  4d1f686ed874414c
    ---------------------------------------------------------------------------------------
    22 quotations                                                 7950 bytes, all PASS

**`A1` did not fire**; every node the specification names was located.

**The `O(1)`-versus-`O(N)` carrier.** The landed `Q-M2` names
`results/recovered-2026/session_log_full.md:101`, which is written in Cantonese.
**The carrier in the artifact is that line's own bytes**, quoted in full;
alongside it the artifact carries the landed English rendering at
`P2-XI-B0a...:311-314`, which the landed text itself marks *"a working
translation, identified as such"*. **The rendering is not substituted for the
carrier**, and it is not this task's translation: it is landed text quoted as
landed text. `GATES.md:665-668` is quoted for what kind of source that log is —
"historically reported by the recovered session message, not independently
verified … recorded here as pre-registration *input*, not as a result." **`C1`
PASS.**

## 3. `M2` — the enumeration

**Nine inputs.** The three the ruling names at minimum are `I-1`, `I-2` and
`I-3`; the remaining six were enumerated from the extracted landed material and
from the structure of the chain at `P2-NORM-01:26`, not from that list.

    id    input                                              enters the chain at
    --------------------------------------------------------------------------------
    I-1   the condensate scalar's own curvature coupling ξ_χ   β_s
    I-2   the functional-measure inheritance                   Z(m²) / Γ
    I-3   how the O(1)-versus-O(N) counting enters the         Z(m²), before β_s
          normalization chain
    I-4   the scalar species' heat-kernel data and the         β_s
          β_s prefactor rule
    I-5   the mass at which the scalar species enters, and     Z(m²) and L
          the L treatment if it differs from the single m
    I-6   the Z convention the result is stated in             Z(m²) → 4 G_c β_F
    I-7   non-overlap with what the landed chain already       Z(m²)
          contains
    I-8   which ξ observable the result would be a             ξ_ind, the endpoint
          contribution to
    I-9   the scalar species' β_s as a function of its ξ       β_s

Each entry states what it is, why the computation requires it, and where it
enters, in the artifact's §3 beside the landed text its outcome rests on. **The
artifact carries the sentence that recording an input is not evidence that it is
obtainable, and that the enumeration order carries no priority. `C2` PASS.**

## 4. `M3` — the classification

    outcome                                        inputs
    -------------------------------------------------------------------
    LANDED                                         I-4, I-6
    LANDED — NOT DETERMINABLE                      I-2
    DERIVABLE                                      I-9
    REQUIRING A PI RULING                          I-1, I-3, I-5, I-8
    ROUTED TO PI — CLASSIFICATION NOT DETERMINABLE I-7
    -------------------------------------------------------------------
    inputs                                         9

**Every input carries exactly one outcome.** No outcome was assigned by default,
by elimination, or by a judgement of what is likely.

**`LANDED` (I-4, I-6).** The heat-kernel bundle data and the `β_s` prefactor
rule are landed at `P2-HK-01:59-68` and `CONVENTIONS.md:21`; both `Z`
conventions and the ownership of each value are landed at the ledger's `:68-82`.
Each assignment quotes its fixing text with a resolving `path:line`.

**`LANDED — NOT DETERMINABLE` (I-2).** `P2-SRC-B0:186-188` fixes that the
functional measure is `NOT DETERMINABLE`; the artifact writes the outcome in
that form per `COND-S`. **The status is landed; the value is not obtained**, and
Ruling 3's direction to state and not resolve `DET-01` is what the entry does.
`𝔊` is not chosen.

**`DERIVABLE` (I-9).** The scalar species' `β_s` as a function of its `ξ`. **The
route is NAMED and NOT walked**: from `P2-HK-01:59-68`'s non-minimal-scalar row
together with `CONVENTIONS.md:21`. The artifact states the route's two inputs
explicitly — `I-1`'s `ξ`, and the identification of the species with that table
row — and produces no coefficient, value, sign or magnitude.

**`REQUIRING A PI RULING` (I-1, I-3, I-5, I-8).** For each, the category is
known and an authoritative choice is missing; each entry states the choice and
that this task does not make it. `I-5` is recorded as **not** `DERIVABLE`
because the only route in sight runs through which branch is the vacuum, which
`P2-PHASE-01_fierz_verification...:326-330` records as `OPEN-AC-2` and
unanswered — a route requiring an unlanded choice is not a `DERIVABLE` route.

**`ROUTED TO PI — CLASSIFICATION NOT DETERMINABLE` (I-7).** Landed state does
carry a non-overlap statement (`P2-XI-B0a:358-369`), and landed state also
records that double counting "can first appear above `O(G²)`"
(`P2-FIERZSUM-01:447-449`), with the diagrammatic accounting that would settle
it among the deliverables `P2-FIERZSUM-01 §4` says the gate must freeze, in a
note `GATES.md:1374-1376` records as not registered. **Whether `I-7` is `LANDED`
on the non-overlap statement or `REQUIRING A PI RULING` therefore depends on the
truncation order — the model-level choice at issue.** `A3` was applied as the
specification directs: the fourth outcome was assigned, the item was recorded
under `M4`, and the remaining inputs were classified.

**`I-7` is not routed for difficulty.** The specification forbids the fourth
outcome as a fallback for a laborious trace, and this is not one: the outcome
itself turns on the choice.

**`COND-S` compliance.** No sentence of the artifact treats a landed status as
possession of a value. The one entry at risk, `I-2`, states the point explicitly
in its own text. **`C3` PASS.**

## 5. `M4` — the routed list

Present in the artifact at §4, in one place, with the two PI-facing outcomes
kept in separate tables so they are distinguishable: `4a` carries the four
`REQUIRING A PI RULING` inputs, `4b` the single
`ROUTED TO PI — CLASSIFICATION NOT DETERMINABLE` input. Each row names the
model-level choice at issue and what depends on it, and each table closes with
the statement that this task neither answers nor recommends.

**`4b`'s "what depends on it" is different in kind from `4a`'s**, and the
artifact says so: what depends on the choice there is *which outcome the input
carries*. **`C4` PASS.**

## 6. `M5` — the artifact's discipline statements

    "This artifact proposes nothing."                              present, §0,
      quoted from P2-XI-B0a:601 and attributed
    "Every question below is recorded. None is answered here, and   present, §0,
      the fact that a question is listed is not evidence about        quoted from
      its answer."                                                   :607-608
    COND-D  functional-measure status                              present, §0
    COND-E  enumeration is not exhaustion                          present, §0
    COND-S  status, not value                                      present, §0

**All five are present. `C5` PASS.**

## 7. `M6` — suite

Run on a full, non-shallow tree.

**At the Base**, tree `b113df9eb19becb467fad9be349241c7495d1b62`:

    ........................................................................ [ 20%]
    ........................................................................ [ 41%]
    ........................................................................ [ 62%]
    ........................................................................ [ 83%]
    ........................................................                 [100%]
    344 passed, 2 deselected in 39.11s

**At the post-`M5` tree**, which is `T`:

    T = f2766d60fdd7ec7ca0191ee9ef90b5e259050f42

    ........................................................................ [ 20%]
    ........................................................................ [ 41%]
    ........................................................................ [ 62%]
    ........................................................................ [ 83%]
    ........................................................                 [100%]
    344 passed, 2 deselected in 36.86s

Identical outcomes. **No test fails on `T` that passes at the Base. `C6` PASS.**

    git diff --name-status <Base>..<the artifact commit>
    A   derivations/P2-XI-QM2-SCOPE-01_condensate-loop-input-scope.md
    A   reviews/chatgpt/2026-08-24T1500Z_xi-qm2-scope-01_v2.md
    A   specs/2026-08-24T1500Z_xi-qm2-scope-01_v2.md

Exactly this task's declared paths, the report excepted, which is this commit.

## 8. Acceptance criteria

    C0  PASS           Base equals the pinned SHA; both M0 checks pass as
                       full-string matches; the Q-M2 row is OPEN and valueless.
    C1  PASS           All 22 quotations appear verbatim with a path:line
                       resolving at the Base; the carrier appears in its own
                       language and its rendering is carried as a translation
                       and not substituted for it.
    C2  PASS           The enumeration includes the ruling's three named inputs,
                       states for each entry what it is, why required and where
                       it enters, and carries the not-evidence-of-obtainability
                       sentence.
    C3  PASS           Every input carries exactly one of the four outcomes;
                       each LANDED assignment quotes its fixing text; the
                       LANDED — NOT DETERMINABLE form is used where what is
                       fixed is non-determinability; the DERIVABLE route is
                       named and not walked; both PI-facing outcomes state the
                       choice and the non-making; no sentence treats a landed
                       status as possession of a value.
    C4  PASS           The routed list exists, contains all five PI-facing
                       inputs, and keeps the two outcomes distinguishable.
    C5  PASS           All five discipline statements are present.
    C6  PASS           344 passed and 2 deselected at the Base and at T.
    C7  See §10. Its first limb is post-commit and is measured outside this
                       report; its other limbs hold here — this report records
                       M0–M6 and no later measurement, names T, and asserts no
                       SHA of its own.

## 9. Abort conditions

    A0  DID NOT FIRE.  The Base equals its pin and both M0 checks pass.
    A1  DID NOT FIRE.  Every node named in M1 was located at the Base.
    A2  DID NOT FIRE.  Nothing was computed, estimated, bounded, signed or
        ordered; the Q-M2 row is not disposed; the ledger is unmodified and is
        not among this task's changed paths; no gate moved; no PASS/FAIL was
        emitted; and no ordering or priority is stated between the Q-M3 check
        and this task or for the later bounding computation — Ruling 4 is quoted
        instead.
    A3  APPLIED AS DIRECTED, NOT AS A FAILURE.  One input, I-7, could not be
        placed among the first three without making the model-level choice at
        issue. The fourth outcome was assigned, the item recorded under M4, and
        the remaining inputs classified. **Not every input carries the fourth
        outcome**, so the terminal case A3 reserves — and which the Reviewer's
        §15 note addresses — does not arise, and no Rule 22 finding of that kind
        is made.
    A4  DID NOT FIRE.  The bounding computation was not performed, designed,
        method-scoped or authorized. Ruling 3's reservation of it to a separate
        later task is quoted in the artifact and is not narrowed.

## 10. Stops and clarifications (Amendment B)

Nothing stopped execution. One observation is recorded.

### 10a. Four quotation defects, found by measurement and corrected before the commit

**Category: `OBSERVATION_METHOD_ERROR`, found and corrected inside this task.**
The artifact's first draft failed byte-identity on four of its twenty-two
quotations. The verification is a substring test of each extraction against the
artifact's bytes, so it caught them before the commit; all four are corrected in
the committed artifact, and the corrected file is what §2 measures.

    node                                   defect                       fix
    -----------------------------------------------------------------------------
    Ruling 4                               labelled :55-60; the         relabelled
                                           quoted bytes are :55-59      :55-59
    the ledger's both-Z-conventions block  labelled :68-83; the         relabelled
                                           quoted bytes are :68-82      :68-82
    the double-counting passage            labelled :448-450; the       relabelled
                                           quoted bytes are :447-449    :447-449
    CONVENTIONS.md:22, the L definition    TRANSCRIBED FROM A           replaced
                                           TRUNCATED TERMINAL DISPLAY   with the
                                           and materially wrong in its  file's
                                           closing sentence            exact bytes

**The first three are label errors and the fourth is not.** The `L` definition
had been read through a `cut -c1-200` display and its closing sentence written
from that truncation; the file's own closing sentence is different. **No
downstream sentence rested on the wrong text** — `I-5` uses the `L` definition
only for the proposition that `L` is landed for a single mass, which both
versions carry — but the defect is recorded rather than quietly fixed, because
the method that produced it, reading a quotation off a truncated display instead
of off the file, is the part a later reader has to be able to distrust.

**The lesson is already this task's own rule.** The specification's MEASUREMENT
UNITS clause requires byte comparisons and stated normalizations; the practice
that satisfies it is to extract with `git cat-file blob` and never to retype
from a terminal rendering. All twenty-two quotations in the committed artifact
are extractions, and all twenty-two pass.

## 11. Rule 22

No result in this task is `INCONCLUSIVE`. **`I-7`'s fourth outcome is a
classification, not an inconclusive result**, and the specification says so: an
input carrying it satisfies "exactly one outcome". The terminal Rule 22 case
`A3` describes arises only if every enumerated input carries that outcome, which
is not what was measured. No subclass and no resolution path are therefore owed,
and none is asserted.

## 12. Environment

    python      3.11.15
    pytest      9.1.1
    numpy       2.4.6
    sympy       1.14.0
    ruff        0.15.8
    scipy       ABSENT
    repository  non-shallow, verified before each suite run

No environment repair was needed and none was performed. This task adds no code;
the only files it adds are Markdown, so no linter was run over changed sources.

## 13. Push scope

`docs/BRANCHING_POLICY.md` `science/*` scope. **Only
`refs/heads/science/xi-qm2-scope-01` is pushed.** `main` does not move, no other
`science/*` branch moves, and no session or harness branch is pushed.
**Integration is a separate task and is not begun, scheduled or constrained
here.**

`C7`'s first limb — that the final tip differs from `T` only by this report — is
post-commit and is recorded outside this file, which is itself the next commit
on `T`.

END OF REPORT
