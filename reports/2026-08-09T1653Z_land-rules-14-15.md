# Execution report — land amendments A–D and new Rules 14 and 15

Specification: `specs/2026-08-09T1653Z_land-rules-14-15.md`
Specification evidence base: `f309f61c9c14b0e2c63e078f9c0d0809422742e7`
Branch: `governance/land-rules-14-15`
Pre-report head: `2e878f61319d98d83314865028ceb66ed7ccf3a9`

**Outcome.** `CONVENTIONS.md` now runs 1–15. Four refinements are
attached to rules 7, 8, 9 and 13; Rules 14 and 15 are added after Rule
13. **Rules 1–13 keep their numbers and titles, and — after removing
only the authorised insertions — their pre-existing text is
byte-identical to the evidence-base file.** Both append-only measures
are zero. All three A1 pins and all ten §7 literal checks reproduce.
Four validators pass. No science, no gate, no result is touched, and no
`reviews/` record was created.

**Two findings the Reviewer should read before the detail.**

**(a) Rule 13 now carries two diagnostic-order enumerations, and the
older one is still phrased as complete.** §11.1 sets it out. It is not
an error in the landing — Amendment D's text is landed exactly as
approved and rewording Rule 13 is prohibited — but a reader who stops at
the pre-existing paragraph gets five steps and never sees `(0)` or
`(6)`. **It needs a one-line fix in a later task.**

**(b) Rule 15 is executable by me in three of its four clauses. The
fourth is not mine and is assigned to nobody.** §12 answers the
specification's question directly: I never receive a pre-execution
review as an artifact, so I cannot commit one, and Rule 15 does not say
who does. **`reviews/chatgpt/` exists at the evidence base containing
nothing but `.gitkeep`** — the placement was provisioned and never used.

---

## 1. A10 — refs, read from the remote

    refs/remotes/origin/main    f309f61c9c14b0e2c63e078f9c0d0809422742e7
    remote refs/heads/main      f309f61c9c14b0e2c63e078f9c0d0809422742e7
    local  refs/heads/main      0f7961747abe2a18b436c0b1e5b928f425ea4d9a

Both remote refs resolve to the evidence base. **Local `main` is stale by
design and was not repaired.** The branch was created from `f309f61c…` in
a separate worktree; no `main` ref was moved, no branch was deleted.

**`{HHMM}Z` is `1653`**, fixed by commit 1 and reused.

## 2. A1 — pinned inputs, verified before use

`CONVENTIONS.md` and `AGENTS.md` digests read from the git objects at the
evidence base; the draft digest from the supplied file. **All three
matched.**

    CONVENTIONS.md
      ec8d23aa8b0596d4d68cb5df4ab4821428641e923980d0fe1b45c4770c0089e6   MATCH
    AGENTS.md
      270c7ea4c38621cbafc9cee940c95c5ecf4cc65ff1e5d01e8029b1b12339d461   MATCH
    DRAFT_amendment_reporting_and_digests.md
      bef29c4d0ef3d391f2caa2f17ab6336e11677c73a3c37cbf4acb17b23e566822   MATCH

## 3. §7 — pre-issue literal verification, re-run

Executed here against the same two targets, as exact literal substring
containment with **no normalisation on either side**:

    PASS  count=1  '## Amendment A (attached to Rule 7)'                          draft
    PASS  count=1  '## Amendment B (attached to Rule 8)'                          draft
    PASS  count=1  '## Amendment C (attached to Rule 9)'                          draft
    PASS  count=1  '## Amendment D (attached to Rule 13)'                         draft
    PASS  count=1  '## New Rule 14'                                               draft
    PASS  count=1  '### 7. Evidence precedence'                                   CONVENTIONS.md
    PASS  count=1  '### 8. Responsibility separation'                             CONVENTIONS.md
    PASS  count=1  '### 9. Outcome-based task specification'                      CONVENTIONS.md
    PASS  count=1  '### 12. Acceptance criteria must be mechanically checkable'   CONVENTIONS.md
    PASS  count=1  '### 13. Execution environment'                                CONVENTIONS.md
    ALL REPRODUCE: True

**All ten reproduce, each exactly once.** No target was adjusted.

**One note on the eighth.** `### 8. Responsibility separation` is a
proper prefix of the file's actual heading,
`### 8. Responsibility separation (root rule of this section)`. **The
containment check passes and the heading is not what the literal says.**
That is correct for an insertion-target check — it locates the section —
but it is worth stating, because a reader could take the table as
evidence that the heading equals the literal. **It does not.**

**And the confirmation the specification asked for.** Rule 15's text is
**not** in the draft:

    "Governing artifacts are committed"   in draft:  False
    "## New Rule 15"                      in draft:  False

**That is a fact about the draft, not a failed lookup.** §2 of the
specification supplies the text and is its first durable record.

**§7 establishes insertion targets only.** Content fidelity is
established in §5 against the digest-pinned draft.

## 4. A2 — current numbering confirmed

`CONVENTIONS.md` at the evidence base, extracted headings matching
`^### <n>. `:

    ### 1. Contradiction-stop
    ### 2. Scope precedence
    ### 3. Declared frozen scope is normative
    ### 4. Execution prompts are evidence
    ### 5. Minimum mandatory merge discipline
    ### 6. Reporting honesty for merges
    ### 7. Evidence precedence
    ### 8. Responsibility separation (root rule of this section)
    ### 9. Outcome-based task specification
    ### 10. Self-correction authority and its limit
    ### 11. Task granularity and integration boundary
    ### 12. Acceptance criteria must be mechanically checkable
    ### 13. Execution environment

**Thirteen rules, 1 through 13. `grep -c '^### 1[45]\.'` returns 0** —
no Rule 14 or 15 exists. **The premise holds; no stop.**

**A structural note that bears on the insertions.** The rules span two
`##` sections: rules 1–7 under `## Execution discipline for decisive runs
and merges`, and rules 8–13 under `## Role separation and outcome-based
task specification`, whose heading sits immediately after Rule 7's body.
**Amendment A therefore had to be inserted before that `##` heading, not
after it**, or it would have landed inside the wrong section.

## 5. A3 — the four refinements

**Method.** Each amendment's text was extracted programmatically from
the digest-pinned draft by heading boundary, its blockquote prefixes
(`> `) stripped, and the result inserted at the end of the target rule's
body. **`CONVENTIONS.md` contains zero blockquote lines**, so retaining
`> ` would have introduced a representation the file does not use; the
substance is unchanged and every word is preserved.

**What was landed for each amendment.** The draft gives, for each, a
normative blockquote and one or more accompanying paragraphs recording
the incident and the reasoning. **Both were landed.** The draft states
its own position on this at its head — *"each records that incident,
because a rule whose origin is stated is one a later reader can
evaluate"* — and `CONVENTIONS.md` already carries origin paragraphs of
exactly this kind, in Rule 7 (the Arm-H audit) and in the Role-separation
section header. **Dropping them would have discarded content the draft
says is part of the amendment.**

**What was NOT landed, and why.** The draft's own apparatus: its
`Status: PROPOSED` front matter, the heading-naming rationale, `Two
things this amendment does NOT do`, `Queued items this amendment
references but does not implement`, and `Questions for the reviewer`.
**These describe the amendment package at a moment in time, not standing
rule text** — two of the reviewer questions are already marked
`ANSWERED by review`, and the queued items name tools that do not exist.
**Landing them would put a review conversation into a constitution
file.** Reported here so the exclusion is a decision on the record rather
than a silent omission.

**Each amendment is labelled where it lands**, so its provenance is
visible and the block is identifiable:

    **Amendment A, adopted 2026-08-09 — <title>.**

The label is new text, not draft text, and is part of the removable
block verified in §7 below.

### 5.1 Amendment A — attached to Rule 7

**(1) The pre-existing rule text, unchanged** — `CONVENTIONS.md` lines
137–148 at the evidence base, 12 lines:

    ### 7. Evidence precedence

    When repository state, committed artifacts, and narrative reports disagree,
    the committed artifacts take precedence. Reports explain; artifacts govern.
    Any claim about the state of the repository — by any agent, in any review, at
    any time — MUST be verified against the committed artifacts before it is
    relied upon or recorded.

    This rule arose from the Arm-H audit, in which several claims about repository
    state, made from recollection of prior sessions rather than from inspection,
    were found on checking to be incorrect. Verification against artifacts is not
    optional courtesy; it is the governing evidential rule.

**(2) The exact normative text the amendment adds** — 49 lines,
landed at lines 150–198:

    **Amendment A, adopted 2026-08-09 — mid-task authorizations are reproduced verbatim in the task report.**

    Where a task specification is amended mid-task, the amendment's text
    MUST be reproduced verbatim in the task report, together with: the
    **authorizing authority**; the **time or sequence point** at which it
    was issued, so that "before the commits" can be distinguished from
    "after them"; and the specific **acceptance criterion, scope manifest,
    invariant or prohibition** it superseded or extended. A report that
    omits any of these is incomplete regardless of how correct the
    repository state is.

    **Where the amendment changes a frozen path manifest, authorizes an
    otherwise prohibited repository modification, or changes an expected
    governance outcome, the report MUST reproduce the amended manifest or
    criterion IN FULL**, not summarise it.

    **Prior authorization and retrospective ratification are different
    things and MUST be named differently.** An instruction issued BEFORE
    the affected action is a *mid-task amendment* or *authorization*. An
    instruction issued AFTER it is a *retrospective ratification*, and
    **MUST NOT be described as prior authorization**. The report records
    the two distinctly.

    **Retrospective ratification may affect the task's disposition, but it
    does not make the earlier action authorized at the time it occurred.**
    This is the same principle as Rule 14's: an acceptance changes what is
    done about a fact, never the fact.

    No separate amendment artifact is required: a committed task report
    carrying all of the above is a sufficient audit chain, and requiring a
    file per amendment would multiply artifacts without adding evidence.

    *Incident.* Two commits in the integration task were explicitly
    authorized mid-task — a digest correction and the addition of a task
    record to the frozen scope manifest. Neither authorization existed
    anywhere except in conversation. The independent reviewer, reading only
    the final report, could not distinguish an authorized amendment from an
    executor expanding its own scope, and correctly raised both as possible
    violations. Resolving them required the specifier to supply evidence
    after the fact.

    *Why verbatim.* A summary of an amendment is not the amendment. What a
    later auditor needs is the wording the executor was actually working to,
    so that the executed result can be checked against it rather than
    against a paraphrase written once the outcome was known.

    **This does not require mid-task amendments to be reviewed before
    issue** — the PI may amend a task in flight without a review cycle. It
    requires only that the amendment become visible in the artifact the

**(3) The resulting rule section** is exactly (1) followed by (2), and
occupies lines 137–199 of the landed file, with `## Role separation and
outcome-based task specification` following at line 201. The seam:

    ...
    optional courtesy; it is the governing evidential rule.        <- (1) ends

    **Amendment A, adopted 2026-08-09 — mid-task authorizations …   <- (2) begins

**Why (3) is given as a range and a seam rather than re-quoted in
full.** It is the byte-exact concatenation of (1) and (2), and that is
not asserted but machine-verified: §7 below removes the four inserted
blocks and reproduces the evidence-base file byte for byte, which is
only possible if each resulting section is precisely its pre-existing
text plus its insertion. **Re-printing (3) would treble this section's
length and add no evidence** — and a duplicated transcription invites a
reader to diff two copies of the same thing. Both readings the
specification anticipated are answered: (1) is the whole pre-existing
rule, (2) is only the insertion, and (3) is identified exactly.

### 5.2 Amendment B — attached to Rule 8

**(1) The pre-existing rule text, unchanged** — lines 162–217 at the
evidence base, 56 lines:

    ### 8. Responsibility separation (root rule of this section)

    The **specification author** defines objectives, invariants and
    acceptance criteria.

    The **executor** determines the implementation necessary to satisfy
    them.

    The **reviewer** verifies the sufficiency and internal consistency of
    the specification before execution, and after execution independently
    assesses whether the resulting evidence actually supports the claimed
    outcome — including whether the acceptance criteria tested the
    intended object rather than merely passing implemented assertions.
    (Phase A's verifier rounds are the precedent: tests can pass while
    testing the wrong thing. A reviewer restricted to "did the criteria
    pass" would not catch that.)

    Two distinct review functions may be exercised by two independent
    reviewers: a **theory reviewer** (completeness, definitions, physics
    and mathematics) and an **evidence verifier** (repository results,
    computation, non-circularity of checkers, and whether claims exceed
    what was shown). These are functions, not fixed assignments to a
    particular agent.

    These are FUNCTIONS, not fixed agents: specification/theory,
    execution/experimentation, and independent review/verification. **PI
    authorization sits above all three** — adoption, exceptions, and final
    decisions are the PI's, and no rule in this section transfers that
    authority.

    **Refinement — caller roles.** Neither a specification nor a tool may assign
    roles on the caller's behalf; it must fix the interface rather than presume the
    caller's meaning. (Origin: the hard-coded worktree-correspondence target and
    the proposed target-type enumeration.)

    No role prescribes another role's INCIDENTAL implementation process. A method
    MAY be prescribed where it is itself load-bearing to scientific
    validity, independence, reproducibility, provenance, or governance —
    for example: separation of blind compute from comparison; exact
    rational rather than floating-point arithmetic; independent
    reproduction; clean-clone validation; the prohibition on one program
    generating both expected and actual values; mutation tests; sample
    size, seed, and regulator prescriptions; merge parentage and the
    no-force-push rule. **Every prescribed method MUST carry a stated
    reason**, so that ordinary implementation cannot be re-labelled
    "load-bearing" to smuggle procedural control back in.

    Concretely: the specifier does not choose the executor's git strategy,
    working directory, or command sequence; the executor does not alter
    objectives, invariants, or scientific content; the reviewer does not
    design the implementation.

    **Review of specifications, not of executions.** Theory and
    acceptance criteria are reviewed BEFORE the task; the resulting state
    is reviewed AFTER it. Individual implementation steps are not
    submitted for review.

**(2) The exact normative text the amendment adds** — 59 lines,
landed at lines 270–328:

    **Amendment B, adopted 2026-08-09 — every task report carries a "Stops and clarifications" section.**

    Every task report MUST contain a section recording each stop, with:
    where it stopped (stage and acceptance criterion) and the exact
    output — **reproduced in the report, or stored in a committed,
    content-digested artifact that the report identifies by path,
    revision and digest. The report MUST still reproduce the lines that
    establish the stop.** This permits bulk raw output to live in an
    auditable attachment; it does not permit a summary in place of
    evidence; whether the stop was correct; the defect's category; and the
    clarification or amendment that followed, naming the specific wording
    or value it changed.

    The category is one of exactly five:

    - `SPECIFICATION_DEFECT` — the task specification's criterion, scope,
      literal, prohibition or workflow is wrong, contradictory,
      unsatisfiable, or broader than its purpose.
    - `ENVIRONMENT` — execution identity, runtime, ACL, filesystem,
      package, or harness.
    - `OBSERVATION_METHOD_ERROR` — repository state is correct, but the
      inspecting command, path, hashing method, worktree, or parsing was
      wrong.
    - `REPOSITORY_DEFECT` — the repository's actual state violates a
      frozen requirement, or an artifact is itself wrong.
    - `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY` — the authoritative
      text or evidence cannot uniquely determine the outcome, and the
      executor has no authority to decide it.

    **Each stop has exactly one PRIMARY category. Secondary findings
    discovered through that stop MAY be recorded separately and classified
    independently.**

    *Why the classification is normative.* Across recent tasks the great
    majority of stops were specification defects — prohibitions written
    more broadly than their purpose, criteria unsatisfiable as written,
    literals mangled in transcription. A record showing "stopped eleven
    times" without categories invites the reader to infer executor
    unreliability, when the executor's stops were correct in every case.
    The distribution is itself the finding.

    *Why five and not four.* A fourfold scheme has no place for the
    `P2-PHASE-01` Phase-A/Phase-B dependency stop: the task specification
    was not wrong, the repository was not broken — the repository simply had
    not decided the question, and the executor was correctly forbidden from
    deciding it. That is a distinct and common class, and collapsing it into
    "specification defect" would misattribute the fault.

    *Why `REPOSITORY_DEFECT` rather than "genuine repository problem".* The
    looser name invites lumping together an unwelcome scientific result, an
    artifact nobody has yet promised to create, and an actual violation of a
    frozen requirement. Only the last is a defect.

    *Incident.* The integration task stopped three times. All three were
    primarily `OBSERVATION_METHOD_ERROR`; one of them surfaced a secondary
    `REPOSITORY_DEFECT` — a wrong digest already written into `GATES.md` —
    which was then corrected. **That is precisely the case the
    primary/secondary split exists for**: forcing it into a single category
    would either hide the registry error or misdescribe the stop.

**(3) The resulting rule section** is (1) followed by (2), occupying
lines 213–329 of the landed file, with `### 9. Outcome-based task
specification` following at line 330. Seam: `submitted for review.` is
the last pre-existing line; the Amendment B label is the first inserted
one.

### 5.3 Amendment C — attached to Rule 9

**(1) The pre-existing rule text, unchanged** — lines 219–265 at the
evidence base, 47 lines:

    ### 9. Outcome-based task specification

    A task specification MUST define **what must be true when the task is
    complete**. It SHOULD avoid prescribing implementation details unless
    those details are themselves governance objects — merge protocol
    (rule 5) and blind-campaign procedure are governance objects, and
    remain prescribed; git strategy, working directories, command
    sequences and tool invocations are not.

    A conforming specification has four MANDATORY NORMATIVE sections.
    Other material — context, governing sources, dependency state,
    authority and scope, definitions, outcome taxonomy, known limitations
    — may be present; only these four carry execution authority:

    **(a) Objective** — the required end state, stated as a condition of
    the repository, not as a sequence of actions.

    **(b) Acceptance criteria** — conditions, each independently
    checkable, whose conjunction is sufficient for the objective. Every
    criterion MUST have a machine-executable verification procedure and a
    defined expected outcome; the specification MAY identify the verifier
    interface and required outputs WITHOUT prescribing incidental command
    syntax, environment paths, or working-directory details. A procedure
    may be a checker script, a symbolic identity, a statistical decision
    rule, a generated manifest, or a structured comparison — it need not
    be a shell one-liner. A criterion requiring human judgement is not an
    acceptance criterion and belongs in (c) or in review.

    **(c) Invariants and prohibitions** — what may not change or be done
    under any circumstance. This part is authored by the specifier and is
    never inferred by the executor.

    **(d) Report contract** — what the executor must return: the raw
    output of every acceptance criterion, every self-correction with its
    before/after hashes and its reason, and any condition it could not
    satisfy.

    **Refinement — protected prohibitions and conditional choices.** Every
    prohibition MUST state what it protects, so its scope need not be inferred;
    where a specification offers a choice, each acceptance criterion MUST be
    conditional on that choice. (Origin: over-broad repair, formatter, and
    scratch-tool prohibitions; and an equivalence-policy alternative whose
    criterion allowed only one option.)

    Within the bounds of (c), the executor MAY choose its own method,
    explore alternatives, retry, and correct its own working artifacts.
    **It MUST NOT infer, extend, or relax (c).**

**(2) The exact normative text the amendment adds** — 34 lines,
landed at lines 378–411:

    **Amendment C, adopted 2026-08-09 — digest semantics and binary-safe computation.**

    A recorded SHA-256 digest is the SHA-256 of **the exact committed
    file-content bytes stored in the Git blob**. It is NOT the Git blob
    object ID, unless the record explicitly says "Git blob object ID" —
    the two are different quantities and are routinely confused.

    **Acceptable methods:** reading `git cat-file blob <revision>:<path>`
    through a binary-preserving subprocess API, or writing that byte
    stream to a binary file and hashing the file.

    **Prohibited:** any pipeline or shell construct that decodes the blob
    as text, performs line-oriented processing, uses command substitution,
    applies implicit encoding conversion, or otherwise fails to preserve
    the byte stream exactly. **The defect is not the pipe** — a genuinely
    binary subprocess pipe is fine; it is decoding, newline conversion and
    substitution. **PowerShell text pipelines are not presumed
    byte-preserving and MUST NOT be used for committed-content digests.**

    **The record MUST state the revision and path whose committed content
    was hashed** — `sha256(content of <rev>:<path>)`, not a bare filename
    and digest.

    A working-tree digest MAY be used for scratch diagnosis, but it MUST
    be labelled as such and **cannot satisfy an acceptance criterion that
    requires a committed-content digest**.

    *Incident.* Two draft digests recorded in `GATES.md` were wrong. The
    cause was hashing through a PowerShell pipeline that altered the byte
    stream. The error was found by an acceptance criterion designed for a
    different purpose (detecting whether a registry commit had touched a
    draft), and the first diagnosis — a line-ending artefact — was itself
    wrong; `core.autocrlf` was already `false`. Two round trips were spent
    before the actual cause was identified.

**(3) The resulting rule section** is (1) followed by (2), occupying
lines 330–412 of the landed file, with `### 10. Self-correction
authority and its limit` following at line 413. Seam:
`**It MUST NOT infer, extend, or relax (c).**` is the last pre-existing
line.

### 5.4 Amendment D — attached to Rule 13

**(1) The pre-existing rule text, unchanged** — lines 388–424 at the
evidence base, 37 lines, which ran to the end of the file:

    ### 13. Execution environment

    The repository declares its execution environment in an execution-environment
    document. The execution environment SHALL satisfy that document.

    **Standing authorization.** An executor MAY restore the declared execution
    environment without returning for authorization, and reports what it did in one
    line.

    **"Restore" means bringing the environment into conformity with the declared
    document. It does NOT authorize changing the declaration itself.**

    **Still requiring PI authorization:** changing what the document declares;
    installing anything it does not declare; modifying `pyproject.toml`, lint rules,
    validator configuration, or file permissions; and anything that alters whether
    a check passes or what a computation yields.

    **Environment failures SHALL be diagnosed in this order:** (1) execution
    identity; (2) interpreter availability; (3) permissions; (4) filesystem and
    workspace; (5) package availability.

    **Execution-environment repair SHALL NEVER be used as justification to modify
    repository content.** Restoring an environment never licenses editing a test,
    `pyproject.toml`, lint configuration, a script, a gate, or any other repository
    file. If a repository change appears necessary to make the environment work,
    that is a separate specification requiring its own authorization.

    **Repository content SHALL NEVER be modified solely because a different
    execution environment would make the modification unnecessary.** The two
    clauses bind in both directions: an environment problem is fixed in the
    environment, and a repository problem is not fixed by changing what the
    environment happens to be.

    **The repository and the execution environment are distinct layers.**
    `pyproject.toml`, tests, lint configuration and scripts are repository;
    interpreter, virtual environment, packages and permissions are environment. A
    failure in one is not evidence of a defect in the other.

**(2) The exact normative text the amendment adds** — 34 lines,
landed at lines 572–605:

    **Amendment D, adopted 2026-08-09 — execution location, and the process/harness layer.**

    Rule 13's diagnostic order is extended by a step before identity:

    (0) **execution location** — repository path, worktree identity,
        current branch or detached state, and the **resolved HEAD commit
        SHA**. A directory alone is not a location: the same worktree can
        sit at different revisions.
    (1) execution identity; (2) interpreter availability;
    (3) permissions; (4) filesystem and workspace; (5) package
        availability; (6) **process and harness lifecycle** — timeouts,
        stdout/stderr pipe closure, plugin teardown, child-process
        survival, signal or job-object termination.

    **Where a command evaluates a revision without checking it out, the
    report MUST distinguish the executing worktree's revision from the
    object revision being inspected.** Both are legitimate; conflating
    them is how "X is absent" gets asserted about the wrong revision.

    A conclusion of the form "X is absent" is not established until the
    location it was observed from has been established.

    *Why layer 6 exists.* Five validator files each reaching `[100%]` and
    each terminating at the same 120.2 s boundary is a process-lifecycle
    condition, not a package-availability one. Without its own layer, the
    most significant recent environment finding had nowhere in the
    diagnostic order to belong.

    *Incident.* The integration task stopped because the governance tools
    appeared to be missing. They were present at both pinned revisions. The
    observation had been made in the main worktree, which sits on its own
    branch and had never checked out the revision under test. The
    observation was accurate about that worktree and false about the
    revision, and nothing in the diagnostic order caught the difference.

**(3) The resulting rule section** is (1) followed by (2), occupying
lines 534–606 of the landed file, with `### 14. Validator outcome
contract` following at line 607. Seam: `failure in one is not evidence
of a defect in the other.` is the last pre-existing line.

**See §11.1.** This amendment is the one that creates a readable
inconsistency inside its own rule, and it is not fixable here.

## 6. A4 — Rules 14 and 15, quoted in full as landed

**Both are new `### <n>. <title>` sections placed immediately after Rule
13**, in the file's existing style. In each source the text opens with a
bold title line — `**14. Validator outcome contract.**` and
`**15. Governing artifacts are committed.**` — and **that line became the
section heading**, so nothing is duplicated and nothing is lost.

### 6.1 Rule 14, from the draft — landed at lines 607–658


    Unless a task specification states a stricter or explicitly different
    requirement, a validator PASSES only when all of the following hold:
    the intended validator process started successfully; it completed
    without timeout or external termination; it returned **exit status
    0**; and no required test, collection phase, or teardown phase was
    skipped or aborted.

    **Output showing `[100%]`, or the absence of an assertion failure,
    does NOT override a non-zero exit status.**

    A task specification may then simply say "validators must pass under
    Rule 14", rather than restating the exit contract each time. Where a
    task genuinely needs different semantics — an expected `exit 5`, a
    deselection, a known `xfail` — the specification states it explicitly.

    **Disposition vocabulary.** Three distinct dispositions:

    - **satisfied** — the criterion genuinely passed as written;
    - **not satisfied, PI-authorized exception** — the criterion was
      evaluated, was not met, and the PI accepts the task nonetheless;
    - **waived** — the PI removed the obligation BEFORE its evaluation
      deadline, so it was never evaluated.

    **A criterion already evaluated and found unsatisfied cannot later be
    reported as waived.** Its disposition is a retrospective exception, or
    task rejection. Waiving after the fact would erase an evaluation that
    actually happened.

    Every waiver and every exception records: the authorizing authority;
    the time or sequence point; the criterion affected; the specific
    evidence accepted; and whether it applies only to this task.
    Record an accepted timeout as:

        A9: NOT SATISFIED
        Disposition: PI-AUTHORIZED EXCEPTION
        Evidence: all selected tests reached completion output with no
                  assertion failure; process exit 124 after 120.2 s.

    **An exception changes the disposition of the TASK, not the historical
    evaluation of the CRITERION.** A criterion that was not satisfied is
    never written back as passed.

    *Incident.* Five validator files each reached `[100%]` with no
    assertion failure and each terminated at ~120.2 s with exit 124.
    Because all five stopped at the same boundary regardless of their
    individual cost, the cost lies in process shutdown rather than in the
    tests. The executor reported this as a qualified condition rather than
    as a pass, which was correct, but the acceptance criterion had no
    vocabulary for the outcome.


### 6.2 Rule 15, from the specification's §2 — landed at lines 659–678


    **A specification, a pre-execution review, a task report, and any
    manifest supplied to a checker are governing artifacts and are
    committed to the repository.** An artifact that determines what a task
    was authorised to do, or that records what it did, is not evidence
    while it exists only in a conversation.

    **Placement.** Specifications under `specs/`; task reports under
    `reports/`; pre-execution reviews under `reviews/<function>/`, using
    the existing directory for the reviewing party. Supplied manifests are
    reproduced in the report that used them.

    **Timing.** A specification is committed as the task's first commit.
    **A pre-execution review is committed before the work it authorises
    proceeds.** A report is committed by the task that produced it.

    **Prospective only.** Records created before this rule are not
    retrospectively non-conforming, and are not to be back-filled.


## 7. A5 — rules 1–13 unchanged apart from the four authorised insertions

**Check 1 — headings.** All thirteen numbers and titles are identical
between the evidence base and the landed file, in the same order:

    ### 1. Contradiction-stop                                        unchanged
    ### 2. Scope precedence                                          unchanged
    ### 3. Declared frozen scope is normative                        unchanged
    ### 4. Execution prompts are evidence                            unchanged
    ### 5. Minimum mandatory merge discipline                        unchanged
    ### 6. Reporting honesty for merges                              unchanged
    ### 7. Evidence precedence                                       unchanged
    ### 8. Responsibility separation (root rule of this section)     unchanged
    ### 9. Outcome-based task specification                          unchanged
    ### 10. Self-correction authority and its limit                  unchanged
    ### 11. Task granularity and integration boundary                unchanged
    ### 12. Acceptance criteria must be mechanically checkable       unchanged
    ### 13. Execution environment                                    unchanged
    ### 14. Validator outcome contract                               ADDED
    ### 15. Governing artifacts are committed                        ADDED

**Check 2 — byte identity of the remaining text**, which is the property
the heading check only proxies for. **The specification is right that
heading equality would not detect a rule silently reworded under an
unchanged heading**, so this was executed:

    remove from the landed file, as exact unique substrings:
      the Amendment A block
      the Amendment B block
      the Amendment C block
      the Amendment D block
      the two new rule sections
    compare the remainder to the evidence-base blob

    each block occurred exactly once and was uniquely removable
    remainder == evidence-base CONVENTIONS.md   ->  True

    base 24710 bytes  ->  landed 37426 bytes

**Nothing outside the five insertions differs by a byte.** No rule was
renumbered, reworded, reordered, or silently edited.

## 8. A6 — the `DECISION_LOG.md` entry

One new top-level entry,
`## 2026-08-09 — CONVENTIONS.md amendments A–D adopted; Rules 14 and 15
added`, with `Date: 2026-08-09`,
`Decision owner: Principal Investigator`,
`Effect: refines four execution-discipline rules and adds two new ones`.

**It records the adoption** with the amendment-to-rule mapping and the
statement that no rule was renumbered, reworded or reordered.

**It distinguishes the two provenances in its own section**, headed
*Two provenances, which are not the same*:

> **Amendments A–D and Rule 14 come from a previously approved amendment
> draft that had never been executed.** The draft is
> `DRAFT_amendment_reporting_and_digests.md`, SHA-256
> `bef29c4d0ef3d391f2caa2f17ab6336e11677c73a3c37cbf4acb17b23e566822`,
> supplied with the authorising specification. […]
>
> **Rule 15 does not come from that draft, and its text is not in it.**
> Rule 15 was approved by the PI separately, before this execution, and
> is supplied normatively by
> `specs/2026-08-09T1653Z_land-rules-14-15.md` §2. **That prior approval
> has no separate repository artifact; the authorising specification is
> the first durable record of it, and this entry is the second.** It is
> not recorded as extracted from an approved document, because it was
> not.

**It records that everything binds prospectively only**, that nothing is
back-filled, that no `reviews/` record was created because creating one
would be the back-filling Rule 15 forbids, and that this task is governed
by the rules in force at its evidence base — ordering, not exemption.

**It states plainly that Rule 15 changes the Reviewer function's
workflow**, not only the Executor's.

### 8.1 Both append-only measures

**Measure 1 — evidence base to branch head:**

    git diff --numstat f309f61c… 2e878f61…
      253     0       CONVENTIONS.md
      106     0       DECISION_LOG.md
      323     0       specs/2026-08-09T1653Z_land-rules-14-15.md

    deleted lines across the whole diff:  0

**Measure 2 — each commit against its parent:**

    0192871   deletions=0
    2e878f6   deletions=0

**Both zero.** `DECISION_LOG.md`'s landed blob has the evidence-base blob
as an exact byte prefix, with 4785 characters appended and nothing before
them touched. The file grew from 1517 to 1623 lines.

## 9. A7 — nothing else touched

Blob and tree hashes read from the git objects, evidence base against
pre-report head:

    AGENTS.md         5e60b5f…  IDENTICAL
    GATES.md          bd48205…  IDENTICAL
    pyproject.toml    9fc6fdd…  IDENTICAL

**Whole subtrees, compared as tree objects** — which covers every path
under each, present or absent, without enumerating:

    scripts/       tree 75f03934e5ff7ae131c64ae94851cb2342596fbf   IDENTICAL
    results/       tree 23fe5e80426a69feaf1f90f78cb187c396e1935a   IDENTICAL
    tests/         tree 422db3fd5170eada01b3393f5cfcf6bdc232372f   IDENTICAL
    derivations/   tree 75db5a9babe87cfb356a9cef466f7668483c5fe9   IDENTICAL
    reviews/       tree 0f9fc2d27fcc41b48cd3644762752858af3defd9   IDENTICAL

**`reviews/` is identical as a tree**, which is the strongest form of the
"create no `reviews/chatgpt/` record" prohibition: not merely that I
added no file, but that the subtree object is the same object. Its
contents at both ends:

    reviews/README.md
    reviews/chatgpt/.gitkeep
    reviews/claude/.gitkeep
    reviews/claude/2026-07-19-paper2-followup.md
    reviews/claude/2026-08-01-p2-lattice-ontology-01.md
    reviews/codex/2026-08-04-p2-phase-01-feasibility-inventory.md
    reviews/pi/2026-08-03-governance-tools-environment-authorization.md
    reviews/pi/2026-08-03-outcome-based-task-specification-amendment.md
    reviews/pi/2026-08-03-p2-dual-pipeline-probe-repin.md

**`reviews/chatgpt/` contains nothing but `.gitkeep`.** That is an
observation, not a complaint, and §12 uses it.

**`AGENTS.md` was not modified.** Its research rules are a different
numbering from `CONVENTIONS.md`'s execution-discipline rules, and this
task touched only the latter.

**No gate status changed.** `GATES.md` blob-identical, `^## P2-` anchor
count 14, `P2-GAP-01` still `PASS`, `P2-PHASE-01` still `PROPOSED`.

## 10. A8 — scope, and A9-pre

### 10.1 Scope

**Manifest template** (SHA-256
`cd4563c4ccb722ebd68870cf5df59f129b8b381b9ec44695a76d228e30e9113f`):

    {
      "base": "f309f61c9c14b0e2c63e078f9c0d0809422742e7",
      "head": "{PUSHED_HEAD}",
      "mode": "exact",
      "required": [
        {"operation": "add", "path": "specs/2026-08-09T1653Z_land-rules-14-15.md"},
        {"operation": "add", "path": "reports/2026-08-09T1653Z_land-rules-14-15.md"},
        {"operation": "modify", "path": "CONVENTIONS.md"},
        {"operation": "modify", "path": "DECISION_LOG.md"}
      ],
      "optional": [],
      "forbidden_operations": ["delete", "rename", "copy", "type_change", "unmerged", "unknown"]
    }

**Intended final resolution:** `head` set to the pushed head, all four
records required — **2 additions and 2 modifications.** A fifth path
would be a defect.

**Pre-report scope check** at `2e878f61…`, with the report record removed
because the report does not yet exist — checker output verbatim:

    {
      "base": "f309f61c9c14b0e2c63e078f9c0d0809422742e7",
      "failures": [],
      "head": "2e878f61319d98d83314865028ceb66ed7ccf3a9",
      "mode": "exact",
      "observed_operations": [
        {
          "operation": "modify",
          "path": "CONVENTIONS.md"
        },
        {
          "operation": "modify",
          "path": "DECISION_LOG.md"
        },
        {
          "operation": "add",
          "path": "specs/2026-08-09T1653Z_land-rules-14-15.md"
        }
      ],
      "overall": "PASS",
      "tool": "scope_checker"
    }

    exit status 0

**The final scope check at the pushed head is post-report evidence.**

### 10.2 A9-pre — validators at the pre-report head

Run individually with `python -m pytest <path>`, that exact invocation,
since `pytest` on this host resolves to 9.0.2 while `python -m pytest`
resolves to 9.1.1.

    tests/test_repository_structure.py    exit=0    4 passed
    tests/test_si1_governance.py          exit=0   14 passed
    tests/test_gate_anchors.py            exit=0   18 passed, 2 deselected
    tests/test_governance_tools.py        exit=0    8 passed

`pytest 9.1.1`, Python 3.11.15. **A9-final at the pushed head is
post-report evidence.**

### 10.3 What the validators assert about `CONVENTIONS.md` — nothing structural

A9 asks for this, and the answer is worth having.

    tests/test_repository_structure.py:18
      "CONVENTIONS.md" appears in a required-paths list. It asserts the
      file EXISTS. Nothing about its contents.

    tests/test_governance_tools.py:370, 403, 412, 561, 584, 622, 645
      "CONVENTIONS.md" is used as a FIXTURE PATH inside synthetic
      criteria — prefix_hash, changed_files, scope manifests — evaluated
      against historical commits BASE and MERGE, not against HEAD. They
      exercise the checkers, not the file.

**No validator asserts anything about `CONVENTIONS.md`'s structure, its
rule count, its numbering, or its headings.** A rule could be deleted,
renumbered or reworded and every validator would still pass.

**That is a finding, not a convenience.** The file this task edits is the
constitution-level document of the programme, and it has **less
structural testing than `GATES.md`**, which has `test_gate_anchors.py`
asserting its `^## P2-` anchors. The A5 byte-identity check I ran here
was written for this task and does not persist. **A standing test — rule
numbers contiguous from 1, no duplicates, headings matching
`^### <n>. <title>` — would have caught a renumbering that this task's
prohibitions rely on an executor not committing.** I did not add one:
`tests/` is protected under A7 and that is a separate authorization.

## 11. Does any existing rule now read as contradicted by 14 or 15?

**Reading all fifteen together, one genuine problem and three
near-misses that resolve.**

### 11.1 Rule 13 now contains two diagnostic orders, and the older one still reads as complete

**This is the real one.** Rule 13's pre-existing text says:

    **Environment failures SHALL be diagnosed in this order:** (1) execution
    identity; (2) interpreter availability; (3) permissions; (4) filesystem and
    workspace; (5) package availability.

Amendment D, landed 24 lines later in the same rule, says:

    Rule 13's diagnostic order is extended by a step before identity:

    (0) **execution location** — repository path, worktree identity,
        current branch or detached state, and the **resolved HEAD commit
        SHA**. …
    (1) execution identity; (2) interpreter availability;
    (3) permissions; (4) filesystem and workspace; (5) package
        availability; (6) **process and harness lifecycle** …

**Both enumerations now sit inside Rule 13, and the earlier one is
phrased as complete** — "SHALL be diagnosed in this order", with five
steps and no forward reference. Amendment D's opening clause does say
"is extended by", which signals supersession to a reader who reaches it.
**A reader who stops at the earlier paragraph gets five steps and never
learns about `(0)` or `(6)`** — and `(0)` is precisely the step whose
absence produced the incident Amendment D records.

**This is not a defect in the landing.** Amendment D's text is landed
exactly as approved, and §5's prohibition forbids rewording Rule 13's
pre-existing text. **The approved amendment was written as a standalone
proposal and reads correctly as one; it reads ambiguously once
concatenated into the rule it amends.**

**What would fix it**, in a later authorised task: one clause on the
pre-existing sentence — *"…(5) package availability; see the Amendment D
extension below, which supersedes this enumeration"* — or moving the
amendment's enumeration to replace it. **Both are rewordings of Rule 13
and are outside this task.**

**Note the shape.** This is Amendment G's own subject: a structural
change whose consequences propagate to text that does not name it. **The
amendment package predicted this class of failure and then produced an
instance of it.** That is not an argument against the package; it is
evidence that the rule is about a real and easy mistake.

### 11.2 Rule 4 and Rule 15 overlap, and Rule 4 asks for more

**Rule 4, in full:**

    ### 4. Execution prompts are evidence

    The execution prompt governing any decisive or pre-registered run MUST be
    committed to the repository, AND its sha256 recorded in the run's report,
    before the run begins.

**Rule 15 says** a specification is committed as the task's first commit,
and says nothing about a digest.

**Not a contradiction — Rule 15 is broader in coverage and weaker in
requirement.** Rule 4 covers "decisive or pre-registered runs" and
demands commit **plus digest in the report**; Rule 15 covers every task
and demands commit. **The risk is that a later reader takes Rule 15, as
the newer and more general statement, to be the complete obligation and
drops Rule 4's digest.** Both bind; nothing in either says otherwise.

**And this surfaces a live compliance question that is not mine to
answer.** Recent task reports — including this one — record the
specification's commit SHA and message but **not its sha256**. Whether
that breaches Rule 4 depends on whether a governance task is a "decisive
or pre-registered run", which Rule 4 does not define. **I raise it rather
than resolve it**, and note that landing Rule 15 is what made the overlap
visible.

### 11.3 Rule 12 and Rule 14's exception vocabulary — compatible

Rule 12 requires each acceptance criterion to have "a machine-executable
verification procedure with a defined expected outcome". Rule 14 adds
dispositions — `satisfied`, `not satisfied, PI-authorized exception`,
`waived`.

**No conflict.** Rule 12 constrains how a criterion is *written*; Rule 14
constrains how an *outcome* is dispositioned. A criterion can be fully
mechanical and still be dispositioned by exception, and Rule 14 is
explicit that the exception changes the task's disposition, never the
criterion's historical evaluation — which is exactly what preserves Rule
12's meaning.

### 11.4 Rule 3 and Amendment A — complementary, but Rule 3 is now incomplete alone

Rule 3 says the declared frozen file set "is authoritative… a file
outside it constitutes an authorization failure, not a matter of review
judgement", **and provides no amendment path.** Amendment A now
contemplates an amendment that "changes a frozen path manifest" and
requires the amended manifest be reproduced in full.

**Amendment A supplies the path Rule 3 lacked**, and the two are
consistent — the amended manifest becomes the declared set. **But Rule 3
read alone still says there is no judgement to be exercised**, and a
reader would not learn from Rule 3 that a manifest can be amended at all.
**Worth a cross-reference in a later task**; not a contradiction.

### 11.5 Rule 10 and Amendment A's retrospective ratification — different actors

Rule 10 governs what the **executor** may correct without returning for
authorization. Amendment A governs how a **PI** instruction issued after
an action is named and recorded. **Different actors, no overlap.**
Amendment A's insistence that retrospective ratification "does not make
the earlier action authorized at the time it occurred" strengthens Rule
10 rather than qualifying it.

## 12. Is Rule 15 executable by me?

**Three of its four clauses, yes and already. The fourth is not mine and
is assigned to nobody.**

    specification committed as the task's first commit      YES — every
                                                            recent task,
                                                            including this
    task report committed under reports/                    YES — every
                                                            recent task
    supplied manifests reproduced in the report             YES — §10.1
                                                            reproduces the
                                                            template and the
                                                            checker JSON
    pre-execution review committed under reviews/<function>/
      before the work it authorises proceeds                NOT EXECUTABLE
                                                            BY ME

**Why the fourth is not executable by me, stated concretely.** I never
receive a pre-execution review. Specifications arrive already issued; the
Reviewer's assessment of a specification reaches me, when it reaches me
at all, as changes to the specification itself. **I cannot commit an
artifact I have never seen, and I cannot commit it "before the work
proceeds" when the work is the first thing I am asked to do.**

**And Rule 15 does not say who commits it.** It states placement
(`reviews/<function>/`) and timing (before the work proceeds) but names
no actor. **Every other clause has an implicit actor — the task commits
its own specification and its own report — and this one has none.**

**Supporting observation.** `reviews/chatgpt/` exists at the evidence
base and contains nothing but `.gitkeep`, while `reviews/claude/`,
`reviews/codex/` and `reviews/pi/` all carry real records. **The
placement was provisioned and has never been used.** That is consistent
with the clause describing a step that is not currently anyone's, which
the specification itself says in §2: *"which is not currently anyone's
step."*

**What would make it enforceable**, offered as observation and not as a
proposal I am authorised to make: name the actor, and make the review's
absence detectable. **As written, a task that proceeds with no review
committed is indistinguishable from a task that needed none** — there is
no criterion an executor could fail, and no validator that could notice.
Rule 15's own rationale is that an artifact existing only in conversation
is not evidence; **an obligation with no actor and no detection is in the
same position.**

**None of this argues against landing it.** The clause is landed exactly
as supplied, and the gap is in enforceability, not in correctness.

## 13. A0 — commit order, SHAs and messages

    commit 1  01928711c9103c62554e92ff5fb15c27c3389ac5
              specs/2026-08-09T1653Z_land-rules-14-15.md
              "spec: land amendments A-D and new Rules 14 and 15"

    commit 2  2e878f61319d98d83314865028ceb66ed7ccf3a9
              CONVENTIONS.md, DECISION_LOG.md
              "docs: adopt amendments A-D and add CONVENTIONS.md Rules 14 and 15"

**No derivation note was written**, as A0 states none is required.

**The specification file is byte-identical to the specification as
issued**, with a single trailing newline added because the issued text
did not end in one and every other file in `specs/` does. 323 lines.

### Commit-message hygiene

Each message was written to a file, inspected for `Co-Authored-By`,
`Claude-Session`, `claude.ai`, `Generated with` and `http` before
committing, committed with `git commit -F <file>` and never `-m`, and the
stored message read back from the object afterwards with
`git log -1 --format=%B`.

    commit 1   trailers suppressed: Co-Authored-By, Claude-Session
    commit 2   trailers suppressed: Co-Authored-By, Claude-Session

**Suppression is a fact to disclose, not an absence.** This harness
appends both by default; `-F` prevents it, and the read-back confirmed
neither reached any stored message. The intended report commit message
was prepared the same way, with the same two suppressed:

    docs: report the landing of amendments A-D and Rules 14 and 15

    Records that CONVENTIONS.md now runs 1-15, with rules 1-13
    byte-identical after removing only the authorised insertions, and
    both append-only measures zero.

    Reports two findings. Rule 13 now carries two diagnostic-order
    enumerations and the older one still reads as complete, so a reader
    who stops there gets five steps and never sees (0) or (6); the fix
    is a rewording of Rule 13 and is outside this task. And Rule 15 is
    executable in three of its four clauses - the pre-execution review
    clause names no actor, cannot be performed by the executor, and has
    no detection.

    Also records that no validator asserts anything about
    CONVENTIONS.md's structure, numbering or rule count.

## 14. Repository inputs actually read, by path

    CONVENTIONS.md
    AGENTS.md                                    (digest only; not modified)
    GATES.md
    DECISION_LOG.md
    tests/test_repository_structure.py
    tests/test_governance_tools.py
    scripts/governance_tools/scope_checker.py
    reviews/  (tree listing only)

and, supplied with the specification rather than from the repository:

    DRAFT_amendment_reporting_and_digests.md
      bef29c4d0ef3d391f2caa2f17ab6336e11677c73a3c37cbf4acb17b23e566822

**Nothing was installed.** No science artifact was read: this task
touches governance text only.

## 15. Stops and clarifications

**No stop occurred.**

### `SPECIFICATION_DEFECT`

**None.** Two observations that are not defects:

**(a)** §7's literal table includes
`### 12. Acceptance criteria must be mechanically checkable`, but **no
amendment attaches to Rule 12.** The check reproduces and is harmless;
I take it as an insertion-target check that bounds Rule 13's start.
Reported so a reader does not look for a fifth amendment.

**(b)** §7's eighth literal is a proper prefix of the actual heading, not
the whole of it. **The containment check is the right check for locating
a section**, and the table says "EXACT LITERAL SUBSTRING", so it is
correctly labelled. Noted in §3 so it is not misread as heading equality.

### `ENVIRONMENT`

None. Nothing was installed.

### `OBSERVATION_METHOD_ERROR`

**None reached an output. One was caught during execution and is recorded
because the class matters.** My first attempt to quote the four inserted
blocks used line ranges computed from the pre-insertion file, which are
wrong for the post-insertion file — the first extraction returned Rule
8's body where Amendment B's label should have been. **The blocks were
then located by their label lines rather than by arithmetic**, and every
boundary was printed and checked before quoting. **A line number is a
coordinate in one version of a file, and this task edits the file it is
measuring.**

### `REPOSITORY_DEFECT`

**None**, and §10.3's finding is deliberately not filed here.
`CONVENTIONS.md` having no structural test is a gap in coverage, not a
violation of a frozen requirement, and calling it a repository defect
would misuse the category.

### `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`

**One, raised and not resolved.** Whether a governance task is a
"decisive or pre-registered run" within Rule 4, and therefore whether
recent reports breach Rule 4 by omitting the specification's sha256
(§11.2). **I have no authority to decide it and did not.**

## 16. Secondary findings, and what I would have specified differently

**1. A3's three-part reporting requirement produces (3) as a mechanical
consequence of (1) and (2).** The reason given for it is sound — "before
and after" is ambiguous between the whole rule and the insertion point —
but the fix is to name the two things, which (1) and (2) do. **(3) is
their concatenation and is byte-determined**, as A5's removal test
proves. I supplied it as a line range plus the seam and said why. **I
would specify (1), (2), and "identify (3) by range and show the seam"**,
which answers both readings without trebling the report.

**2. `CONVENTIONS.md` has no structural validator, and this task shows
why it should.** Every prohibition in §5 — do not renumber, do not
reword, do not reorder — is enforced by an executor choosing to comply
and by a report the reviewer must read. **The A5 byte-identity check that
actually establishes compliance was written for this task and does not
persist.** A standing test asserting contiguous numbering from 1, no
duplicates, and `^### <n>. <title>` headings would make the next
renumbering impossible to land silently. **`GATES.md` has this;
`CONVENTIONS.md` does not**, and `CONVENTIONS.md` is the more
constitutional of the two.

**3. Landing an approved standalone proposal into the rule it amends is
not a copy, and §11.1 is the proof.** Amendment D reads correctly as a
proposal and ambiguously as an insertion, because the pre-existing
sentence it extends is still phrased as complete. **A landing task should
be required to report, for each amendment, whether the host rule now
contains superseded text that still reads as operative** — which is a
narrower and more checkable question than "does any rule contradict".
I answered it here because §6 asked the broad version, but the narrow
version is the one that catches this.

**4. Rule 15's fourth clause needs an actor before it can bind.** §12
sets it out. **The rule is correct and I landed it unaltered**; the gap
is that no one is named, no executor can perform it, and no check can
notice its absence. **A follow-up that names the actor and adds a
detectable obligation would turn it from a statement of intent into a
rule.**

**5. Recurring, and raised for the ninth time — but now partly
addressed.** The `CONVENTIONS.md` index entry for the conventions a
computation consumes is still unwritten, and five decisions still live
only as dated `DECISION_LOG.md` entries, two of them located by exact
heading text by a script already on `main`. **Rule 15 does not fix this**
— it requires governing artifacts to be committed, and those entries are
committed; what is missing is that they are not discoverable from
`CONVENTIONS.md`. **The two are adjacent and are not the same
obligation**, and I note it here because landing Rule 15 could easily be
read as having closed it.
