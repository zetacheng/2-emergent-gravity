# Execution report — `P2-XI-HSPRESC-01`

    Task            P2-XI-HSPRESC-01, the decoupling prescription for the
                    assembled chain, definition only
    Specification   specs/2026-08-25T1200Z_xi-hspresc-01_v6.md
    Review          reviews/chatgpt/2026-08-25T1200Z_xi-hspresc-01_v6.md
    Verdict         APPROVE FOR EXECUTION
    Base            main @ b01bb18ba51008d09b64b442afad37b800b2d3d1
    Branch          science/xi-hspresc-01
    Result          INCONCLUSIVE — CONSTRUCTIVE GAP IDENTIFIED

This report records `M0` through `M5` and no later measurement. It names the
tested tree `T` and is itself the next commit on `T`. It does not state its
own commit SHA.

---

## Rule 18 / Amendment N — the binding, measured before the specification commit

    review declares   940d8d7820d6fd58ad728637808cf8aee7f17ff1cc38f38639a3a01508fdc497
    measured sha256   940d8d7820d6fd58ad728637808cf8aee7f17ff1cc38f38639a3a01508fdc497
    over              the 28850 supplied bytes, measured BEFORE the commit
    field present     yes, at the review's :4 and :205, identical
    equal             yes, as a full string
    verdict           APPROVE FOR EXECUTION, at :7 and :184

Provenance, recorded because the specification's own version note records it:
`v5` was returned unexecuted on a `REVISE BEFORE EXECUTION` verdict, before
`M0` ran and with nothing written. Its digest is not restated here — this task
did not measure it, and an identifier is used only at a value this task has
measured. Nothing from that return is carried into this task except the fact
of it.

After the commit, the committed blob was re-measured and equals the same
value. The review file's own sha256 is
`4aa3713c76fb8332da27c96c3e92aedcbca75a4305868fe9f6d3a53fb53e5e23`.

Rule 15 commit order on the branch: specification, then review, then the work
artifact, then this report last.

    8681b2bf482028de47263a8822e789cfe312d68e   spec
    0148972bac290605d2bf50cfd87efe5d181e7415   review
    a9c4d6ef56135dab109add60b6b8041b1217f870   the prescription artifact

---

## Amendment D step 0 — execution location

    worktree                  /home/user/2-emergent-gravity
    git dir                   .git
    shallow repository        false
    HEAD at entry             b01bb18ba51008d09b64b442afad37b800b2d3d1
    HEAD ref at entry         refs/heads/science/integrate-xi-rulings-03
    working tree at entry     clean
    branch cut                science/xi-hspresc-01, from the Base

    Python                    3.11.15
    pytest                    9.1.1
    numpy                     2.4.6
    sympy                     1.14.0
    ruff                      0.15.8
    scipy                     ABSENT

Observation about the local repository, recorded and not acted on: the local
ref `refs/heads/main` is at `6da1f7cb8ea1d28d7deadb8a938c67365b28384c`, which
is not an ancestor of, and has no commits in common ahead of, the canonical
`refs/remotes/origin/main` at `b01bb18ba51008d09b64b442afad37b800b2d3d1`. The
local ref is stale. The Base is a pinned SHA and was resolved as that SHA, so
nothing in this task depended on either ref. `refs/heads/main` was not
touched.

---

## `M0` — precondition and provenance

    Base measured             b01bb18ba51008d09b64b442afad37b800b2d3d1
    Base pinned               b01bb18ba51008d09b64b442afad37b800b2d3d1
    equal, full string        yes

`M0(a)`, as a pre-registered comparison — both values stand in the
specification, written before the measurement was taken:

    decisions/P2-XI-RULINGS-03.issued.md
      sha256 measured         1a982547f6c4a25ab29ec2d02e8ba54fa3e89c6871a80df395ac0d8b07418686
      sha256 pre-registered   1a982547f6c4a25ab29ec2d02e8ba54fa3e89c6871a80df395ac0d8b07418686
      blob id measured        0b331afb6f21f6591a0c3934fc8916bda742d8de
      blob id pre-registered  0b331afb6f21f6591a0c3934fc8916bda742d8de
      bytes                   5604
      both equal, full string yes
    decisions/2026-08-31-xi-rulings-03.md   present
      decision key            2026-08-31-xi-rulings-03, present at :5 and :39

`M0(b)`:

    decisions/P2-XI-RULINGS-02.issued.md
      sha256 measured         ab2e90ddb6fa8c24c9b913a26b4b455809ca358d82cff2d2256f3526957ebbf5
      sha256 pre-registered   ab2e90ddb6fa8c24c9b913a26b4b455809ca358d82cff2d2256f3526957ebbf5
      equal, full string      yes

`M0(c)`:

    derivations/P2-XI-LEDGER-01_conditional-analytic-ledger.md
      sha256                  aa0c79e21568b09d6efed64ec538c1ee9b4892ebc65653cb76deecfbd25f1454
      bytes                   23217
      row at :305             condensate scalar's own fluctuation loop, both
                              value columns `—`, status OPEN(Q-M2)
      row at :307             Hubbard–Stratonovich Jacobian / normalization
                              term, both value columns `—`, status OPEN(Q-M3)

**`C0` satisfied. No `A0` condition met.** No write of any kind preceded these
measurements.

---

## `M1` — provenance extraction

Every node the specification names was located at the Base and appears in the
artifact as a verbatim quotation with a `path:line` that resolves there. No
node was missing; **no `A1` condition met**.

    the R-1 ruling, in full            decisions/P2-XI-RULINGS-03.issued.md:1-105
    the 2026-08-09 ruling, in full     DECISION_LOG.md:1749-1781
      its three limits                 within those bytes at :1766-1767,
                                       :1773-1776, :1778-1781
    the prescription requirement       derivations/P2-FIERZSUM-01.md:218-220
    the determination table, both      derivations/P2-XI-QM3-DEP-01_hs-jacobian-
      lists, in full                     curvature-dependence.md:302-335
    the landed g = +2c authority       DECISION_LOG.md:1244-1281, the mapping
                                       itself at :1258-1262
    the no-channel-selected statement  derivations/P2-PHASE-01_channel_character.md:7-10
                                       and :371-377
    the landed assembled chain         derivations/P2-NORM-01_normalization_chain.md:23-28,
                                       its G_c node at :50-52, the landed
                                       derivation of that node at
                                       derivations/P2-GAP-01_gap_criticality.md:30-54,
                                       and the ledger's own statement of the
                                       chain at
                                       derivations/P2-XI-LEDGER-01_conditional-
                                       analytic-ledger.md:26-32
    DET-01's status statement          derivations/P2-SRC-B0_source-side-scope.md:186-188,
                                       with the adjudication at
                                       derivations/P2-BETAV-DET-01_measure-
                                       adjudication.md:226-245 and its null
                                       result at :167-169

**The determination-table check the specification made an `A1` trip.** The
table at the Base places the exponent mapping in its "Fixed by landed text"
list at `:308` and names exactly two entries in its "Not fixed by landed text"
list — the channel at `:312-313` and the decoupling prescription at
`:325-326`. That is what the specification says it should read. `A1` is not
met on this node and this task did not proceed on the specification's account
of it: the table's own bytes were extracted and quoted.

`C1` satisfied: every extracted node appears as a verbatim quotation verified
by containment against the file's bytes.

### The quotation discipline, and how it was enforced

No quotation in the artifact was typed from a terminal rendering or from
memory. The artifact was generated by splicing byte ranges read directly from
the Base's blobs with `git cat-file blob`, and was then checked a second time
by an independent verifier that re-reads the **committed** artifact bytes and
re-resolves every quotation against the Base's blobs without reference to the
generator:

    corpus files resolved at Base                     15
    fenced block quotations found                     29
      not byte-contained in a cited Base blob          0
    inline quotations found (curly-quoted)            14
      not contained under whitespace collapse          0

    POSITIVE CONTROL
      unmodified first block found in corpus         True   (expected True)
      corrupted  first block found in corpus        False   (expected False)
      unmodified first inline found in corpus        True   (expected True)
      corrupted  first inline found in corpus       False   (expected False)

    QUOTATION VERIFICATION                            PASS

The two normalizations are stated in the artifact itself: fenced block
quotations carry normalization NONE; inline quotations carry whitespace
collapse and nothing else.

---

## `M2` — the elements

    E1  the HS channel and auxiliary-field content   FIXED   LANDED-DERIVED
    E2  the exact exponent convention                FIXED   LANDED-DERIVED
    E3  the g↔c mapping                              FIXED   LANDED-DERIVED
    E4  constraints and integration contour          FIXED   LANDED-DERIVED
    E5  the functional-measure treatment             FIXED in part, LANDED-DERIVED;
                                                     the measure's normalization
                                                     ROUTED under A3
    E6  independent versus dynamically generated     FIXED   LANDED-DERIVED
    E7  the mathematical definition of N_α[g]        ROUTED under A3

**No element is marked `PRESCRIBED-HERE`.** Everything this prescription
fixes, landed text already fixed; where landed text stops, this task routed
rather than chose. That is the honest shape of the result and it is recorded
as such in the artifact at §5.2.

`E3` was recorded `LANDED-DERIVED` with its authority quoted, was not routed,
and was not re-adjudicated. No `A5` was declared on it and none was met: no
apparent need to choose a sign arose.

`C2` satisfied: each element is either fixed with its basis cited and marked,
or recorded as routed with its choice stated; none is fixed by default, by
elimination, or by a judgement of what is likely. `E5` assumes no invertible
change of variables. `E7` records its identification of the object's
mathematical form, with the landed text that identification rests on, before
any defining expression — and no defining expression follows, because the
element is routed.

### `C3` — the prohibited-term check, and its enumeration

The enumeration rule is stated in the artifact at §0, **before** any
conditioned statement is written, and is drawn from the artifact's own
structure: a line outside every fenced block whose bytes end with
`| COND-R, COND-M**` or `| COND-R, COND-M, COND-J**`. The rule is fence-aware
because landed text quoted in the artifact carries its own source's `COND`
markers — one such line is inside the block quoted at the artifact's §2.4 —
and those are that source's statements, not this artifact's.

Measured over the committed bytes:

    total lines                                     1157
    fence delimiter lines                             66  (balanced)
    lines outside fences                             681
    CONDITIONED STATEMENTS ENUMERATED                 12

The twelve, by line in the committed artifact: `:702`, `:723`, `:746`,
`:775`, `:802`, `:804`, `:849`, `:877`, `:955`, `:972`, `:1003`, `:1052`.

The check asks whether any of the twelve evaluates `N_α[g]`, states its
curvature dependence, its sign, its magnitude, its `L`-scaling, or a `Q-M3`
verdict:

    PROBE v2 hits                                      0
    POSITIVE CONTROL, synthetic offending statement  trips the check (expected)
    POSITIVE CONTROL, ordinary-adjective statement   does not trip (expected)

    C3                                              PASS

Occurrences of the prohibited terms elsewhere in the artifact — in quoted
landed text, in the prohibitions of §5.3 and §7, and in statements that
something was NOT done — are context, not failures, and the check does not
reach them.

`C8` satisfied: all twelve carry `COND-R` and `COND-M`; the `E7` statement
additionally carries `COND-J`; `COND-J`'s sentence appears in the artifact at
§0.

---

## `M3` — consistency with the landed chain

Recorded in the artifact at §4 as a comparison with both sides shown, not as
an assertion. The channel the chain's `G_c` node was derived in and the
channel `E1` fixes are the same; the auxiliary-field character is the same;
the admissibility condition the landed identity states is met for this channel
under `E2` and `E3` by the landed branch table. No adjustment was made to
either side.

On the exponent convention: `E2` is fixed and `E3` arrives `LANDED-DERIVED`,
and together they determine one convention with no `±` left open. The
INCONCLUSIVE branch's exponent clause therefore does not arise — no exponent
ambiguity is carried to `M4`.

The artifact records at §4.3 what the comparison does not cover: the frozen
quartic carries an `S` and a `P` family each summed over `A`, the chain
reaches that quartic only through `G_c`, and landed text derives `G_c` from
the scalar-channel treatment alone. The prescription decouples that channel.
No landed line requires that it decouple the whole quartic, and none is
contradicted by recording that it does not.

**No inconsistency between a fixed definition and landed text was found. `A5`
is not met.** The two routed elements are unresolved for want of authority,
and `M3` does not report either as a contradiction.

`C4` satisfied.

---

## `M4` — the binary determination

    RETURN   INCONCLUSIVE — CONSTRUCTIVE GAP IDENTIFIED

Blocking routed elements, each with what its routing blocks:

    E7   the defining expression for N_α[g]
         blocks the uniqueness of the normalization object itself
    E5   the normalization of the auxiliary functional measure
         blocks E7; it is the first of E7's two named requirements

Both bear on uniqueness, so under `A3` this return follows however many
elements were fixed. The six fixed elements are recorded as gap
characterization, not promoted into a completed prescription.

`E7`'s identification is recorded even though the element is routed: the
landed construction adjoins an auxiliary Gaussian integration to a fermionic
integration it does not remove, so the object relating the two partition
functions is the normalization that adjoined integration carries — **not** the
Jacobian of an invertible change of variables, because landed text establishes
no such change of variables.

The artifact states in these terms that it does not supply `Q-M3`'s subject.
No intermediate or partial-uniqueness return appears anywhere in it.

**One available reading was refused and the refusal recorded.** The landed
identity is written with no normalization prefactor; read literally as a
functional statement it would make `N_α[g]` the identity and settle what
`Q-M3` asks. Taking it would have been choosing the functional measure (`A3`,
outside this task) and stating its consequence would have been a `Q-M3`
verdict (`A2`). Neither was done.

### Rule 22 — subclass and resolution path

    Subclass         CONSTRUCTIVE GAP. Two named landed conventions are
                     absent, each suppliable by a bounded act of authority.

    R-1'  a landed statement fixing the functional measure for the real scalar
          auxiliary field. The three alternatives that would each settle it
          are already named in landed text at
          derivations/P2-BETAV-DET-01_measure-adjudication.md:236-245, quoted
          in the artifact; the path names all three and prefers none.

    R-2'  a landed statement promoting the single-variable HS identity at
          derivations/P2-PHASE-01_channel_character.md:144 to a functional
          integral over the auxiliary field in a curved background.

    Symmetry, on the Researcher's reading: R-1' and R-2' together would let E7
    be written; neither alone would. Nothing in either is written so as to
    make one answer to the later Q-M3 question easier to reach than another.

    **Resolution path, symmetric, defined here and NOT walked.**

`C5` satisfied.

---

## `M5` — suite

Run on a full, non-shallow tree.

At the Base, `b01bb18ba51008d09b64b442afad37b800b2d3d1`:

```text
344 passed, 2 deselected in 36.11s
```

At the post-`M4` tree `T = e54392a0038cfb0b1e4b16c6818b8b56c24778c9`:

```text
344 passed, 2 deselected in 40.05s
```

**No test fails on `T` that passes at the Base.** `C6` satisfied.

---

## Stops and clarifications (Amendment B)

**Primary category: `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`.**

Two elements of the prescription cannot be fixed from landed text and are
routed to the PI under `A3`: the normalization of the auxiliary functional
measure (part of `E5`), and the mathematical definition of `N_α[g]` (`E7`).
Fixing the first would require choosing `𝔊` and so resolving `DET-01`; fixing
the second additionally requires a promotion of the landed algebraic identity
to a functional one that no landed line states. Neither is within this task's
authorization, and neither was taken. The symmetric resolution path is defined
in the artifact at §6.1 and is not walked.

**Secondary category: `OBSERVATION_METHOD_ERROR`** — two probe defects, both
found and corrected **before** the artifact commit, neither treated as an
abort, both recorded here.

1. A line-range probe for an inline quotation assumed the phrase
   `(the dynamical self-energy)` lay within
   `derivations/P2-GAP-01_gap_criticality.md:33`. It spans the wrap between
   `:33` and `:34`. The probe raised on its own assumption; the range was
   re-measured as `:33-34` and the citation in the artifact carries the
   measured range.

2. The `C3` prohibited-term probe was first written as a case-insensitive
   substring match, which flagged the `E6` statement on the word
   `independent` — an ordinary adjective describing the fermion integration
   variables, not the `Q-M3` verdict token `INDEPENDENT`. The probe failed
   against its own assumption, not against the evidence. It was re-written to
   match the verdict vocabulary as whole words and case-sensitively, and both
   probes are reported above: v1's two hits are recorded as the probe defect
   they are, and v2 returns zero with a positive control in each direction —
   a synthetic offending statement trips it, an ordinary-adjective statement
   does not. **No `A2` was declared on the false failure.**

Nothing else stopped. No `A0`, `A1`, `A2`, `A4` or `A5` condition was met.
`A3` was met twice and, as the specification provides, routing continued the
measurement rather than aborting it — and it did not lower `M4`'s bar.

---

## Deliverables

    derivations/P2-XI-HSPRESC-01_assembled-chain-decoupling-prescription.md
      sha256      a478154dbaf266cfd389a808f82d5cabe1533a0868b0775ccb137a7ef20a4e41
      bytes       54544
      commit      a9c4d6ef56135dab109add60b6b8041b1217f870
      tree        T = e54392a0038cfb0b1e4b16c6818b8b56c24778c9

    reports/2026-09-03T1919Z_xi-hspresc-01.md — this file, the next commit
      on T

Integration is a separate task and is not performed here. The push scope is
the `science/*` branch of `docs/BRANCHING_POLICY.md`; nothing else is pushed.

END OF REPORT
