# Execution report — `P2-POLE-B0-INTEG-01`

    OUTCOME     A4 ABORT — the governing clause of docs/BRANCHING_POLICY.md
                cannot be identified for a science/* branch.
                NO MERGE. NO LANDING. NO REGISTER ENTRY.
                Returned to the Researcher.

**Task:** `science/integrate-pole-b0`
**Specification:** `specs/2026-08-19T0319Z_pole-b0-integ-01.md`
**Review:** `reviews/chatgpt/2026-08-19T0319Z_pole-b0-integ-01.md`
**Integration base:** `11af14a792c5858b368180d99ab9ee4692a7f698`
**Branch to integrate:** `science/pole-b0-milestone-scope @ 3f78acfebd7e994460eb30ac54ffd31a5cf3c21f`

**`main` was not touched.** No merge commit exists. No entry was written to
`derivations/P2-DEFERRED-ITEMS.md` or to `docs/GOVERNANCE-DEBT.md`. This
branch carries the specification, its review, and this report — **the record
of a refusal, not of failed work**, in the form
`docs/BRANCHING_POLICY.md:190-192` already names for the one prior instance.

**Every §2 measurement was completed before the abort was declared**, because
they are reads and they are what the re-issue will need. Two of them —
**M6 and M7** — independently return a classification to the Researcher on
their own terms, and one of them corrects a claim in my own `POLE-B0` report.

---

## §0 — Binding SHAs (MEASURED, no A1 abort)

    origin URL, verbatim      https://github.com/zetacheng/2-emergent-gravity
    observed refs/remotes/origin/main
                              11af14a792c5858b368180d99ab9ee4692a7f698
    §0 declared base          11af14a792c5858b368180d99ab9ee4692a7f698
    observed branch tip       3f78acfebd7e994460eb30ac54ffd31a5cf3c21f
    §0 declared branch tip    3f78acfebd7e994460eb30ac54ffd31a5cf3c21f

**Both match. Neither has advanced. This is not a stale-base stop.**

**Review binding (Rule 18, Amendment N).** The field
`Reviewed specification SHA-256:` is **present**, once, populated — checked
before its value. Computed over the specification bytes as committed:
`6ee7c8154516b94b9f9a4f6fee6c3b2a51a6fbf48777cf02e717d30d832634d1`; the
review's bound SHA is the same. **MATCH.** Verdict `APPROVE FOR EXECUTION`.

---

## Environment (MEASURED)

**Amendment D step 0.** Execution location
`/home/user/2-emergent-gravity` for the ref reads, then the linked worktree
`…/scratchpad/ipole`, created at the integration base. Resolved HEAD
`11af14a792c5858b368180d99ab9ee4692a7f698` at creation.

**Rule 13's diagnostic order.** Not shallow; 423 commits on HEAD, 555 across
all refs; Python 3.11.15; pytest 9.1.1; numpy 2.4.6; sympy 1.14.0; ruff
0.15.8; **`scipy` ABSENT.** **No environment failure occurred, so neither of
Rule 13's two diagnostic orders was exercised** as a failure path.

---

## §2 — Pre-execution measurements

### M1 — Test suite on the merge product

    invocation   python3 -m pytest -q
    result       332 passed, 2 deselected in 50.19s
    exit status  0

    comparison run at the base 11af14a7, same invocation
                 332 passed, 2 deselected in 48.47s, exit 0

**No A3 abort. No failure on the merge product, and none at the base.**

**This figure is a correction of my own first measurement, and the cause
matters.** My first attempt materialised the merge product with
`git archive | tar -x` into a plain directory and ran the suite there. It
returned **6 failed, 326 passed, 2 deselected, exit 1** —

    tests/test_betav_campaign_guards.py::test_guard4_compare_five_checks_in_order
    tests/test_governance_tools.py::test_consistency_checker_distinguishes_conflict_and_underspecification
    tests/test_governance_tools.py::test_consistency_checker_uses_resolved_domains_and_validates_schema
    tests/test_governance_tools.py::test_self_application_of_all_four_tools
    tests/test_task_checker.py::test_real_history_landing_task_satisfies_rule_15_order
    tests/test_task_checker.py::test_real_history_register_is_read_from_the_shipped_policy

**All six are git-history-dependent governance tests, and the extraction was
not a git repository** — `git rev-parse --is-inside-work-tree` returned
*"fatal: not a git repository"* inside it. **The failures were produced by my
measurement method, not by the merge product.**

**The corrected method, recorded because it is what makes the number
trustworthy.** I built the merge tree with `git merge-tree --write-tree`, then
created an **unreferenced** merge object with `git commit-tree` naming both
parents, and checked it out **detached** in a real worktree. That object is
reachable from no branch, was never pushed, and the worktree has been removed.
**Creating an object is not landing it**, and this route gave a genuine git
repository whose working tree is the merge product without performing the
merge action §4 governs.

**Had I reported the first run, this task would have declared a spurious A3
abort on top of a real A4 one, and the six named tests would have entered the
record as merge-product failures.**

### M2 — `P2-*` artifact count on the merge product

    command   git ls-tree -r --name-only HEAD derivations/ | grep -c '/P2-'
    observed  52

    command   ls derivations/ | grep -c '^P2-'
    observed  52

    base 11af14a7, same command   51

**52 on the merge product, 51 at the base.** The one addition is
`derivations/P2-POLE-B0_milestone-scope.md`. Two independent commands agree.

**§2's closing paragraph applies and is honoured:** the handover's figures are
historical observations against a different tree, and nothing here was
adjusted toward them.

### M3 — Ancestry

    git merge-base --is-ancestor 3f78acfe refs/remotes/origin/main
      exit 1

**`3f78acfe` is NOT currently an ancestor of `main`.** The branch is unlanded,
which is the expected pre-integration state.

### M4 — Dry-run merge

    git merge-tree --write-tree 11af14a7 3f78acfe
      exit 0
      tree e2b67705fc76afac6923432e0182369557ded325
      conflict lines: 0

**Conflict-free. No A2 abort.**

### M5 — `git diff --name-only 11af14a7..<merge product>`

    derivations/P2-POLE-B0_milestone-scope.md
    reports/2026-08-19T0223Z_pole-b0-milestone-scope.md
    reviews/chatgpt/2026-08-19T0223Z_pole-b0-milestone-scope.md
    specs/2026-08-19T0223Z_pole-b0-milestone-scope.md

    count 4

**C9 — paths under `scripts/`: ZERO.** The criterion does not assert the set
is empty; it requires that whatever M5 observes be reported. **What M5
observes is that no `scripts/` path arrives**, and no `tests/`,
`pyproject.toml`, `GATES.md`, `CONVENTIONS.md` or register path arrives
either. All four arriving paths are additions of Markdown documents.

### M6 — The two `scipy` statements, read on the merge product

**Read, not confirmed. Both locations were measured; neither was assumed.**

**Statement one.**

    CURRENT LOCATION   pyproject.toml:12, inside [project] dependencies
                       at :10-14
    AS READ            "scipy>=1.11", listed alongside "numpy>=1.26" and
                       "sympy>=1.12" as a dependency of the project
                       "emergent-gravity-research".
    MEANING            scipy is declared as a dependency OF THE PROJECT.

**Statement two.**

    CURRENT LOCATION   scripts/recon2026/proca_curved.py:42-43
    AS READ            "Only ``numpy`` is used.  ``scipy`` is not a declared
                       package of this environment and is not installed."
    MEANING            two clauses — (a) scipy is not a declared package OF
                       THIS ENVIRONMENT, and (b) scipy is not installed.

**The location has MOVED relative to the figure §6 R-2 quotes for
orientation.** The specification quotes `:43-44`; **the measured location is
`:42-43`.** The specification was explicit that its figures are not targets
and that a moved location is to be recorded as observed. It is recorded as
observed, and it was not adjusted toward the quoted value.

**This also corrects my own `POLE-B0` report**, which cited the same statement
at `:43-44` in its A3 section and again in its Stops section. **The measured
location is `:42-43`.** The blob is unchanged between the base and the merge
product, so this is a citation error in that report and not a change in the
file.

#### Are the two statements mutually contradictory as read? — NO, and this is the material finding

**They are not, and the reason is a third document neither statement names.**

    docs/local/execution_environment.md:10
      | Required packages | `pytest`, `ruff`, `numpy`, `sympy` |

**That is the repository's declaration of the required packages for an
execution environment, and `scipy` is not among them.**

So `proca_curved.py:42-43`'s clause (a) — *"not a declared package of this
environment"* — **is TRUE against the document that declares this
environment's packages.** Clause (b), *"and is not installed"*, is also true,
confirmed independently by the import failure recorded above. **Both clauses
of statement two are true.**

**`pyproject.toml:12` and `proca_curved.py:42-43` do not have the same
subject.** One declares a dependency **of the project**; the other denies a
declaration **of the environment**, and the repository maintains those as two
separate surfaces in two separate files.

**So M6 does NOT reproduce the contradiction as the specification frames it,
and per §6 R-2 and C7b, no R-2 entry is created.** The classification returns
to the Researcher, and §6's own text governs that outcome: *"If M6 does not
reproduce it: no R-2 entry is created… The merge itself is unaffected."*

**There IS a real inconsistency in the repository, and it is between a
different pair.** `pyproject.toml:12` declares `scipy>=1.11` as a project
dependency; `docs/local/execution_environment.md:10` lists the required
packages without it. **Whether those two are a defect or a deliberate
separation of project dependencies from validator-environment requirements is
a classification I do not make** — it is outside M6's terms, which name a
specific pair, and outside this task's scope.

**I am correcting my own prior report on this point.** The `POLE-B0` report's
A3 and Stops sections state *"The manifest says `scipy` is declared. The code
says it is not"* and treat that as a contradiction inside the repository.
**On a careful read of the referent "this environment", it is not one.** The
code's statement is true against `execution_environment.md:10`. The
inconsistency I described exists, but between `pyproject.toml` and
`execution_environment.md`, not between `pyproject.toml` and the code — and
that difference is exactly what M6 was written to catch. **M6 caught it, on a
claim of mine.**

### M7 — Status vocabulary of the deferred-items register

**The register is `derivations/P2-DEFERRED-ITEMS.md`**, identified from
`docs/GOVERNANCE-DEBT.md:16-19`, which names it as one of the two pre-existing
science-side registers.

**It defines NO status vocabulary.** There is no list of legal values anywhere
in its 253 lines. The four existing entries carry free-text statuses:

    :46   DEFERRED-01   **Status:** deferred, not excluded.
    :89   DEFERRED-02   **Status:** deferred, neither admitted nor excluded.
    :151  DEFERRED-03   **Status: PI HYPOTHESIS, UNTESTED.**
    :204  DEFERRED-04   **Status:** deferred, neither admitted nor excluded.

**`PROPOSED` is never used as a status in this register.** It occurs twice, at
`:187` and `:253`, and in both places it refers to `P2-PHASE-01`'s **gate**
status remaining `PROPOSED` — a different object in a different file.

**Per §6 R-1, this returns to the Researcher:** *"If it defines no
corresponding value, record the vocabulary observed and return to the
Researcher rather than coining a status token here."* **The vocabulary
observed is recorded above. No token was coined.**

**Two further observations, made because they bear on whether R-1 belongs in
this register at all, and neither is a decision.**

**First, `:191`: "Entries are added by PI decision."** R-1 as specified would
be added by an executor.

**Second, the register's own scope excludes items of R-1's kind.** `:12-17`:

> **This register holds work that has been CONSIDERED and consciously
> postponed.** It is not a list of open questions or of things not yet
> thought about. **The distinction is the point**: an open item may simply
> never have been examined, while an entry here was examined and deferred
> with a reason, and carries the PI's position at the time of deferral.

and `:19-22` directs open questions elsewhere — to the `OPEN-AC-*` and
`OPEN-PD-*` items and to `DECISION_LOG.md` entries opening an item as
`UNESTABLISHED`.

**§6 R-1 describes its item as "raised but not scoped".** Whether that is
*considered and consciously postponed* or *an open question* is a judgement
about which register it belongs in, and I do not make it. **Recorded and
returned.**

---

## §4 — Merge action: the A4 abort

**`docs/BRANCHING_POLICY.md` contains no clause governing integration of a
`science/*` branch, and no merge-mode prescription for any branch class.**

**The evidence, by reading and by exhaustive search with the exclusions
stated.**

**One — `science` does not appear in the file at all.**

    grep -c -i 'science' docs/BRANCHING_POLICY.md    0

**Two — the branch-name taxonomy at `:5-11` does not contain the class.**

    gate/<gate-name>
    paper/<paper-version>
    review/<review-topic>
    fix/<issue>
    archive/<retired-route>

**Five prefixes. `science/` is not among them.** The branch this task was to
integrate belongs to a class the policy does not name.

**Three — the Rules section `:13-22` prescribes no merge mode.** Its eight
bullets govern what `main` contains, where active calculations live, that
failed branches are preserved, that history is never squashed, commit-message
preference, tags, one-branch-one-task, and paper-branch `.tex` timing.
**`:18` — "Never squash scientific derivation history" — is a PROHIBITION on
one mode, not a prescription of another.** It leaves `--no-ff` and
fast-forward equally permitted and equally unchosen.

**Four — the merge-mode vocabulary is absent.** Measured over the whole file:

    no-ff          0        rebase        0
    fast-forward   0        merge mode    0
    fast forward   0        squash        1   (the :18 prohibition)

**Five — the two integration statements in the file govern a different
question.** `:109` — *"A superseded branch MUST NOT be integrated"* — is a
prohibition applying to a class `science/pole-b0-milestone-scope` is not in.
`:117` states that deletability and integrability are independent questions.
**Neither prescribes how an integrable branch is to be merged.**

**Six — the policy delegates to no other document**, and no other document
supplies the clause. Searched `CONVENTIONS.md`, `docs/`, `GATES.md` and
`AGENTS.md`: **`science/` as a branch class returns zero**, and
`--no-ff`/`fast-forward` return zero.

**Substring exclusions, per §8.** The `merge` search was performed by reading
every one of its 20 matching lines rather than by counting; 12 of them are in
the deletion-state machine and the supersession register, where "merged" is a
deletion precondition, not a merge instruction. The `pole` collision §8 names
did not arise in this section — `pole` is not searched here — and the
`integrat` search was likewise resolved by reading all five hits, listed
above.

### The abort

**§4 states the consequence in its own words:**

> If the policy is silent on this branch class, that is an **A4 abort**: the
> ruling request is raised by the Researcher, not decided by the executor.

**The policy is silent on this branch class. This is an A4 abort.**

**§3 governs what follows: "Execution stops, with no partial landing, and
returns to the Researcher."** No merge was performed. `main` remains at
`11af14a792c5858b368180d99ab9ee4692a7f698`.

**What I did not do, and why each would have been the executor deciding a
ruling.** I did not merge `--no-ff` on the ground that four prior integrations
in this line did so — **precedent in task specifications is not a clause in
the policy**, and §4 directs the executor to the policy. I did not
fast-forward on the ground that the merge is conflict-free and the branch is a
descendant — that would choose a mode the policy does not choose. I did not
read `:18`'s squash prohibition as selecting `--no-ff` by elimination —
prohibiting one of three modes does not select between the remaining two. I
did not treat `science/` as an unlisted synonym for `gate/` — the taxonomy at
`:5-11` is a list of five, and reading a sixth into it is an amendment.

**One observation the Researcher may want, offered as an observation and not
as a proposal.** **MEASURED: thirty-seven `science/*` branches are already
ancestors of `main`** — counted by running `git merge-base --is-ancestor`
against `refs/remotes/origin/main` for every `origin/science/*` ref — landed
by earlier tasks whose specifications each prescribed a merge mode directly. **The policy was never updated to name the class.** So
the gap this abort reports is not new to this task; it is the first task whose
specification delegated the mode to the policy rather than stating it, and
that delegation is what made the gap visible. **Whether the remedy is to amend
the policy, to name the class, or to have the specification prescribe the mode
as its predecessors did, is a PI decision and is not made here.**

---

## §5 — Landing record

**Not produced. There was no landing.**

The two-layer blocker structure of §5.1, the transcribed accounting of §5.2,
the load-bearing `N_both = 0` finding of §5.3, the two dependency-transfer
statements of §5.4, the three negative delimitations of §5.5, the verdict of
§5.6 and the polarity requirement of §5.7 **all remain to be written by the
re-issued task.** Nothing about them is in question — the branch report at
`3f78acfe` carries all of it — and none of it is transcribed here, because a
landing record that records no landing would be a false document.

**A5 was not reached.** The §5.2 accounting could not fail to match a
transcription that was never made. For the re-issue's benefit only, and as a
read rather than a transcription: the branch's own execution report at
`3f78acfe` states `N_total = N_both + N_impl + N_spec + N_neither` = `12 = 0 +
3 + 3 + 6`. **That is quoted here as evidence that the source text exists and
is legible, not as the §5.2 transcription**, which belongs in a landing record.

---

## §7 — Acceptance criteria, evaluated

**Per §10.4, a criterion that cannot be evaluated is recorded INCONCLUSIVE,
never PASS.** That clause is written for the Researcher's verification pass; I
apply it to my own, because the alternative is to report criteria as passing
on the strength of an abort.

    C1   INCONCLUSIVE   no merge; 3f78acfe is not an ancestor of main
    C2   INCONCLUSIVE   no landing record exists to read
    C3   INCONCLUSIVE   no transcription was made
    C4   INCONCLUSIVE   no landing record exists
    C5   INCONCLUSIVE   no landing record exists
    C6   PASS           M1, M2, M5, M6, M7 recorded above with observed
                        values and the commands or reads used
    C7a  INCONCLUSIVE   R-1 not created; M7 returned to the Researcher
                        because the register defines no status vocabulary
    C7b  PASS           R-2 does not exist, M6's observation is recorded,
                        and the classification is returned. C7b's own text
                        passes on exactly this branch: "C7b does not require
                        R-2 to exist."
    C8   INCONCLUSIVE   no landing record exists
    C9   PASS           M5 observed and reported; zero scripts/ paths

**C6, C7b and C9 are satisfiable without a landing and are satisfied.
Everything else awaits the re-issue.**

---

## §1a — Non-objectives, all honoured

The POLE-B0 verdict was **not** re-adjudicated and POLE-B0 was **not** re-run.
`R1`, `R2`, `R3` and `R4` were **not** ruled. The scipy question was **not**
resolved — M6 recorded and returned it. The POLE construction layer was
**not** scoped, designed or begun. No `RECON-01a` artifact was modified or
reinterpreted. The `r = 1` conflict and the status of `M` were **not**
adjudicated; §12 lists them for PI registration and this task did not action
them.

**And the abort itself was not converted into a decision.** §4's silence was
reported as silence.

---

## Stops and clarifications

**`SPECIFICATION_DEFECT`** — **none.** This is the point worth stating
plainly: **the A4 abort is not a defect in the specification.** §4 anticipated
the case in its own text, named the outcome, and assigned the ruling to the
Researcher. **The specification worked.** It delegated a decision to the
policy, the policy turned out to be silent, and the specification had already
said what to do about that. Likewise M6's non-reproduction and M7's absent
vocabulary are outcomes the specification enumerated in advance, each with its
own branch. Three of this task's findings were pre-registered by the document
that produced them.

**`ENVIRONMENT`** — `scipy` absent for the fourteenth consecutive task. No
environment failure occurred, so neither of Rule 13's two diagnostic orders
was exercised. **The `scipy` declaration question is NOT filed here**: M6
establishes that the code's statement is true against
`execution_environment.md:10`, so this is not environment drift, and it is not
the repository contradiction §6 R-2 anticipated either. It is recorded under
M6 and returned.

**`OBSERVATION_METHOD_ERROR`** — **one, mine, caught and corrected before it
entered the record.** M1's first run reported 6 failures because I
materialised the merge product with `git archive` into a non-git directory and
the six failing tests read git history. **A spurious A3 abort was one report
away.** The corrected run in a real git worktree returns 332 passed, 2
deselected, exit 0, matching the base. **Both runs are recorded above**, as A3
requires when a failure appears, even though the failure proved to be the
instrument's.

**Secondary, same category:** §8's substring hazards did not fire in this
task's own searches, but the `merge`, `integrat` and `science` checks were
performed by reading all matching lines rather than by counting, and their
exclusions are stated in §4 above — which is what §8 requires of any check
performed by search.

**`REPOSITORY_DEFECT`** — **one, and it is the abort's subject.**
`docs/BRANCHING_POLICY.md` does not name the `science/*` branch class and
prescribes no merge mode for any class, while **thirty-seven** `science/*`
branches are already ancestors of `main`. **The policy and the repository's practice have
diverged.** Classification of that divergence, and its remedy, are the ruling
this abort returns; I record the observation and do not classify it further.

**A second observation, not classified:** `pyproject.toml:12` declares
`scipy>=1.11` as a project dependency while
`docs/local/execution_environment.md:10` lists the required packages without
it. **Whether that is a defect or a deliberate separation of project
dependencies from validator-environment requirements is not decided here**, and
it is a different pair from the one §6 R-2 names.

**`UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`** — **two, both returned.**

**One — which register R-1 belongs in.** `derivations/P2-DEFERRED-ITEMS.md`
defines no status vocabulary, states at `:191` that entries are added by PI
decision, and scopes itself at `:12-17` to work *considered and consciously
postponed* as distinct from open questions, directing the latter elsewhere.
§6 R-1's item is described as *raised but not scoped*. **Which side of that
line it falls on is a judgement I do not make.**

**Two — the merge mode for a `science/*` branch.** Returned as the A4 ruling
request.

**Corrections to my own prior work, stated once and not enumerated further:**
`proca_curved.py`'s scipy statement is at `:42-43`, not `:43-44` as my
`POLE-B0` report cited it; and that statement does not contradict
`pyproject.toml:12`, because its subject is the environment's declared
packages and `execution_environment.md:10` is the document that declares
those. My `POLE-B0` report presented the pair as a repository contradiction.
**It is not one, and M6 is why that is now known.**

---

## What this branch contains

    commit 1  specs/2026-08-19T0319Z_pole-b0-integ-01.md
    commit 2  reviews/chatgpt/2026-08-19T0319Z_pole-b0-integ-01.md
    commit 3  this report

**No merge commit. No register entry. `main` untouched.**

**This is the record of a correct refusal, not of failed work** —
`docs/BRANCHING_POLICY.md:190-192` establishes that form, and this is the
second instance the repository will carry.
