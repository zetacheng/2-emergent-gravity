# Execution report — `P2-RECON-01B-B0`

    OUTCOME     MEASURED. No abort fired.
                Three of the ten components changed state since the prior
                assessment; all four CIRC-01 sectors fall INSIDE the
                projected object. No landing, main unmoved.

**Specification:** `specs/2026-08-19T0511Z_recon-01b-b0-scope.md`
**Review:** `reviews/chatgpt/2026-08-19T0511Z_recon-01b-b0-scope.md`
**Artifact:** `derivations/P2-RECON-01B-B0_scope-assessment.md`, 551 lines
**Evidence base:** `968e726a5a4322eecf4254ff69b25832f263c155`

**Measurement head: commit 3, `7a6e1589be7a0c0ad075036b2ca3be475509fd23`.**
Figures depending on commit 4 or the push are labelled **INTENDED** or returned
in the post-report layer.

**Nothing was computed.** No `β_V` value, no `k`-scan, no determinant, no
derivative, no projection applied to anything. **No script named in the
artifact was executed.**

---

## §0 — Binding SHA (MEASURED, no A1 abort)

    observed refs/remotes/origin/main   968e726a5a4322eecf4254ff69b25832f263c155
    §0 declared evidence base           968e726a5a4322eecf4254ff69b25832f263c155

**Equal. `main` has not advanced.**

**Review binding (Rule 18, Amendment N).** Field
`Reviewed specification SHA-256:` present, once, populated — checked before its
value.

    sha256 of the specification bytes as committed
      905d1338a5db55fb55cb3dab8f91be26b094d785e02f4be7aba764908b020be7
    the review's bound SHA
      905d1338a5db55fb55cb3dab8f91be26b094d785e02f4be7aba764908b020be7

**MATCH.** Verdict `APPROVE FOR EXECUTION`.

---

## Environment (MEASURED)

**Amendment D step 0.** Ref reads in `/home/user/2-emergent-gravity`; all
commits in the linked worktree `…/scratchpad/r01bb0`, created at the evidence
base.

**Rule 13's diagnostic order.** Not shallow; 423 commits on HEAD, 572 across
all refs; Python 3.11.15; pytest 9.1.1; numpy 2.4.6; sympy 1.14.0; ruff 0.15.8;
**`scipy` ABSENT.** **No environment failure occurred, so neither of Rule 13's
two diagnostic orders was exercised.**

---

## The two answers

### Q1 — component states

    N_both      5      components 1, 2, 4, 7, 8
    N_impl      0
    N_spec      4      components 3, 5, 6, 9
    N_neither   1      component 10
               --
    N_total    10      5 + 0 + 4 + 1 = 10   ✓

**Three components moved from `SPECIFICATION ONLY` to
`IMPLEMENTATION + SPECIFICATION`: 1, 2 and 4.** One event accounts for all
three — **`scripts/recon2026/` did not exist at the prior assessment's base**,
and `P2-BETAV-RECON-01a` landed it in between.

    git ls-tree -r --name-only ece34f7b scripts/ | grep -c 'recon2026'    0
    occurrences of "recon2026" in the prior assessment                    0

**The prior classification was correct when it was made**, and the artifact says
so. It reclassified components 1 and 2 because the only implementations were
recovered-pipeline code whose reuse would defeat the clean-room label; that
ground is gone for those two, and `proca_curved.py:9-10` states why in the
file's own words.

### Q2 — the projection and the sectors

**All four sectors `CIRC-01` reports fall INSIDE the object that survives the
axis-TT projection.** None is `INCONCLUSIVE`, and the `M5` limitation did not
bind — **no sector required executing anything.**

**The evidence is the order of operations in the code that produced
`CIRC-01`'s table.** `betav_decomp_q2.py:74-75` applies `TT_RECIPES` to the
vertex derivative tensors; `:83-85` builds the vertices from the already-projected
result; only then, at `:86-97`, are the `T`/`L` projectors applied to the
internal propagators. **A sector cannot be outside a projection that was applied
before the sector existed.**

**And the two decompositions act on different index spaces** — axis-TT on the
external polarization pair, `T`/`L` on the internal Proca propagator indices at
`k` and `k+q` separately. `RECON-01a:246-254` states the same distinction
independently, warning that reporting the two against each other *"WOULD BE A
CATEGORY ERROR"*.

**So the measured `≈0.4%` mixed contribution is not a blind spot of the
projection** — it is a component of what the projection retains.

---

## §2 — Measurements

**M1** — the inventory was located at
`derivations/P2-BETAV-RECON-01_scope-assessment.md:473-484`, ten numbered rows.
**No `A2` abort**; the list was read from the repository and not reconstructed
from the specification, which does not contain it. Per-component evidence is at
the artifact's `§1.2`, with file and line for every state and a stated reason
for each implementation ruled not applicable. **The four totals were counted
from the ten rows, not carried**, and the sum is shown.

**M2** — the comparison is present in both directions: a ten-row table showing
`differs?` per component, and the totals side by side. Three differ; the cause
is measured at `§2.1`.

**M3** — the axis-TT projection was located in both forms. Implementation:
`scripts/recovered_2026/mlog_coeff.py:21-31`, quoted in full. Specification:
`GATES.md:739` and `:748`. **No `A3` abort.** It **retains** the
five-dimensional symmetric traceless spatial tensor space for `q ∥ e₀`;
it **discards** every external index pair involving index `0` and the spatial
trace.

**M4** — `CIRC-01`'s four rows are quoted from
`P2-BETAV-CIRC-01_determinant-decomposition.md:37-45` with their magnitudes and
scaling exponents as stated, plus the stability line at `:47`. **Nothing was
recomputed.**

**M5** — one determination per sector, all four `INSIDE`, with the code evidence
at the artifact's `§5.1` and the index-space argument at `§5.2`.

**M6** — the analytic anchor at `GATES.md:750-751`, the kill criterion at
`:756-759`, the regression anchors at `:753-754`, and the gate status at `:727`
and `:769-773`, all quoted as read. The blinding mechanism is `:748`'s
*"targets kept out of code/tests"* together with `:751`'s *"compared only at the
end"* — **the target is written in the gate, not hidden from the repository;
what is required is that it stay out of the measuring code.** The sign is
settled by `P2-BETAV-SIGN-01`'s landed `SIGNED NEGATIVE`, quoted at `:11-15`.

---

## §7 — Acceptance criteria

    C1   PASS   Ten component rows in M1's table, each with a state, and each
                with file-and-line evidence in §1.2. Counted over §1's table
                alone: 10. (The artifact contains a second ten-row table, M2's
                comparison; a whole-file count of table rows would return 20
                and would be the wrong measurement.)
    C2   PASS   Four totals present; 5 + 0 + 4 + 1 = 10, and the table has ten
                rows. Arithmetic checked by recomputation from the rows, not
                asserted: the component lists beside each total name 5, 0, 4
                and 1 components and together name each of 1-10 exactly once.
    C3   PASS   M2 present in its "differences with causes" form, with the
                comparison shown as a per-component table and a totals
                comparison.
    C4   PASS   M3, M4 and M5 present. M5 has one determination per sector M4
                reports — four sectors, four determinations. None is
                INCONCLUSIVE and none needed to be.
    C5   PASS   M6 records locations and text as read, quoted in fenced blocks
                from GATES.md and the SIGN-01 artifact.
    C6   PASS   The §6.5 statement of non-establishment is present as the
                artifact's §8, naming all three required items: no β_V value,
                no PI decision on the blind target, and a count is not a
                measure of difficulty or duration.
    C7   PASS   Verified by reading. The artifact contains no recommendation on
                the blind target and no β_V value. Three phrases match a
                recommendation search — `:15`, `:534`, `:540` — and all three
                are negations: "It recommends no course of action on the blind
                target", "decided, recommended, or hinted at here", "It does
                not establish that RECON-01b should proceed". The e-notation
                figures in the artifact are CIRC-01's quoted q² sector
                coefficients, which M4 requires recorded as stated; they are
                Z contributions, not β_V ratios, and none was produced here.
                `−3` and `−5` appear only inside the quoted kill criterion.
    C8   PASS   Diff base→HEAD lists three paths, all in the §1b manifest:
                the derivation artifact (P1), and this task's spec and review
                (P2). The report joins them at commit 4.
    C9   INTENDED — main unmoved and push scope; measured in the post-report
                layer.
    C10  PASS   Every `scripts/` blob the assessment references is recorded
                below with its observed id, and **zero `scripts/` paths appear
                in the diff.**

### C10 — blob ids observed at `968e726a`

    scripts/recon2026/proca_curved.py              03f46905e5798fb7f6880dfae9ed5a1931be895b
    scripts/recon2026/flat_validation.py           6b21f9d6db67641ec7de31b7006884b617de3e8c
    scripts/recovered_2026/mlog_coeff.py           1464a8da9fbbaac07a9a57e622dff824f47f4d2f
    scripts/recovered_2026/proca_loop.py           7b83069ad3ac279fd95a3965f0182159e100c2e9
    scripts/recovered_2026/boson_loop.py           dbdd61621d5dcaa4dbc70600e35dcc355c48f811
    scripts/recovered_2026/batch2/gfvec_loop.py    e348051d7f73b5a3dccd2108864e40fe10e99b33
    scripts/betav_decomp_q2.py                     76ea1fac8df316639d4898108ed08ba63da05752
    scripts/betav_decomp_check.py                  712210e1f59a7471c1ec7921615a6eda5f41af35
    scripts/betav_assembly.py                      f3d8fa25d233871c4cd3de8c7acc3343bdc7bf9f
    scripts/P2-BETAV-CAMPAIGN/harness_compute.py   86c83ffe87319a4d7d9f93b0e79a8e535117d9bb
    scripts/P2-BETAV-CAMPAIGN/compare.py           b6bda1fc924844f7edb63681d803d5d3ca5154d2

    tests/test_recon2026_flat_limit.py             1d7ba5672614dedcd3b78483b5d43431af65fc7a
      — not under scripts/, recorded because the assessment cites it as
        component 7 evidence

**`scripts/betav_assembly.py` is `f3d8fa25…` at both this base and the prior
assessment's base `ece34f7b`** — measured, and it matters for `§2.2`: the
component-6 refinement is a difference in what was recorded, not in the file.

---

## §8 — Substring hazards, and how each was handled

**Every check in the artifact that could have been done by counting was done by
reading, and the exclusions are stated where the check is.**

**`TT`** matches `tt_check` and matches inside unrelated words. The `M3`
determination was made by reading the five recipes and their comment at
`mlog_coeff.py:21-31`, not by counting any `TT` token.

**`projection` does not match `projector`, and both are relevant.** This
distinction did real work: `scripts/recon2026/` contains
`longitudinal_projector_flat`, a rank-1 **projector**, and contains no axis-TT
**projection**. The artifact's component-5 evidence records
`grep -c -i -E 'axis|TT_RECIPE|traceless'` returning **0** on both `recon2026`
files — a count used to establish an absence, with the search terms stated,
not to establish a presence.

**`transverse`** appears both as a sector name and in prose about the transverse
block. The artifact never counts it; `§4` reads `CIRC-01`'s table and `§5.2`
reads what the labels denote.

**`k`** — the scan variable and the `k` of the ratio expression are written
alike. The artifact states which is meant at each use: component 6 is the scan
over `k ∈ {0,1,2,3,½}`; `−(k+2)` in `§6` is the ratio's `k`; and
`betav_assembly.py`'s `k`-scan is identified as scanning analytic prefactors,
not determinants.

**`C5`** — the specification's criterion `C5` and component 5 of the inventory
are different objects. **The artifact never writes the bare token for the
component**, referring to it as *"Component 5"* throughout; this report writes
`C5` only for the criterion.

---

## §1a — Non-objectives, all honoured

**No decision was made** on whether the blind target and kill values are
reusable, re-registered, or amended — `M6` measures their status and `§7.1` of
the artifact lists what it could not settle. **No part of `RECON-01b` was run**,
no `β_V` number produced, no `k`-scan executed. **Nothing was implemented,
repaired or ported.** `CIRC-01`'s verdict was **not** re-adjudicated — `M4`
quotes its table and `M5` reads the order of operations in the code that made
it. **No file under `scripts/` was modified**; C10 records eleven blob ids and
the diff contains zero `scripts/` paths. **Nothing was merged and `main` did not
move.** **No `R`-node was ruled.**

---

## Stops and clarifications

**`SPECIFICATION_DEFECT`** — **none.** Two provisions were exercised and both
worked. `§5.1`'s requirement to distinguish "no code exists" from "code exists
and is not applicable" is what surfaced the component-6 refinement at `§2.2`;
without it that fact would have been invisible in a state-only comparison.
And `§9`'s note that `C7` is satisfiable — established by reading for a
recommendation and passing on its absence, not requiring a declaration — is why
the three negation hits at `:15`, `:534` and `:540` are a pass rather than an
ambiguity.

**`ENVIRONMENT`** — `scipy` absent for the seventeenth consecutive task; no
validator imports it and nothing failed. **No environment failure occurred, so
neither of Rule 13's two diagnostic orders was exercised.**

**`OBSERVATION_METHOD_ERROR`** — **none, and one near-miss worth recording.**
`C1` is a count of component rows; a whole-file count of markdown table rows
returns **20**, because the artifact carries a second ten-row table for `M2`.
**Counting the file rather than the section would have reported twenty
components against a specification requiring ten.** The count was taken over
`§1`'s table alone and the artifact's second table was measured separately, at
ten. This is the same failure mode as the substring cases the specification's
`§8` names — a count taken over the wrong extent — arriving through table
structure rather than through a word boundary.

**`REPOSITORY_DEFECT`** — **none found.** Two standing observations, neither
new and neither this task's to act on: `GATES.md:753-754` records the
reconstruction's regression anchors as `None yet (proposed)`, which is component
10's `NEITHER` state seen from the other side; and `SIGN-01` recorded at its
`:22-23` that it left three documents asserting the target unsigned and
corrected none of them. **Whether those were subsequently repaired was not
measured** — `M6` asks for the target's and kill values' status, and both are as
quoted from the gate.

**`UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`** — **four, all recorded in the
artifact's `§7` as open rather than resolved in prose.**

**One is a classification judgement I made and flagged as reversible.**
Component 3's determinant primitive exists (`logdet_operator`) and the
`k`-weighted assembly `Γ_k` does not. **I classified on the assembly, because
that is what the component names, and recorded that a different reader could
classify on the primitive and reach `IMPLEMENTATION + SPECIFICATION`** — which
would make `N_both = 6` and `N_spec = 3`. The evidence for both readings is in
`§1.2` so the classification can be checked rather than taken.

The other three: what lives in the external index space the projection discards,
where no repository measurement reports a magnitude; whether component 9's
tolerance rule is specified or merely identified; and whether a clean-room
re-expression of `TT_RECIPES` would count as reuse, given that the five tensors
are fixed by symmetry rather than chosen. **None is decided here.**

---

## Layering

**Everything above is measured at commit 3,
`7a6e1589be7a0c0ad075036b2ca3be475509fd23`**, except `C9`, labelled INTENDED.

**Returned to the Reviewer in chat, not written back:** `C9` as measured — the
push exit status, the remote read-back, and confirmation that `main` is still
`968e726a`.

**This task performs no landing.** The `science/*` clause's merge-mode and
allowed-ref provisions are not engaged, and integration of this branch, if the
PI directs it, is a separate specification.
