# Execution report — `P2-GAPA-INTEG`

    OUTCOME     MERGED AND LANDED. No abort fired.
                The GAP-A derivation is on main, with its momentum
                condition attached to every statement of the outcome.

                M4's SUBJECT SET IS EMPTY, and that is a measurement rather
                than a skipped check: main's advance since the source forked
                was purely additive, so no path both sides carry differs and
                A5 has nothing to fire on. The last integration's four
                differing paths were not a permanent feature.

                THE REVIEWER'S EXECUTION-TIME CHECK WAS RUN BEFORE THE WORD
                "sole" WAS USED. Q1's Resolution path on main names exactly
                two gaps and has not been amended.

**Specification:** `specs/2026-08-20T0345Z_gapa-integ.md`
**Review:** `reviews/chatgpt/2026-08-20T0345Z_gapa-integ.md`
**Landing record:** `reports/2026-08-20T0345Z_gapa-integ_landing-record.md`
**Base:** `f76387474204a4cadda5275995eaa031ccdca8bf`
**Source:** `science/gapa-bridge-01 @ 02a83403e359941dcf1fff3c7690d1fc778b277e`

**Measurement head: `d4cc3f8`,** the landing commit. This report is the commit
after it. `C2` and `C14` are INTENDED and measured in the post-report layer.

---

## §0 and `M1` — Binding SHAs, and the abbreviation (MEASURED, no A1 abort)

**§0 records the source SHA abbreviated and forbids merging against an
abbreviation.** `M1` read the full value from the remote:

    git ls-remote origin refs/heads/science/gapa-bridge-01
      -> 02a83403e359941dcf1fff3c7690d1fc778b277e

    §0's abbreviation      02a83403
    M1's full value        02a83403e359941dcf1fff3c7690d1fc778b277e
    begins with it?        YES

    main   observed f76387474204a4cadda5275995eaa031ccdca8bf   declared identical

**`A1` does not fire on either limb.** Every merge and ancestry command below
names the full forty-hex value; **no command in this task was given the
abbreviation.**

**Review binding (Rule 18, Amendment N).** The
`Reviewed specification SHA-256:` field was checked for PRESENCE before its
value was compared. Present, once, populated, at
`reviews/chatgpt/2026-08-20T0345Z_gapa-integ.md:4`.

    sha256 of the specification bytes as committed
      839a672f94320113ed0554027c73569d89c2cc15b608477004555d161623f814
    the review's bound SHA
      839a672f94320113ed0554027c73569d89c2cc15b608477004555d161623f814

**MATCH.** Verdict `APPROVE FOR EXECUTION`.

---

## Environment (MEASURED)

**Amendment D step 0.**

    execution location   …/scratchpad/igapa    integration branch
                         …/scratchpad/base05   detached at f7638747
    worktree identity    two real git worktrees
    resolved HEAD        science/integrate-gapa-01, d4cc3f8
    ref reads            /home/user/2-emergent-gravity

    Python 3.11.15   pytest 9.1.1   numpy 2.4.6   sympy 1.14.0   ruff 0.15.8
    scipy  ABSENT — ModuleNotFoundError: No module named 'scipy'

Twenty-ninth consecutive task without `scipy`. **The arriving script imports
`numpy` and `sympy` only**, so the absence does not affect anything landing
here; this task itself computes nothing.

**Rule 13.** No environment failure occurred. Neither diagnostic order was
exercised.

---

## §2 — Measurements

Nothing was carried, including from the source's execution report and from the
Researcher's independent reconstruction of its result.

### `M2` — validators, both sides (MEASURED)

    merge product de97aaf5   332 passed, 2 deselected   exit 0
    base f7638747            332 passed, 2 deselected   exit 0

Real git worktrees. Identical. **`A3` does not fire.**

### `M3` — dry-run merge (MEASURED)

    git merge --no-commit --no-ff 02a83403e359941dcf1fff3c7690d1fc778b277e
      exit 0, "Automatic merge went well; stopped before committing as requested"
      conflicting paths (--diff-filter=U): 0
      aborted; HEAD restored to b532431

**`A2` does not fire.**

### `M4` — THE REVERT HAZARD: the subject set is EMPTY (MEASURED)

**Fork point: `a6be149f531c4a55ad331f26412a16472b803628`.**

**Paths present on both `f7638747` and the source at differing blobs: ZERO.**

**Why, established rather than assumed.** `main`'s advance from the fork to the
base was **purely additive** — `git diff --name-status a6be149f..f7638747`
returns eight paths, **all with status `A`**, the `P2-RECON-01B-B0-INTEG`
landing:

    A  derivations/P2-RECON-01B-B0_scope-assessment.md
    A  reports/2026-08-19T0511Z_recon-01b-b0-scope.md
    A  reports/2026-08-20T0324Z_recon-01b-b0-integ.md
    A  reports/2026-08-20T0324Z_recon-01b-b0-integ_landing-record.md
    A  reviews/chatgpt/2026-08-19T0511Z_recon-01b-b0-scope.md
    A  reviews/chatgpt/2026-08-20T0324Z_recon-01b-b0-integ.md
    A  specs/2026-08-19T0511Z_recon-01b-b0-scope.md
    A  specs/2026-08-20T0324Z_recon-01b-b0-integ.md

No file that existed at the fork was modified or deleted, and the source
modified nothing either. **With no path changed on either side, there is no
path at which a revert could occur.**

**`A5` cannot fire, and the reason is measured rather than presumed.** The
previous integration's four differing paths — `CONVENTIONS.md`,
`DECISION_LOG.md`, `docs/GOVERNANCE-DEBT.md`, `reviews/README.md` — arose
because that source forked much earlier. **The hazard is a property of how far
a branch has fallen behind, not a fixed feature of the repository**, and this
source forked one landing ago.

**The check was performed in full, not skipped because it was expected to be
empty.** The three-way blob comparison was run over both complete trees; the
empty result is its output.

### `M5` — registers, PER RECORD (MEASURED — and the two records differ)

**`R-1` — ADMITTED by `docs/GOVERNANCE-DEBT.md`.** `R-1` asks whether a rule
should exist requiring a scoped result to be cited with its scope. That is a
question about the rule set, and the register's stated scope at `:1-6` is
recording what "the rules, amendments and task reports already carry".
**Landed as `G-17`, disposition `OPEN`**, with an identifier from that
register's own sequence — `G-01` … `G-16` preceded it.

**`R-2` — NO REGISTER ADMITS IT.** Same three scopes, same three grounds as the
two preceding integrations found:

    P2-DEFERRED-ITEMS.md        ":191" — entries added by PI decision, each
                                carrying a PI position. EXCLUDED, who may write.
    C-CHECK_OPEN-ITEMS.md       ":3-5" — scope is the C1/C2/C3 line.
                                EXCLUDED, subject.
    docs/GOVERNANCE-DEBT.md     ":1-6" — governance-side. R-2 names scientific
                                work. EXCLUDED, kind.

**That the two records went to different places is the point of `M5`'s
per-record wording**, and it is the first time in this line that a task's
records have actually split: `R-1` is governance and `R-2` is science.

**`R-2` does not increment the homeless count.** `P2-PROJ-01-INTEG`'s `O-2` was
"The bridge tasks. `GAP-A` and `GAP-B`, one bounded task each". `GAP-A` is now
closed, so **`R-2` is `O-2` narrowed to its remaining half.** The count of
obligations with no admissible home **stays at three**.

**No register was created and none was used by convenience.**

### `M6` — the governing clause (MEASURED)

`docs/BRANCHING_POLICY.md:25-40`, `## Science branch integration`: `--no-ff`
into a dedicated integration branch, squash/rebase prohibited; "only the
integration branch and `refs/heads/main` may be pushed"; "Source branch,
session branches and unrelated refs must not move"; "`main` advances only by
fast-forward". **No contradiction with §4. `A4` does not fire.**

### `M7` — two measurements, and they differ (MEASURED)

**Base-relative, `git diff --name-only f7638747..d4cc3f8` — NINE paths:**

    A  derivations/P2-GAPA-BRIDGE-01_basis-identification.md   P1
    M  docs/GOVERNANCE-DEBT.md                                 P2
    A  reports/2026-08-20T0232Z_gapa-bridge-01.md              P1
    A  reports/2026-08-20T0345Z_gapa-integ_landing-record.md   P3
    A  reviews/chatgpt/2026-08-20T0232Z_gapa-bridge-01.md      P1
    A  reviews/chatgpt/2026-08-20T0345Z_gapa-integ.md          P3
    A  scripts/diagnostics/gapa01_basis_identification.py      P1
    A  specs/2026-08-20T0232Z_gapa-bridge-01.md                P1
    A  specs/2026-08-20T0345Z_gapa-integ.md                    P3

**Source's own contribution, `git diff --name-status a6be149f..02a83403…` —
FIVE paths, all `A`:**

    derivations/P2-GAPA-BRIDGE-01_basis-identification.md
    reports/2026-08-20T0232Z_gapa-bridge-01.md
    reviews/chatgpt/2026-08-20T0232Z_gapa-bridge-01.md
    scripts/diagnostics/gapa01_basis_identification.py
    specs/2026-08-20T0232Z_gapa-bridge-01.md

**THEY DIFFER: nine against five.** The four extra are this task's own three
artifacts plus the register modification. **`A6` does not fire**: every
base-relative path maps to `P1`, `P2` or `P3`, and `docs/GOVERNANCE-DEBT.md` is
the register `M5` selected for `R-1`, which `P2` authorises.

**A note on `scripts/`, because §1b.6 reads as forbidding it.** §1b.6 makes
"modify any file under `scripts/`" a non-objective, and one `scripts/` path
appears above. **It ARRIVES from the source by the merge and is `P1`, not a
modification by this task.** Measured: `git diff --name-status f7638747..d4cc3f8
-- scripts/` returns one path with status `A`, and **no existing file under
`scripts/` appears at all.** The distinction is the same one `P2-PROJ-01-INTEG`
recorded for `specs/`.

### `M8` — the reviewed result, transcribed (TRANSCRIBED, not recomputed)

Transcribed in the landing record's §1 and §2. In summary:

    outcome              IDENTIFICATION HOLDS, for q ∥ e₀
    D1 symbolic          rank Im(P2) = 5; P2 idempotent and symmetric;
                         P2+P1+P0s+P0w = I₁₀ exactly; all five recipes fixed
                         by P2; rank[Im(P2)|recipes] = 5
    D2 numerical         tolerance 1e-10; ‖P2map − Q_span(recipes)‖_F
                         = 4.228e-16; singular values 1.4142 ×5 then
                         1.055e-16, 0.0
    second conjunct      D1↔P0w; D2,D3,D4↔P1; D5↔P0s — a 1+3+1 partition,
                         each component annihilated by P2 and fixed by exactly
                         one block; ‖(P1+P0s+P0w) − Q_span(D1…D5)‖_F
                         = 4.191e-16, so the correspondence is also onto
    direction held       q ∥ e₀, and independent of |q₀|
    failure elsewhere    e1, e2, e3 at 2.404e+00; diagonal at 2.121e+00 —
                         O(1) against a 1e-10 tolerance, not marginal

### `M9` — `A-EXT-01` and `H-EXT-01` statement pins (MEASURED)

    A-EXT-01  ca8e5a870b5c7734321a9b6b97f3844046d8ceb689aece0ca65082b70a522378
    H-EXT-01  e5dd8a28eaff7623af23ab11404ef2d43dc8053599807162863cf38aca239a47

Each present once. `git diff --stat f7638747..d4cc3f8 -- CONVENTIONS.md
assumptions/H-EXT-01.md` is **empty** — **neither file is modified**, which is
what `C11` asks in addition to the digests. **`A7` does not fire.**

---

## §3 — Abort conditions

    A1  DID NOT FIRE.  Both §0 SHAs equal; M1's full value begins with §0's
                       abbreviation; no command used the abbreviation.
    A2  DID NOT FIRE.  Dry-run conflict-free, zero unmerged paths.
    A3  DID NOT FIRE.  M2 identical on both sides.
    A4  DID NOT FIRE.  M6 found the clause; it contradicts nothing in §4.
    A5  COULD NOT FIRE. M4's subject set is empty, measured over both trees.
    A6  DID NOT FIRE.  Nine base-relative paths, all in the §1c manifest.
    A7  DID NOT FIRE.  No §5 item required editing A-EXT-01, H-EXT-01, or any
                       arriving file; C3 shows all five arrive unedited.

---

## §7 — Acceptance criteria

    C1   SATISFIED   02a83403… is an ancestor of the integration head and is a
                     merge parent: de97aaf5 parents b5324315 02a83403.
    C2   INTENDED    Fast-forward from f7638747, measured post-report.
    C3   SATISFIED   Five arriving paths, five byte-identical blobs, per path.
    C4   SATISFIED   M4's subject set is empty, with the direction of main's
                     advance established as purely additive. No silent revert
                     occurred and none could.
    C5   SATISFIED   Every occurrence of the outcome carries its condition.
                     Verified by reading all five, listed below.
    C6   SATISFIED   Both conjuncts present; the second as a block-by-block
                     correspondence, not as a statement about dimension.
    C7   SATISFIED   §5.3 transported verbatim — checked line by line against
                     the source, zero divergences.
    C8   SATISFIED   All four §5.4 items present, including the third.
    C9   SATISFIED   §6 discharged, matching M5: R-1 in a register, R-2 in
                     none, with the scopes read.
    C10  SATISFIED   §5.5 present, with the symbolic-float incident.
    C11  SATISFIED   Both Statement SHAs unchanged and neither file modified.
    C12  SATISFIED   The base-relative list contains no path outside §1c.
    C13  SATISFIED   M7 records both and states that they differ, 9 against 5.
    C14  INTENDED    Measured in the post-report layer.

### `C3`, per path

    path                                                   source        product
    derivations/P2-GAPA-BRIDGE-01_basis-identification.md   f7e9d7fa0ebf  f7e9d7fa0ebf  IDENTICAL
    reports/2026-08-20T0232Z_gapa-bridge-01.md              dbfa3589f65d  dbfa3589f65d  IDENTICAL
    reviews/chatgpt/2026-08-20T0232Z_gapa-bridge-01.md      22652588e680  22652588e680  IDENTICAL
    scripts/diagnostics/gapa01_basis_identification.py      3ef6f8aa68f5  3ef6f8aa68f5  IDENTICAL
    specs/2026-08-20T0232Z_gapa-bridge-01.md                e247be2865f7  e247be2865f7  IDENTICAL

### `C5`, every occurrence read

Five sites in the landing record state the outcome or a conjunct. Each carries
the condition:

    :14   "IDENTIFICATION HOLDS — for q ∥ e₀, and at no other momentum
          direction tested."
    :38   "so the identification, holding for q ∥ e₀, covers every use…"
    :43   "The result, for q ∥ e₀, holds for that convention…"
    :50   heading — "First conjunct — subspace equality, for q ∥ e₀"
    :71   heading — "Second conjunct — the block-by-block correspondence,
          for q ∥ e₀"
    :111  inside the §5.3 verbatim quote, whose own preceding clause is
          "e₀ is the only direction the extraction ever uses"

**The second conjunct's heading and opening sentence were amended before commit
to carry the condition.** As first drafted they stated the correspondence
without it. **`C5` says "anywhere", and a conjunct is the outcome**; the
condition applies to it for the same reason it applies to the first, since the
Barnes–Rivers blocks are built from `q` and `D1`–`D4` are defined by carrying
the axis index `0`. Recorded because the criterion caught a real omission
rather than confirming a foregone pass.

---

## §8 — Substring hazards, and how each was handled

    TT              THE DECISIVE ONE, and this task is precisely about two of
                    its three referents coinciding: tt_check (a module),
                    TT_RECIPES (a table), and the Barnes–Rivers TT block (a
                    subspace). No count over "TT" was used; every statement
                    above names which object.
    spin-2          Matches the manuscript's channel discussion, which
                    concerns the stress-tensor correlator — a different
                    object. It appears nowhere in this report as a claim, and
                    the landing record's §4 states explicitly that the
                    manuscript's suppression statement is not made true of the
                    measured object by this result.
    identification  Matches "identify" in unrelated prose. C5 was checked by
                    reading each site, not by counting the word.
    holds           Named as matching "household", "stronghold", "holds that".
                    A case-insensitive whole-word scan of the landing record
                    returns TWO occurrences of "holds", at :14 and :43, plus
                    one of "holding" at :38 — THREE sites, and all three are
                    the result, each carrying q ∥ e₀. No decoy
                    sense occurs. The count is given because it was recounted
                    after the landing record was amended; an earlier scan
                    returned one, before the second-conjunct fix added a site.
                    No count stood for a reading anywhere: C5 was checked by
                    reading each site.
    axis            Matches "axis-TT" and ordinary geometric prose. Both
                    senses occur above — "axis-TT projection" and "the axis
                    index 0" — and each use names which.

**Rule.** "A check that cannot state its exclusions is performed by reading."

---

## §1a and §1b — the boundary and the non-objectives

**§1a — no new rule, convention or citation policy was created.** The question
whether a standing citation rule should exist is registered as `G-17` at
disposition `OPEN` and is **not answered there**; `G-17`'s own text says the
integration "did not create such a rule and was forbidden to". The reviewed
result's sentence about what it does not license is transported verbatim
instead.

    1  attempt, scope, or prejudge GAP-B          NOT DONE
    2  change Q1's verdict, subclass, or path     NOT DONE — the classification
                                                  artifact's blob is unchanged
    3  alter A-EXT-01, H-EXT-01, or any exact
       statement                                  NOT DONE — M9, diff empty
    4  draw any consequence for H-EXT-01          NOT DONE — §5.4 states the
                                                  opposite, and the landing
                                                  record's §4 spells out which
                                                  inference step is missing
    5  update any readiness register              NOT DONE — O-1 untouched
    6  compute anything, or modify any file
       under scripts/                             NOT DONE — this task computes
                                                  nothing; the one scripts/
                                                  path ARRIVES from the source
                                                  and no existing file there is
                                                  modified
    7  modify any file arriving from the source   NOT DONE — C3, per path

---

## Stops and clarifications

### `S-1` — the Reviewer's §10/§19 check, run before the word was used

The Reviewer directed that before describing `GAP-B` as the **sole** remaining
gap, the executor re-read the authoritative `Q1` `Resolution path` at execution
time and confirm it contains only `GAP-A` and `GAP-B` and has not been amended.

**Run, on `main`, before the landing record was written.**

    derivations/P2-PROJ-01-CLASS-01_q1-classification.md
      §2, the gaps recorded          ### GAP-A at :155,  ### GAP-B at :178
      §5, the Resolution path        ### GAP-A at :304,  ### GAP-B at :340
      third gap                      none, in either section
      commit history                 a single commit, 0bd78ae
      blob on main                   e7b63d44319a7df3807f7d61aceb15afc2e0aa59

**Exactly two gaps, in both places, and the artifact has not been amended since
it landed.** The condition the Reviewer set is reproduced, so "sole remaining"
is permitted and is used.

### `S-2` — `M4` came back empty, and that is a result about the hazard

**The previous integration found four differing paths and three were
load-bearing. This one found none.**

The difference is the fork distance: `P2-RECON-01B-B0`'s source forked at
`968e726a` and `main` had advanced through rule 22, four register entries, an
append-only log and a README section. **This source forked at `a6be149f`, one
landing ago, and that landing added eight files and modified none.**

**The check is not less necessary for having come back empty.** Its value is
that the answer is measured; an empty subject set established by comparing both
complete trees is evidence, while an empty subject set assumed from recency is
not. **Recorded so that a later reader does not conclude from this task that
`M4` is a formality.**

### `S-3` — the two records split between registers, for the first time in this line

`M5` has been worded per-record since `P2-PROJ-01-INTEG`, and until now every
record it examined landed the same way — nowhere. **Here `R-1` found a register
and `R-2` did not.**

**The split is by kind and it is clean.** `R-1` asks whether a *rule* should
exist; `docs/GOVERNANCE-DEBT.md` holds questions about the rule set. `R-2` names
*scientific work*; no register holds that. **This is the shape of the gap the
last three tasks have been reporting**: the repository has a governance-side
register and two science-side registers whose scopes are narrow and whose write
gates exclude an executor, and nothing that holds a scientific follow-up
obligation in general.

**Still three obligations with no admissible home**, unchanged by this task,
because `R-2` restates `O-2`'s remaining half rather than adding to it.

### `S-4` — `CONVENTIONS.md`'s count of the register is stale again, and again could not be repaired here

`docs/GOVERNANCE-DEBT.md` now holds seventeen entries. `CONVENTIONS.md:1619`
describes it as "a fifteen-entry record".

**This is the third consecutive occurrence.** `P2-GOV-HOUSEKEEP-02` found it
stale at eleven-versus-twelve and repaired it to fifteen; `P2-PROJ-01-INTEG`
made it stale by one and could not repair it; this task makes it stale by two.

**`CONVENTIONS.md` is not in the §1c manifest** — `P2` authorises registers
selected under `M5`, and `CONVENTIONS.md` is not a register — **so editing it
would have fired `A6`.**

**The cause is structural and unchanged: a count of one file is stated in
another, and nothing ties them.** The durable repairs remain to delete the count
from `CONVENTIONS.md`, or to put `CONVENTIONS.md` in the manifest of any task
that writes the register. **Neither is this task's to choose**, and `P2`
authorises the register only for the §6 records, so a `G-18` for it would be
outside the manifest too. **Returned as an observation for the third time.**

---

## Layering

This report is measured at `d4cc3f8`, the landing commit, which is commit N−1.
`C2` (the fast-forward) and `C14` (the ref-push scope) are measured in the
post-report layer; that evidence is returned to the Reviewer in chat and is not
written back into this file.
