# Execution report — `P2-GAPB-BRIDGE-01`

    OUTCOME     INDETERMINATE — UNDETERMINED BY READING.
                No abort fired. No merge; main does not move.

                Five mismatches identified and each classified with its
                reasoning. One met, one controlled, three not determinable.

                A MISMATCH THE PRE-REGISTERED QUESTION DID NOT NAME was found
                by executing M1 and M2 as written: L2's object is a FERMION
                determinant and EXT-01's is a PROCA one. It is reported as a
                mismatch, not as a re-opening of GAP-B's wording.

                THE OUTCOME IS NOT "MEASUREMENT REQUIRED", AND THE REASON IS
                REPORTABLE: the species and counterterm gaps are prior to the
                spacing gap and are not measurement questions. The
                measurement is specified anyway, with its limits stated.

**Specification:** `specs/2026-08-20T0408Z_gapb-bridge-01.md`
**Review:** `reviews/chatgpt/2026-08-20T0408Z_gapb-bridge-01.md`
**Derivation artifact:**
`derivations/P2-GAPB-BRIDGE-01_regime-transfer.md`
**Base:** `f23a0e1e1a24398d082a9597444ff9f750ed38e1`

**Measurement head: `cf4c0b0`,** the derivation commit. This report is the
commit after it. `C11`'s push clause is INTENDED and measured in the
post-report layer.

---

## §0 — Binding SHA (MEASURED, no A1 abort)

    observed origin/main   f23a0e1e1a24398d082a9597444ff9f750ed38e1
    §0 declared base       f23a0e1e1a24398d082a9597444ff9f750ed38e1

**Equal.**

**Review binding (Rule 18, Amendment N).** The
`Reviewed specification SHA-256:` field was checked for PRESENCE before its
value was compared. Present, once, populated, at
`reviews/chatgpt/2026-08-20T0408Z_gapb-bridge-01.md:4`.

    sha256 of the specification bytes as committed
      a478ddd0839fd5c938b391fa1b6f36648a09cd6fda17e1629cf3cf66eefe51db
    the review's bound SHA
      a478ddd0839fd5c938b391fa1b6f36648a09cd6fda17e1629cf3cf66eefe51db

**MATCH.** Verdict `APPROVE FOR EXECUTION`, with two execution constraints at
the review's §8 and §20, both honoured and recorded below.

---

## Environment (MEASURED)

**Amendment D step 0.**

    execution location   …/scratchpad/gapb
    worktree identity    a real git worktree, branch science/gapb-bridge-01
    resolved HEAD        cf4c0b0 at measurement
    ref reads            /home/user/2-emergent-gravity

    Python 3.11.15   pytest 9.1.1   numpy 2.4.6   sympy 1.14.0   ruff 0.15.8
    scipy  ABSENT — ModuleNotFoundError: No module named 'scipy'

Thirtieth consecutive task without `scipy`. **This task computes nothing and
added no script**, so nothing was imported at all.

**Validators: 332 passed, 2 deselected, exit 0.** Not a criterion here — this
task changes no code — and recorded as evidence that nothing was broken.

**Rule 13.** No environment failure occurred. Neither diagnostic order was
exercised.

---

## §2 — Measurements taken before any analysis

Nothing was carried, including from `EXT-01`'s report, `PROJ-01`'s adjudication,
or the `GAP-A` landing.

### `M1` — nine conditions, enumerated with file and line (MEASURED)

    L-1  the object is the FERMION-induced kernel        :685-696, eq:Gamma2
    L-2  the invariance is the FERMION sector's          :754-758
    L-3  Symanzik description, breaking at dim ≥ 6       :759-761
    L-4  the approximate Ward identity holds             :762-767, eq:Ward
    L-5  up to contact terms with no propagating poles   :768
    L-6  the trace identity, for the IMPROVED stress
         tensor, up to contact terms                     :775-777
    L-7  the Barnes–Rivers projector basis               :772
    L-8  the conclusion is for the INFRARED kernel;
         Λ ~ 1/a                                         :778; :75, :497, :717
    L-9  suppression by (p/Λ)² RELATIVE to the TT block  :773-775, :779-786

**`L-1` and `L-2` were the conditions the enumeration was most at risk of
missing**, because `L2`'s own subsection does not restate them — they come from
`eq:Gamma2`, which defines the object `L2` decomposes, and from the Ward
subsection that supplies its premise. §11.1 of the specification says a
condition omitted from `M1` cannot be found by any later check; these two are
the reason that is true.

### `M2` — the object's stated properties (MEASURED)

    E-1  a lattice PROCA loop, a massive vector field    proca_loop.py:2-3
         with the action at :5-9 and boson loop sign
         Gamma = +(1/2)<ln det M>                        proca_loop.py:17
    E-2  m = 0.3                                         EXT-01:144
    E-3  n = 12, finite                                  EXT-01:143
    E-4  q-grid [0.10, 0.16, 0.22, 0.28]                 EXT-01:145
    E-5  q ∥ e₀                                          EXT-01:150
    E-6  fit Π(q) = A + B q² + C q⁴; B reported          EXT-01:146-147
    E-7  k = 1, and the bubble does not take k           EXT-01:161-165
    E-8  the ten orthonormal components                  EXT-01:151

**Two properties the pre-registered question names are NOT stated by the
repository.** A repository-wide search for `unimproved` returns **zero**
occurrences in `EXT-01`'s artifact, its report, its diagnostic script, or
`proca_loop.py` — every occurrence is in `PROJ-01`'s adjudication and downstream
of it. No lattice spacing is stated in `EXT-01`'s artifact or report either.
**"a = 1, unimproved" is `PROJ-01`'s characterisation, not `EXT-01`'s
statement.** Both are settled under `D1` by reading the action rather than by
taking the characterisation on trust.

### `M3` — three-valued, per condition (MEASURED)

    L-1  STATES THAT IT DOES NOT   proca_loop.py:2-3, :17
    L-2  STATES NOTHING
    L-3  STATES NOTHING
    L-4  STATES NOTHING for this object
    L-5  STATES THAT IT IS SATISFIED   mlog_coeff.py:10-11
    L-6  STATES NOTHING
    L-7  STATES THAT IT IS SATISFIED   GAP-A, for q ∥ e₀ only
    L-8  STATES THE PARAMETERS, not the comparison
    L-9  STATES NOTHING about whether the object exhibits it

**Two affirmed, one denied, six silent. No silence was collapsed into either
answer.**

**One near-miss recorded because it would have been an easy false affirmative.**
The manuscript at `:1057-1059` records a lattice Ward identity satisfied to
`2×10⁻¹⁴` — but it is the **photon** sector's **gauge** identity
`q̂_μ Π^{μν} = 0` on a different kernel, not the **diffeomorphism** identity
`eq:Ward` on this object. **`L-4` is silent, not affirmed**, and reading that
check as affirming it would have been the error.

### `M4` — the manuscript states several conditions under which `L2`'s conclusion would not apply (MEASURED)

**NOT SILENCE.** Five locations, all in the manuscript itself:

    :818-819      "Up to O(p²/Λ²) corrections, the quadratic action defined by
                  Eq. (PiTT) is the Fierz–Pauli action" — the accuracy bound
    :805-809      the strongest dynamical claim "is ultimately a
                  nonperturbative question"
    :1067-1084    Finding 1 — "two continuum expectations fail at O(1)";
                  "the subtracted kernel is not transverse"; the lattice
                  "induces genuinely non-covariant local terms at cutoff
                  order"; "covariance of the infrared effective action must be
                  restored by local counterterms", including "non-covariant
                  O(q²) structures"
    :1088-1107    Finding 2 — at O(q²) the TT projection receives covariant and
                  H(4)-invariant non-covariant contributions, with "the
                  positive bubble-only axis slope traced entirely to the
                  non-covariant piece", and an "exact null space" making the
                  covariant coefficient "not identifiable from
                  transverse-traceless data"
    :733-739      "transverse-traceless data alone cannot separate the
                  covariant kinetic coefficient from non-covariant counterterm
                  structures"

**These are the manuscript's own statements about where its lattice kernel
departs from the continuum expectations `L2` reasons from**, and `M4` found them
by reading the section that builds the kernel rather than only the subsection
that states the conclusion.

### `M5`, `M6` — the states this task must not alter (MEASURED)

    Q1        INCONCLUSIVE, UNDETERMINED BY READING, subclass CONSTRUCTIVE GAP
              IDENTIFIED                       classification:11-12
    GAP-A     IDENTIFICATION HOLDS, q ∥ e₀ only    bridge:7, §7
    H-EXT-01  UNESTABLISHED — NOT ASSUMED BY RECON-01b   H-EXT-01.md:14

    A-EXT-01  ca8e5a870b5c7734321a9b6b97f3844046d8ceb689aece0ca65082b70a522378
    H-EXT-01  e5dd8a28eaff7623af23ab11404ef2d43dc8053599807162863cf38aca239a47

Each present once and unchanged. **`C13` holds.**

---

## §3 — The analysis

Five mismatches, each classified under §5's `(i)`–`(iv)` **with the reasoning**,
covering all nine `M1` conditions.

    MM-1  SPECIES            L-1, L-2, L-3   (iv)  not determinable here
    MM-2  REGIME             L-8, L-9        (i)   the condition is met
    MM-3  IMPROVEMENT        L-6             (iv)  a measurement would bear
    MM-4  CONTACT TERMS      L-5             (ii)  provably irrelevant
    MM-5  COVARIANCE STATE   L-4, L-8        (iv)  not determinable here

    L-7  MET, by GAP-A, for q ∥ e₀ and not beyond

**`MM-4` is the one cleanly controlled**, and the reason is stated rather than
asserted: `L2`'s contact terms are `q`-independent — the manuscript at
`:1046-1049` says the seagull tadpole "is *exactly* q-independent: contact terms
shift the momentum-independent part of the kernel but not its q² slope", and
`mlog_coeff.py:10-11` says the same of this family of objects — while the
reported quantity is `B`, the `q²` coefficient of `Π(q) = A + Bq² + Cq⁴`. **A
`q`-independent term lands in `A`.** The mismatch is an intercept and the
observable is a slope.

**`MM-2` is met, with the arithmetic shown and no measured value used.**
`Λ ~ 1/a` and `a = 1` — the latter settled by reading `proca_loop.py:5`'s unit
forward differences and the `[−π, π)` Brillouin zone, not by taking `PROJ-01`'s
word. `(q·a)²` runs `0.0100` to `0.0784` across the grid, so `q ≪ Λ` holds.
**Recorded and set aside: `q/m` runs `0.333` to `0.933`, so the measurement is
not at `q ≪ m` — but `L2` states no such condition.** Its "infrared" is the
external kernel's regime relative to `Λ`; `:1133-1134`'s "infrared (k ~ m)
region of the loop" is about **loop** momentum in a different argument. **The two
regimes are not conflated.**

**`MM-3` cannot be waved into (ii), and the reason is specific.** `L-6`'s trace
identity exists in `L2` to remove the **propagating scalar block**, and
`EXT-01`'s discarded set contains exactly the components that block governs —
`D5`, the spatial trace, and `D1`, `h00`, which `GAP-A` identified as the `P0s`
and `P0w` blocks. **A mismatch in the identity that removes the scalar block
cannot be irrelevant to two of the five components at issue.**

**`MM-5` is the strongest candidate for a demonstrated obstruction and does not
reach it.** `eq:PiTT` is stated for the **infrared** kernel, and the manuscript
says covariance of the infrared effective action "must be restored by local
counterterms" including non-covariant `O(q²)` structures. `EXT-01`'s object is
the **bare finite-spacing kernel**, before any restoration — no counterterm
subtraction appears in its pre-registration or its diagnostic. **Finding 2's
exact null space establishes that the covariant coefficient is not identifiable
from TT data — an obstruction to DETERMINING it, not a demonstration that `L2`'s
conclusion is false of the restored kernel.** Converting the first into the
second is the invalid step §5 forbids and the Reviewer's §3 names.

---

## §4 — The outcome, and why it is this one

    INDETERMINATE — UNDETERMINED BY READING

**`TRANSFER HOLDS` is not earned:** it requires every mismatch in `(i)`–`(iii)`
and three are in `(iv)`.

**`TRANSFER FAILS` is not earned:** it requires at least one mismatch shown to
**prevent** the conclusion from applying, and none is. `MM-5` comes closest and
stops at non-identifiability.

**`MEASUREMENT REQUIRED` is not the outcome, and this is the substantive
choice.** `MM-3` is a measurement question. **`MM-1` and `MM-5` are not, and
they are prior to it.** Until a Ward identity exists for the lattice Proca
discretisation, a continuum-limit study of the Proca object would report the
continuum behaviour of the Proca object — **it would not establish whether
`L2`'s fermion-sector conclusion transfers to it.**

**The measurement is specified anyway**, at the artifact's `MM-3`: evaluate the
same ten-component decomposition at two or more lattice spacings at fixed
physical `m` and `q`, and observe whether the discarded group's `q²`
coefficients approach zero relative to the retained group as `a → 0`. **What
would decide each way is stated**: a ratio falling like `a²` toward zero shows a
lattice artefact the continuum limit removes; a ratio approaching a non-zero
constant shows it does not. **`K4` forbids running it and it was not run.**

**Stated plainly because a PI decision may turn on it: the measurement a reader
would reach for first would not, on its own, settle `GAP-B`.** It is specified
so that it can be commissioned for its own sake with its limits visible, rather
than commissioned in the belief that it closes the gap.

---

## §5 — `D3` and `D4`

**`D3`.** Transfer is established in neither direction, so `EXT-01`'s
measurement is **neither** a measurement of a regime `L2` reaches **nor** one it
fails to reach. Its standing is what it was: a measurement of the bare
finite-spacing Proca kernel's ten-component `q²` decomposition at `q ∥ e₀`.
**Its values are not re-analysed and appear as recorded.**

**`D4`, the apparent conflict, recorded after the classification and not used to
reach it.** `EXT-01` records `|sum discarded| / |sum retained| = 0.297300` at
its `:206`. If `eq:PiTT` applied to this object, `L-9` would place the non-TT
content at `O((q·a)²)` relative to the TT block — at most `0.0784` at the top of
the grid. **Both figures are recorded side by side, as stated, neither
adjusted, and no mechanism that would remove the discrepancy is proposed.**

**The Reviewer's §6 order was followed:** derive applicability, classify
transfer, then compare consequences. **The `0.297300` did not enter any
classification above**, and each `(i)`–`(iv)` assignment rests on the conditions
and the object's stated properties alone.

---

## §6 — Abort conditions

    A1  DID NOT FIRE.  origin/main equals the §0 base.
    A2  DID NOT FIRE.  L2 located at paper:770-788.
    A3  DID NOT FIRE.  EXT-01's landed artifact located.
    A4  DID NOT FIRE.  No item required modifying an existing scripts/ file;
                       the diff under scripts/ is empty.
    A5  DID NOT FIRE.  Three paths modified, all inside the §1d manifest.

---

## §8 — Acceptance criteria

    C1   SATISFIED   M1 enumerates nine conditions, each with file and line.
    C2   SATISFIED   M2 records eight stated properties with file and line,
                     and records which named properties the repository does
                     NOT state.
    C3   SATISFIED   M3 is three-valued per condition; six silences recorded
                     as silence.
    C4   SATISFIED   M4 recorded — five locations, and it is not silence.
    C5   SATISFIED   D1 covers all nine conditions; D2 present for each unmet
                     one with its (i)–(iv) class and reasoning.
    C5a  SATISFIED   No outcome rests on literal non-satisfaction. MM-3 and
                     MM-5 are each unmet AND carry a classification with the
                     reason; MM-2 is unmet on q/m and is classified (i)
                     because L2 states no such condition.
    C6   SATISFIED   INDETERMINATE — UNDETERMINED BY READING recorded.
    C7   SATISFIED   D3 present; EXT-01's values appear as recorded and are
                     not re-analysed.
    C8   SATISFIED   No statement that the manuscript is wrong. Every
                     occurrence of "wrong" or "false" in the artifact denies
                     such a statement or draws the reach/correctness
                     distinction — six occurrences, listed below.
    C9   SATISFIED   No reconciliation mechanism proposed. Verified by
                     reading; passes on absence.
    C10  SATISFIED   No outcome characterised as expected, unexpected,
                     surprising or disappointing — a whole-word search
                     returns zero.
    C11  PARTLY IN   Three paths, all inside §1d. main unmoved and only this
         POST-REPORT branch pushed: INTENDED, measured post-report.
    C12  SATISFIED   See below, with exclusions.
    C13  SATISFIED   Both Statement SHAs unchanged; Q1, GAP-A and H-EXT-01
                     unaltered.
    C14  SATISFIED   Both scripts/recon2026/ blobs identical to the base:
                     6b21f9d6db67641ec7de31b7006884b617de3e8c
                     03f46905e5798fb7f6880dfae9ed5a1931be895b

### `C8`, its six occurrences

    :19       "Nothing here says the manuscript is wrong."
    :225      "None of them says L2 is wrong"
    :385-386  "…turning 'cannot be determined' into 'does not hold' is
              precisely the invalid conversion §5 forbids."
    :444      "The comparison is not evidence that L2 is wrong"
    :504      "Nothing here says the manuscript is wrong."
    :506      "An argument that does not reach an object is not an argument
              that is false."

**Every one draws the distinction or denies the statement. None asserts it.**
The line numbers were recomputed after the artifact's §0 was inserted; an
earlier set, taken before that edit, was off by thirteen.

### `C12`, with its exclusions

Searched the derivation artifact for `beta_V`, `beta_B`, `β_V`, `β_B`,
`-2.000`, `-3.000`, `3.2(5)`:

    all patterns   0, EXCEPT β_V and β_B, one occurrence each, both in the
                   single sentence "No β_V, no β_B, no ratio of them" —
                   THE NAMES ASSERTING ABSENCE, NOT VALUES.

**This task added no script and emitted no output**, so the search extent
reduces to the artifact. **`proca_loop.py:18` carries an analytic target in its
docstring; it was not read into anything and its numeral is not restated here.**

### `A5` / `C11`, the manifest

    A  derivations/P2-GAPB-BRIDGE-01_regime-transfer.md   P1
    A  reviews/chatgpt/2026-08-20T0408Z_gapb-bridge-01.md P3
    A  specs/2026-08-20T0408Z_gapb-bridge-01.md           P3

**`P2` is unused: this task added no script.** §1d requires each added script to
be named in the report; there are none to name, and that is recorded rather than
left to inference. **The derivation's only computation is four divisions on
stated parameters, shown in full in the artifact — a script for that would have
been ceremony, not evidence.**

---

## §9 — Substring hazards, and how each was handled

    improved        DECISIVE for MM-3. The Symanzik sense — an improved
                    operator — is the one L-6 uses. A search for the token in
                    EXT-01's artifact and script returns ZERO, which is why
                    M3 records silence and D1 settles it by reading the
                    action instead.
    kernel          TWO OBJECTS, and MM-5 turns on keeping them apart: the
                    INFRARED EFFECTIVE KERNEL of eq:PiTT, and the FLAT
                    LATTICE KINETIC KERNEL M(k) of proca_loop.py:11. Every
                    use above names which.
    contact         "contact terms" in L-5 and L-6, and ordinary prose. Only
                    the first sense occurs above.
    transfer        Named as matching "transverse" loosely and "transfer
                    matrix", a different object in this repository. No
                    pattern search on "transfer" was used; the mismatches
                    were reached from M1's condition list.
    regime          MM-2 turns on TWO regimes that the same word covers —
                    q relative to Λ, and loop momentum k relative to m. The
                    artifact states both and conflates neither.
    L2              Matches "L2 norm" and section labels. L2 here is
                    PROJ-01's label for paper:770-788 and is used in no other
                    sense.

**Rule.** "A check that cannot state its exclusions is performed by reading."

---

## §1c — Non-objectives, all honoured

    1  re-open GAP-A                    NOT DONE. Used at L-7 strictly within
                                        its momentum condition, and not
                                        promoted to a covariant identity —
                                        the Reviewer's §8 and §20 constraint.
    2  change Q1's verdict, subclass or
       Resolution path                  NOT DONE. M5; the artifact says so.
    3  resolve H-EXT-01                 NOT DONE. Unchanged.
    4  compute β_V, β_B, ratio, k-scan,
       or read the target               NOT DONE. C12, with exclusions.
    5  re-run, re-analyse or re-fit
       EXT-01's measurement             NOT DONE. Its values appear as
                                        recorded. The only arithmetic is on
                                        STATED PARAMETERS — q·a, (q·a)², q/m —
                                        which characterise the object, not its
                                        result.
    6  assert that the manuscript is
       wrong                            NOT DONE. C8, six occurrences listed.
    7  modify any existing scripts/ file
       or anything under recon2026/     NOT DONE. C14; the scripts/ diff is
                                        empty.
    8  merge anything, or move main     NOT DONE.

---

## Stops and clarifications

### `S-1` — a mismatch the pre-registered question did not name

**`GAP-B`'s establishing direction, as landed, names the regime, the spacing,
the mass, the extent and the improvement. It does not name the species.**

Measured: `L2`'s object is defined at `eq:Gamma2` by integrating out the
**fermions**, and its Ward premise is the diffeomorphism invariance of the
**fermion** measure and action. `EXT-01`'s object is a **lattice Proca loop** —
a massive vector, boson loop sign.

**This was found by executing `M1` and `M2` as specified**, not by looking for
it: `M1` requires *every* condition the argument states or requires, and the
object and the invariance are two of them.

**It is reported as a mismatch and not as a defect in `GAP-B`'s wording.** The
landed `Resolution path` is not amended, `Q1`'s subclass is untouched, and the
species mismatch is classified under the same `(i)`–`(iv)` taxonomy as the four
the question did name. **Whether the `Resolution path` should be re-worded is
not this task's to decide** — it is a landed reviewed record, and `§1c.2`
forbids changing it.

**It matters for what a follow-up task should be.** A bridge task aimed at the
spacing question would not reach the species question, and the two need
different work: one a lattice campaign, the other a Ward-identity derivation.

### `S-2` — "unimproved" and "a = 1" were `PROJ-01`'s words, not `EXT-01`'s

**Measured: zero occurrences of `unimproved` in `EXT-01`'s artifact, report,
diagnostic script, or `proca_loop.py`.** No lattice spacing is stated in
`EXT-01`'s artifact or report. Every occurrence of the characterisation is in
`PROJ-01`'s adjudication and downstream of it, including in `GAP-B`'s own
pre-registered wording.

**Both are nonetheless true of the object**, and this task established them by
**reading the action** — `proca_loop.py:5-9` is a minimal forward-difference
discretisation with unit shift and no improvement term of any kind.

**Recorded because the chain from characterisation to pre-registered question
ran without the characterisation ever being checked against the object.** It
happened to hold. `M3`'s three-valued discipline is what surfaced it: a
two-valued `M3` would have recorded "unimproved: yes" from the inherited
description and never looked.

### `S-3` — what `M4` found, and why it was not a formality

`M4` asks whether the manuscript or repository states any condition under which
`L2`'s conclusion would fail, weaken, or not apply, and directs that silence be
recorded as silence.

**It is not silence, and the statements are in the manuscript's own §ttcheck.**
Finding 1 states that "the subtracted kernel is not transverse" and that the
lattice induces non-covariant local terms at cutoff order; Finding 2 states that
at `O(q²)` the TT projection mixes covariant and non-covariant structures with
an exact null space along the covariant direction.

**These are the manuscript being careful about its own lattice construction, and
they are the substance of `MM-5`.** A reading of `L2` that stopped at its own
subsection would have found silence and recorded it. **`M4`'s value here was
that it directed the search past the passage under test.**

### `S-4` — the outcome is the weaker-sounding of two `INDETERMINATE`s, deliberately

`INDETERMINATE — MEASUREMENT REQUIRED` is the outcome §5 says "would justify a
PI decision on whether to open such a task", and returning it would have
produced a clear next action.

**It would have been wrong.** `MM-3` is a measurement question, but `MM-1` and
`MM-5` are prior and are not. **A measurement commissioned on that outcome could
run to completion and leave `GAP-B` exactly where it is.**

**The measurement is specified anyway**, so nothing is lost if the PI wants it
for its own sake — and the limit is stated so the decision is informed rather
than implied.

---

## Layering

This report is measured at `cf4c0b0`, the derivation commit, which is commit
N−1. `C11`'s push clause is measured in the post-report layer; that evidence is
returned to the Reviewer in chat and is not written back into this file.
