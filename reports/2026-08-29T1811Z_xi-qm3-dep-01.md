# Report — `P2-XI-QM3-DEP-01` v3: `M0b` returned `NOT UNIQUELY IDENTIFIED`

    Task ID     P2-XI-QM3-DEP-01
    Version     v3
    Spec        specs/2026-08-24T0000Z_xi-qm3-dep-01_v3.md
    Review      reviews/chatgpt/2026-08-24T0000Z_xi-qm3-dep-01_v3.md
    Base        main @ 0c01fc7f26e91dd84b032dccde0feac61f61d8ea
    Branch      science/xi-qm3-dep-01
    Path taken  M0 → M0b → M4 → M6, the early return.
                M1, M2, M3 and M5 did not run.
    Outcome     M0b: NOT UNIQUELY IDENTIFIED.
                Rule 22: INCONCLUSIVE — CONSTRUCTIVE GAP IDENTIFIED.
                No dependence evaluation was performed.

This report records the measurements that ran and names those that did not and
why. It does not state its own commit SHA. It names the tree it is committed
onto — **`T_early`, the pre-report completion tree after `M4`, which is NOT an
`M5`-tested tree because `M5` did not run** — and records that it is itself the
next commit on it.

    T_early   5ae09b05cf9cbb916f11c6ed94118ffaa4a9b8de

**MEASUREMENT UNITS.** Every offset, length and byte-identity comparison in this
task is performed and reported in bytes. Where a comparison applied a
normalization, the normalization is stated; where none is stated, none was
applied.

---

## 0. Execution location and worktree identity (Amendment D step 0)

    host                     vm
    working directory        /home/user/2-emergent-gravity
    worktree top level       /home/user/2-emergent-gravity
    shallow repository       false
    HEAD before the branch   37a2f411ea1166134b3c153e15aab74af3f79bf6
                             (science/integrate-xi-clar-01)
    working tree             clean before the branch was cut

    git checkout -b science/xi-qm3-dep-01 \
        0c01fc7f26e91dd84b032dccde0feac61f61d8ea

## 0a. Binding, before any write

**Rule 18 and Amendment N.** The review carries the line
`**Reviewed specification SHA-256:**` — PRESENT — at its lines 4 and 175,
identically, and

    0fab1fdc58612bfd44971b2d7fef842ce4db4a9ca1ec86fdc926767ce31ebfaa

is the only 64-hex string it contains. Measured before the specification commit,
and re-measured over the committed bytes afterwards:

    sha256 of the spec file, before the commit
      0fab1fdc58612bfd44971b2d7fef842ce4db4a9ca1ec86fdc926767ce31ebfaa
    the digest the review declares itself bound to
      0fab1fdc58612bfd44971b2d7fef842ce4db4a9ca1ec86fdc926767ce31ebfaa
    sha256 of the committed bytes
      0fab1fdc58612bfd44971b2d7fef842ce4db4a9ca1ec86fdc926767ce31ebfaa

The review artifact has no pre-committed hash. Its sha256 is recorded at commit
as its first recorded digest,
`a2c462b9cc465252934d2bfb2288c50dc628ccc11854bf44099c05fc72d46837`, provenance
transmitted by the PI in session. Verdict `APPROVE FOR EXECUTION`.

**Reviewer determinations, recorded as issued and not restated:**

    M0b structure          CONCUR
    M0b authority class    MEASUREMENT
    A5                     DOES NOT FIRE
    Early-return protocol  CLOSED
    Scientific scope       PRESERVED

**`A5` did not fire**, so `M0b` was executed as the measurement the Reviewer
classed it.

Commit order, spec then review, nothing interleaved:

    aaa0f0b169ba35f29f95ab9f2dfab3144c10563e  spec(P2-XI-QM3-DEP-01): v3, ...
    77df230c9fb1031f72d5246de5482b834b31b91b  review(P2-XI-QM3-DEP-01): ...

## 1. `M0` — precondition and provenance

    origin/main                       0c01fc7f26e91dd84b032dccde0feac61f61d8ea
    the Base this specification pins  0c01fc7f26e91dd84b032dccde0feac61f61d8ea
                                      EQUAL, full string

    (a) decisions/P2-XI-RULINGS-02-CLARIFICATION-01.issued.md
        measured 0e549c7c457f22d8e80b62fbca00cf362c410992771ddcee6cad13dc0d363f22
        required 0e549c7c457f22d8e80b62fbca00cf362c410992771ddcee6cad13dc0d363f22

    (b) decisions/P2-XI-RULINGS-02.issued.md
        measured ab2e90ddb6fa8c24c9b913a26b4b455809ca358d82cff2d2256f3526957ebbf5
        required ab2e90ddb6fa8c24c9b913a26b4b455809ca358d82cff2d2256f3526957ebbf5

    (c) derivations/P2-XI-LEDGER-01_conditional-analytic-ledger.md
        present, sha256 aa0c79e21568b09d6efed64ec538c1ee9b4892ebc65653cb76deecfbd25f1454
        OPEN(Q-M2) at :305 and OPEN(Q-M3) at :307, both with an em-dash in
        every numeric cell

The repository is non-shallow. **`A0` did not fire**, and the precondition of
§0b of the specification is satisfied by measurement: the clarification this
task cites for its scope is landed at its canonical path on `main`, so the
citation is to landed state rather than asserted. **`C0` PASS.**

## 2. `M0b` — subject identification

### 2a. How the search was conducted

The specification's §0c surfaces two passages and states that they are
"chat-level inference to verify, not to adopt"; the Reviewer's §4 records that
they are "evidence to verify, not an authoritative closed evidence set" and that
"the outcome is therefore not prejudged". **The search below was conducted by
the executor over landed state and was not limited to that list.**

    search, over the Base                                     result
    ---------------------------------------------------------------------
    git grep -l -i 'Hubbard' -- '*.md'                        50 files
    git grep -i 'decoupl' -- 'derivations/*.md'
      'CONVENTIONS.md' 'decisions/*.md'                       41 hits
    DECISION_LOG.md '^## ' headings matching
      exponent|channel|OPEN-AC|attract|HS                      3 entries
    git grep 'OPEN-AC-1' -- '*.md'                            86 hits
    git grep -e 'Z_HS' -e 'N_α' -e 'N_alpha' -- '*.md'         7 hits;
      the only two outside specs/, reviews/ and reports/ are the
      criterion passage's own two lines
    git grep -i 'auxiliary field' -- 'derivations/*.md'
      'CONVENTIONS.md' 'GATES.md'                              7 hits
    git grep -i -e 'bosonis' -e 'bosoniz' -- 'derivations/*.md'  5 hits
    git grep 'exp\[(g/2)' -- '*.md'                            6 hits
    git grep -i 'assembled chain' -- '*.md'                    5 hits
    git grep -i -e 'Hubbard' -e 'Stratonovich' -e 'auxiliary'
      -- 'paper/*.tex'                                        16 hits

Every statement the search returned that bears on the subject is quoted
verbatim in the artifact with its `path:line`. Eighteen quotations were
extracted with `git cat-file blob <Base>:<path> | sed -n 'A,Bp'` and each was
verified as a byte-substring of the artifact. **No normalization was applied to
either side of any of those comparisons.**

### 2b. What the search corrected in the specification's own surfaced list

**The exponent mapping IS fixed.** §0c surfaces
`derivations/P2-PHASE-01_channel_character.md:141-152` for the proposition that
the HS exponent mapping is not frozen — `g = −2c` or `g = +2c` according to
whether the frozen expression is a term of `S_E` or already sits in the
exponent. That passage says so, and the artifact quotes it. **It is superseded
on that point by a landed PI ruling** at `DECISION_LOG.md:1258-1262`, which
fixes

    g = +2c

The channel-character note is dated 2026-08-08 and the ruling is the
2026-08-08 decision that the note's own withheld verdict prompted; the ruling's
`Reason` says so. **The exponent mapping is therefore not among the unfixed
elements, and §0c's reading of it does not survive measurement.**

The other passage §0c surfaces, `channel_character.md:9`, is accurate about
that note but is not the whole of landed state on the channel: the search found
the later 2026-08-09 route ruling, which §0c does not cite.

### 2c. What the search found unfixed

    element                          landed carriers, all quoted in the artifact
    -------------------------------------------------------------------------------
    which channel, or set of         DECISION_LOG.md:1761-1766 and :1778-1781
    channels, the assembled            the route ruling and its own scope
    chain's decoupling comprises     P2-PHASE-01_input_admissibility_contract.md
                                       :122-127
                                     P2-PHASE-01_microscopic_parameter_domain.md
                                       :347-350
                                     P2-CHANNEL-FREEZE-01_phaseA_freeze.md:109
                                     P2-XI-B0a_induced-xi-scope-assessment.md
                                       :634-636  (Q-M6, landed open)
                                     paper/emergent_gr_paper_v2_15.tex:281-292

    the decoupling prescription —    P2-FIERZSUM-01.md:218-220
    auxiliary variables,             GATES.md:1374-1376
    constraints, Jacobian            P2-CHANNEL-FREEZE-01_phaseA_freeze.md:71-79

**On the channel.** A landed PI ruling does select one — the scalar channel with
a real auxiliary field — and states its own scope: mean-field work for
`P2-PHASE-01`. The landed statements that carry that route forward carry the
same scope, and `OPEN-AC-1` is the `P/V/A/T` construction, which the parameter
domain records does "not block the scalar route, which is the PI's chosen
route." **No landed line extends that route choice to the assembled chain of the
ξ ledger**, whose own definition at the ledger's `COND-1` names no decoupling
and whose chain at `P2-NORM-01:26` contains none. Reading the ruling as fixing
this subject's channel would extend a landed scope, which this task does not do.
Against that, `P2-CHANNEL-FREEZE-01`'s inclusion rule keeps every non-redundant
HS field in the candidate set unless exclusion is affirmatively proven; `Q-M6`
records the ledger's own reading decomposition as landed open; and the
manuscript under verification writes the linearization channel by channel over
`Φ_A` with `G_S = G_P = G`.

**On the prescription.** `P2-FIERZSUM-01.md:218-220` lists "The decoupling
prescription: auxiliary variables, constraints, Jacobian, and an explicit
statement of what is generated dynamically rather than introduced as an
independent field" among the eight deliverables its §4 heading calls "What the
gate must freeze", and `GATES.md:1374-1376` records that note as not registered.
The one landed line that names the HS normalization,
`P2-CHANNEL-FREEZE-01_phaseA_freeze.md:71-79`, lists `HS_scale` as an auxiliary
representation parameter that is not scan eligible — a statement that changing
it creates no new scan coordinate, **not** a statement of its value.

**The manuscript is the SUBJECT of this verification programme, not its
authority**, and is quoted in the artifact on that footing. It is recorded
because what it writes bears on the measurement: it is the text the chain's
construction is read from, and it writes a multi-channel linearization rather
than the single channel the landed route ruling selects for `P2-PHASE-01`.

### 2d. The determination

**`M0b` returns `NOT UNIQUELY IDENTIFIED`**, one of the two pre-registered
returns, with each unfixed element named and its landed carrier quoted.

**No channel was selected, no exponent mapping was fixed, and no preference
among candidates was expressed.** The exponent mapping is reported as fixed by a
landed PI ruling; nothing in this task fixes it. **`A2` did not fire**, and `A1`
does not reach this case: the specification carves it explicitly — "a subject
that is not uniquely identified is `M0b`'s recorded finding, not an abort."

**`C0b` PASS**, including its path-selection limb: on this return `C1`, `C2`,
`C3` and `C5` are NOT EVALUATED, `C0`, `C0b`, `C4` and `C6` are evaluated, and
the artifact carries the Rule 22 subclass and resolution path.

## 3. Rule 22

**`INCONCLUSIVE — CONSTRUCTIVE GAP IDENTIFIED`**, carried in the artifact with
both `COND-R` and `COND-M`.

The subclass is `CONSTRUCTIVE GAP IDENTIFIED` and not `EVIDENCE INSUFFICIENT`:
the landed statements are explicit and they say the object is not fixed. **The
dependence question is returned unasked, and the reason is a property of landed
state, not of the check.**

The resolution path is symmetric, has two branches, is defined in the artifact
and **was not walked**: a landed determination of which channel or set of
channels the assembled chain's decoupling comprises (`R-1`), and a landed
decoupling prescription in the sense `P2-FIERZSUM-01.md:218-220` states (`R-2`).
Both are the PI's. **Nothing in the artifact or this report proposes an answer to
either, prefers a branch, or asserts what the dependence question's answer would
then be.**

## 4. `M4` — scope statement

Present in the artifact at §4, in the wording the early-return path requires,
with all four elements:

    the landed criterion is written for every admissible decoupling      present
    this task was SCOPED to the landed α only, but landed state did not
      uniquely identify that object, so NO dependence evaluation was
      performed                                                          present
    the family-wide residue is the registered open item                  present
    the criterion is therefore NOT discharged in full by this task       present

**The early-return wording claims no evaluation that did not occur.** The
artifact states that `α` was not evaluated, that no `N_α[g]` was constructed,
that no variation was taken, and that no verdict from `M3`'s pre-registered
vocabulary is returned. **`C4` PASS.**

## 5. Measurements that did not run, and why

    M1  did not run.  M0b returned NOT UNIQUELY IDENTIFIED. The
        specification's early-return path is M0 → M0b → M4 → M6, and M1 is
        outside it. No provenance extraction of the check's objects was
        performed, and no A1 evaluation arises: A1 is scoped to "a run
        where M0b returned UNIQUELY IDENTIFIED".
    M2  did not run.  No N_α[g] was constructed and no (i)/(ii)
        decomposition was attempted.
    M3  did not run.  No variation δ log N_α[g] / δR was computed and no
        verdict from its vocabulary is returned. The MEASURE-INHERITED,
        UNDETERMINED branch is likewise not reached; COND-M is carried as
        a condition, not as a finding.
    M5  did not run.  No script and no tests were written; this task adds
        no code, and scripts/xi_qm3_dep.py is not among its deliverables
        on this path. **No suite result is claimed at the Base or at the
        branch tip**, and none was measured: running it would be executing
        part of a measurement the specification says does not run.

**`C5` NOT EVALUATED**, per `C0b` and per `C5`'s own text.

## 6. Acceptance criteria

    C0  PASS           The Base equals the pinned SHA and contains both
                       issued documents at their stated digests, as
                       full-string matches.
    C0b PASS           Exactly one of the two pre-registered returns is
                       recorded; every bearing landed statement is quoted
                       verbatim with a path:line resolving at the Base,
                       located by the executor's own search; no channel is
                       selected, no exponent mapping fixed, no preference
                       expressed; and the path-selection limb holds.
    C1  NOT EVALUATED  M0b returned NOT UNIQUELY IDENTIFIED.
    C2  NOT EVALUATED  As above.
    C3  NOT EVALUATED  As above.
    C4  PASS           The scope statement is present in the early-return
                       wording with all its elements, and claims no
                       evaluation of α.
    C5  NOT EVALUATED  M5 did not run.
    C6  See §7. Its first limb is post-commit and is measured outside this
                       report; its second and third limbs hold here — this
                       report asserts no SHA of its own, and every verdict
                       sentence in the artifact carries COND-R and COND-M.

## 7. Abort conditions

    A0  DID NOT FIRE.  The Base equals its pin and both issued documents
        are present at their required digests.
    A1  NOT REACHED.   Scoped by its own text to a run where M0b returned
        UNIQUELY IDENTIFIED. The specification states that M0b's own case
        is not covered by it.
    A2  DID NOT FIRE.  No channel was selected, no exponent mapping fixed,
        no preference among candidates expressed, and no (i)/(ii)
        decomposition or N_α[g] construction was attempted at all.
    A3  DID NOT FIRE.  No step required answering membership of either
        OPEN row, DET-01's resolution, or the family-wide inquiry. DET-01's
        landed status is quoted as COND-M's citation and is not resolved.
    A4  DID NOT FIRE.  No second decoupling was evaluated, no L-scaling
        measured, no magnitude estimated, neither OPEN row disposed or
        edited, and the ledger artifact is unmodified — it is not among
        this task's changed paths.
    A5  DID NOT FIRE.  The Reviewer classed M0b as MEASUREMENT and recorded
        A5 as DOES NOT FIRE.

## 8. Stops and clarifications (Amendment B)

Nothing stopped execution; the early return is a pre-registered path, not a
stop. Two observations are returned rather than resolved.

### 8a. §0c's surfaced reading does not survive measurement, in one direction

**Category: `OBSERVATION_METHOD_ERROR`, in the specification's own §0c and
corrected here by measurement.** §0c surfaces
`P2-PHASE-01_channel_character.md:141-152` as landed evidence that the exponent
mapping "is not frozen". Measured against landed state, the mapping **is**
fixed, by the PI ruling at `DECISION_LOG.md:1258-1262`, which the note's own
withheld verdict prompted and which post-dates it.

**The determination is unchanged by this**, because the channel and the
prescription are independently unfixed. It is recorded because the
specification's §0c would otherwise stand as an uncorrected reading of landed
state, and because it is the reason the Reviewer's §4 instruction — search
rather than inherit — was not a formality here.

The executor does not amend a specification; the observation is returned.

### 8b. The scope question this determination turns on is the PI's, and is not answered here

**Category: `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`.** The channel limb of
the determination turns on whether the 2026-08-09 route ruling's scope —
"mean-field work for `P2-PHASE-01`" — reaches the assembled chain of the ξ
ledger. **This task read that scope as written and did not extend it**, which is
what `A2` and the Reviewer's §5 require: "The Executor may measure which state is
landed but may not repair non-uniqueness by making a model choice."

**If the PI reads that ruling as already covering the assembled chain, `R-1` is
satisfied by a scope clarification rather than by new work**, and the channel
limb closes. That reading is the PI's to state; this report does not propose it,
and records only that the determination's channel limb rests on a scope boundary
rather than on an absence of any landed selection anywhere. **The prescription
limb, `R-2`, is unaffected either way**: `P2-FIERZSUM-01.md:218-220` lists the
Jacobian among what the gate must freeze, and that note is landed not registered.

## 9. Environment

    python      3.11.15
    pytest      9.1.1
    numpy       2.4.6
    sympy       1.14.0
    ruff        0.15.8
    scipy       ABSENT
    repository  non-shallow

No environment repair was needed and none was performed. **No suite was run**,
per §5: `M5` did not run on this path, this task adds no code, and the only
files it adds are Markdown.

## 10. Push scope

`docs/BRANCHING_POLICY.md` `science/*` scope. **Only
`refs/heads/science/xi-qm3-dep-01` is pushed.** `main` does not move, no other
`science/*` branch moves, and no session or harness branch is pushed.
**Integration is a separate task and is not begun, scheduled or constrained
here.**

`C6`'s first limb — that the final tip differs from `T_early` only by this
report — is post-commit and is recorded outside this file, which is itself the
next commit on `T_early`.

END OF REPORT
