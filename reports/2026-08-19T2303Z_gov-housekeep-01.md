# Execution report — `P2-GOV-HOUSEKEEP-01`

    OUTCOME     STOPPED. Abort A2 fired.
                A §5 ruling cannot be applied as given. Ruling 7 (§5.4)
                names two wording corrections. M3 measured that BOTH are
                attributed to the wrong specification. The quoted strings
                exist and are unique in the repository, but neither is where
                §5.4 says it is, and both true locations are landed
                specifications carrying live `Reviewed specification SHA-256`
                bindings.

                Nothing was landed. No merge was performed. `main` is
                untouched. Rulings 1, 2 and 4 and the §6 mechanism were NOT
                applied, because §3's A2 stops execution rather than
                partially completing it.

**Specification:** `specs/2026-08-19T2303Z_gov-housekeep-01.md`
**Review:** `reviews/chatgpt/2026-08-19T2303Z_gov-housekeep-01.md`
**Base:** `4257e2a22f2460f1669e1869c82130623fd7295b`

**Measurement head: `48a759f573f5062a6d22aec529f4c9277992696c`,** the review
commit — this report is commit 3 and is measured at commit N−1.

---

## §0 — Binding SHA (MEASURED, no A1 abort)

    observed origin/main   4257e2a22f2460f1669e1869c82130623fd7295b
    §0 declared base       4257e2a22f2460f1669e1869c82130623fd7295b

**Equal.** `main` has not advanced. A1 did not fire.

**Review binding (Rule 18, Amendment N).** The
`Reviewed specification SHA-256:` field was checked for PRESENCE before its
value was compared. Present, once, populated, at
`reviews/chatgpt/2026-08-19T2303Z_gov-housekeep-01.md:4`.

    sha256 of the specification bytes as committed
      8a80b7b44feb1ea8f0d42032fdeacbbe7a044d33458ad7db29126b00f7e0eb89
    the review's bound SHA
      8a80b7b44feb1ea8f0d42032fdeacbbe7a044d33458ad7db29126b00f7e0eb89

**MATCH.** Review verdict `APPROVE FOR EXECUTION`.

---

## Environment (MEASURED)

**Amendment D step 0.**

    execution location   /tmp/claude-0/-home-user-2-emergent-gravity/
                         30ed7c63-4aac-52db-8b0d-16eb01e07bca/scratchpad/gov
    worktree identity    a real git worktree (git worktree list), branch
                         science/gov-housekeep-01
    resolved HEAD        48a759f573f5062a6d22aec529f4c9277992696c
    ref reads            /home/user/2-emergent-gravity

    commits reachable from HEAD    560
    commits across all refs        601

    Python    3.11.15 (main, Mar  3 2026, 09:26:23) [GCC 13.3.0]
    pytest    9.1.1
    numpy     2.4.6
    sympy     1.14.0
    ruff      0.15.8
    scipy     ABSENT — ModuleNotFoundError: No module named 'scipy'

`scipy` has been absent for twenty-three consecutive tasks. It is recorded
because its absence is a standing environment fact, not because this task
needed it; nothing measured here imports it.

**Rule 13.** No environment failure occurred. Neither diagnostic order was
exercised.

---

## §2 — Measurements

### `M1` — the PROVISIONAL passages in `decisions/README.md` (MEASURED)

Recorded verbatim, with line spans, at `4257e2a2`. These are the passages
§5.1 would replace.

`decisions/README.md:27` — the section heading:

> ## When a PI decision takes effect — PROVISIONAL, and not a settled rule

`decisions/README.md:29-31`:

> **The following is a PROVISIONAL execution-layer disposition. It is
> reversible by PI adjudication and is NOT a permanent repository rule.** It
> is recorded so that this directory can be used before the question is
> formally settled.

`decisions/README.md:33-39` — the quoted disposition itself, opening:

> > **PROVISIONAL.** A review of a PI decision is recorded, not gating. The
> > […]

`decisions/README.md:41-50` — the pending-adjudication block, opening at
`:41`:

> **The adjudication this stands in for is owed and is named here so it is
> not lost.**

Four passages. Three carry the disposition; the fourth registers the
adjudication as owed. All four are the target of §5.1.

**Substring hazard `provisional` — handled by reading, not counting.** The
token appears both inside the superseded text and inside the sentence that
marks it superseded; a count cannot distinguish the two. The spans above
were established by reading the section.

### `M2` — the three `reviews/pi/` records (MEASURED)

    blob id                                   path                                                                    lines
    930748ed27736c3e369b956b27ec26eb4bff90e8  reviews/pi/2026-08-03-governance-tools-environment-authorization.md        30
    dad02415788f153e2c26266f75d6d7a32c7c6001  reviews/pi/2026-08-03-outcome-based-task-specification-amendment.md       337
    1e9be1f8dbbffb09b416a483c3b0410da1f4c5fa  reviews/pi/2026-08-03-p2-dual-pipeline-probe-repin.md                      80

**Does each state a rule still in force through itself?** Measured by
reading each record's header and operative content:

    #1  governance-tools-environment-authorization
        NO. A task-scoped authorization. It authorized an environment for a
        named task; it does not carry a standing rule.

    #2  outcome-based-task-specification-amendment
        NO — not through itself. Its normative text was relocated: the
        record states at :11 that "the text in `CONVENTIONS.md` governs".
        The rule is in force; this record is not the thing that carries it.

    #3  p2-dual-pipeline-probe-repin
        NO. It states at :75 that "This amendment governs the LANDING-ROUTED
        task only. It sets no [precedent]".

None of the three is a live rule-bearing record. All three are spent in the
sense §5.3 and A6 care about: a retrospective review of any of them would be
provenance work, not a re-opening of an operative rule.

**A6 was checked and did not fire on `M2`.** No §5 or §6 item, as measured,
would require modifying any of the three records — §5.3 lands review
artifacts *beside* the record, never into it. A6's second limb (an
append-only file's history) was also not reached, because execution stopped
before any write.

### `M3` — the two §5.4 passages, current text (MEASURED — THIS IS THE ABORT)

`M3` is the measurement §5.4 requires to be taken first: "`M3` records the
current text first; the report records before and after verbatim for each."
Taking it first is what exposed the defect.

#### `W-1` — attributed to "P2-POLE-B0-INTEG-02, §11"

**The attribution is wrong.** The quoted string does not occur anywhere in
any P2-POLE-B0-INTEG-02 artifact.

Due diligence — every artifact of that task was enumerated from the index
and searched individually, not sampled:

    occurrences of "carries its review"
      reports/2026-08-19T0448Z_pole-b0-integ-02.md                  0
      reports/2026-08-19T0448Z_pole-b0-integ-02_landing-record.md   0
      reviews/chatgpt/2026-08-19T0448Z_pole-b0-integ-02.md          0
      specs/2026-08-19T0448Z_pole-b0-integ-02.md                    0

All four return zero.

The §11 that P2-POLE-B0-INTEG-02 actually carries reads:

> ## 11. What this task does not establish
>
> Landing a negative result adds no support to the programme. This merge
> closes a task. It does not advance `P2-PHASE-01`, which remains
> `Status: PROPOSED` with `Required computations: (not started)`.

There is no wording of the kind §5.4 describes in it.

**Where the string actually is.** A repository-wide search over `*.md`
returns exactly two hits, one of which is the Reviewer quoting the other:

    specs/2026-08-19T1723Z_registry-split-01.md:327          the passage itself
    reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:185  the Reviewer quoting it

The passage, `specs/2026-08-19T1723Z_registry-split-01.md:324-328`, verbatim:

> A directory layout is not a scientific result. This produces no `β_V`,
> moves no gate, and does not advance `P2-PHASE-01`. What it changes is that
> a reader can tell a definition from a hypothesis from a decision from a
> specification review by where the file sits, and that every one of them
> carries its review with it.

It sits under `## 11. What this task does not establish`
(`specs/2026-08-19T1723Z_registry-split-01.md:322`).

**So the section number in the attribution is right and the specification is
wrong.** The passage is in **P2-REGISTRY-SPLIT-01's §11**, not
P2-POLE-B0-INTEG-02's.

#### `W-2` — attributed to "P2-REGISTRY-SPLIT-01, C7"

**The attribution is wrong.** P2-REGISTRY-SPLIT-01's `C7`
(`specs/2026-08-19T1723Z_registry-split-01.md:273-274`) reads:

>     C7   reviews/README.md carries the §6 R-5 amendment; the three
>          reviews/pi/ records are unchanged.

That criterion is about `reviews/README.md`. It contains no article-agreement
error and nothing resembling the quoted string.

**Where the string actually is.** The phrase is line-wrapped in the source,
so a single-line search does not find it; a two-line search does. It is at
`specs/2026-08-19T1141Z_integrate-recon-ext-01.md:286-287`:

>     C7   `D-1`, `D-2`, `D-3`, `O-1`, `O-2` exist, each identified as a
>          execution-layer disposition or an open finding, with identifiers
>          drawn from the selected register's own sequence.

That is **P2-RECON-EXT-01-INTEG's `C7`**. The only other occurrences of the
string in the repository are two downstream mentions of the same typo:

    reviews/chatgpt/2026-08-19T1141Z_integrate-recon-ext-01.md:165
    reports/2026-08-19T1141Z_integrate-recon-ext-01.md:318

both of which name it as an editorial point about that same specification.

**So the criterion label in the attribution is right and the specification is
wrong.** The typo is in **P2-RECON-EXT-01-INTEG's `C7`**, not
P2-REGISTRY-SPLIT-01's.

#### The second obstacle: both true locations are review-bound

Even had the attributions been correct, applying `W-1` and `W-2` would edit
two landed specifications, each pinned by a live review binding:

    file                                            sha256 of current bytes                                             bound in
    specs/2026-08-19T1723Z_registry-split-01.md     6a90c815d4e1912c431e827e0887eb723f2d6bde507782e6245238ad94a49bad    reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:4
    specs/2026-08-19T1141Z_integrate-recon-ext-01.md ac0ec0b59e96094bf285d7d1c2e2ebe0528ab425ceb46699c783ee3c0cda4697   reviews/chatgpt/2026-08-19T1141Z_integrate-recon-ext-01.md:4

Both hash exactly to the values their reviews bind. Changing one byte in
either file breaks the binding under Rule 18: the review would no longer be
approval of the bytes present. That question — whether a landed, review-bound
specification may be edited at all, and if so what happens to its binding —
is not settled anywhere in the repository, and this task has no authority to
settle it.

Note that this is a distinct question from ruling 2's open finding, which
concerns `CONVENTIONS.md` `Statement SHA` entries. The same shape of
question, over a different class of record.

**No before/after pair could be recorded** for either correction, because
there is no "after": no edit was attempted at either the named location (the
string is absent) or the true location (execution stopped first).

### `M4` — which register admits the ruling-2 open finding (MEASURED)

Three registers exist. Their stated scopes, read from their own text:

    derivations/P2-DEFERRED-ITEMS.md
        science-side. ":1-6" — a register for P2-PHASE-01 that "records
        decisions, computes nothing, registers no gate and changes no
        status", under the authority of specs/2026-08-09T1958Z_pi-decisions-v3.md,
        and its own closing text states entries are added by PI decision.
        Does not admit a governance finding.

    derivations/P2-PHASE-01_C-CHECK_OPEN-ITEMS.md
        science-side. ":3-5" — "a register of open items arising from the
        C-check line", the follow-up checks C1, C2 and C3. Scope is that
        line. Does not admit a governance finding.

    docs/GOVERNANCE-DEBT.md
        the governance-side register. Its ":16-19" records that the two
        registers above "already existed at the evidence base and both are
        science-side" and that "There was no governance-side register."

`docs/GOVERNANCE-DEBT.md` is the one that admits the finding. Its stated
dispositions (`:21-32`) are REPAIRABLE, SPECIFIABLE, NOT REPAIRABLE HERE,
RULED, METHOD NOTE, OPEN, and "Each entry carries exactly one". Ruling 2's
finding is explicitly OPEN, which is a disposition the register already
carries.

Its entries run `G-01` (`:48`) through `G-12` (`:258`); the next identifier
in its own sequence would be `G-13`. Nothing was written to it.

`M4`'s fallback branch ("If none does, record the scopes observed and return
to the Researcher") was not reached: a register does admit the finding.

### `M5` — retrospective-review artifacts (MEASURED)

**No retrospective-review artifact for any of the three `reviews/pi/` records
was supplied to this task.** Two attachments were supplied: the specification
and its pre-execution review. Neither is, nor contains, a retrospective
review of a `reviews/pi/` record.

Per §5.3 this is the **"not supplied"** branch, and per the specification's
own words absence "is an expected outcome, not a fault". The branch requires
creating NO review content and instead registering the obligation — three
named records, each owed a retrospective review, status REVIEW PENDING, by
path and blob id — in the register `M4` selects. The paths and blob ids that
entry would carry are exactly those recorded under `M2` above.

That registration was **not written**, because A2 fired. It remains owed.

### `M6`, `M7` — not reached

`M6` (test suite on the merge product and at `4257e2a2`) and `M7`
(`git diff --name-only 4257e2a2..<merge product>`) both take the merge
product as their subject. No merge was performed, so there is no merge
product and neither measurement has a subject. A3, A4 and A5 — which are
defined over `M6` and `M7` — are therefore not evaluated. They did not pass;
they were not reached.

### `M8` — `A-EXT-01` and `H-EXT-01` statement pins (MEASURED)

    A-EXT-01  Statement SHA  ca8e5a870b5c7734321a9b6b97f3844046d8ceb689aece0ca65082b70a522378
              canonical in   CONVENTIONS.md:129
    H-EXT-01  Statement SHA  e5dd8a28eaff7623af23ab11404ef2d43dc8053599807162863cf38aca239a47
              canonical in   assumptions/H-EXT-01.md:53

Both unchanged from `4257e2a2`. This task altered neither — it altered
nothing.

---

## §3 — Abort determination

    A1  DID NOT FIRE.  origin/main equals the §0 base.
    A2  FIRED.         §5.4's ruling 7 cannot be applied as given.
    A3  NOT REACHED.   Defined over M6; no merge product exists.
    A4  NOT REACHED.   No merge was attempted.
    A5  NOT REACHED.   Defined over M7; no merge product exists.
    A6  DID NOT FIRE.  Checked against M2 and against §5.3's landing
                       location. No §5 or §6 item, as measured, requires
                       modifying a reviews/pi/ record or rewriting an
                       append-only file's history.

**A2, verbatim from §3:**

>     A2   a §5 ruling cannot be applied as given — return to the PI; the
>          executor does not reword a ruling

**Why it fires.** §5.4 directs two corrections at two named locations. At
both named locations the text to be corrected does not exist. The ruling
cannot be applied as given.

**Why the obvious repair is not available to the executor.** The quoted
strings do exist elsewhere, and this report says where. But applying the
corrections there would mean editing two documents the ruling did not name —
substituting the executor's identification of the target for the PI's. §0a is
explicit:

> The executor has NO discretion over content. Where a ruling cannot be
> applied as given, that is an abort and a return to the PI.

and §3's A2 adds "the executor does not reword a ruling". Redirecting a
ruling to a different document is a stronger act than rewording it. The
executor located the true text and stopped there; the identification is
evidence returned to the PI, not authority to act.

**Why rulings 1, 2 and 4 and the §6 mechanism were also not landed.** §3's
abort conditions stop execution; they do not select a subset of the
specification to complete. §5 is one transcription block under one authority
statement, and §0a treats a ruling that cannot be applied as a return to the
PI, not as a part to be dropped. Landing three of four rulings would deliver
a repository state no reviewed specification describes, and would leave the
report claiming completion of a specification that was not completed. The
work for rulings 1, 2 and 4 and for §6 is measured and recorded above and is
ready to be applied unchanged the moment §5.4 is re-issued.

---

## §7 — Acceptance criteria

**Not evaluated.** Every acceptance criterion is a property of a landed
state. Nothing was landed. `C4`, `C6` and `C7` in particular are defined over
text this task did not write. Recording them as failed would be as wrong as
recording them as passed; they have no subject.

---

## §8 — Substring hazards, and how each was handled

    review          Named as matching reviews/, Review SHA, pre-execution
                    review, retrospective review, and assumption review.
                    M5 turns on whether a RETROSPECTIVE review was supplied;
                    the two supplied artifacts are a specification and a
                    pre-execution review. Distinguished by reading each
                    artifact's own header, not by matching the word.
    provisional     Decisive for M1. The token appears both in the superseded
                    text and in the sentence marking it superseded. The four
                    spans were established by reading the section; no count
                    was used.
    decision        Named as matching DECISION_LOG.md, decisions/ and
                    "decision owner". M1 reads decisions/README.md by path,
                    so the ambiguity did not arise.
    inconclusive    Relevant only to §6, which was not reached.
    append-only     C4 turns on its ABSENCE as an assertion while the phrase
                    legitimately appears inside the open finding's own
                    question. Not reached — C4 has no subject.

    carries its review    NOT a hazard as it turned out, and this is why the
                          M3 search was conclusive: the string is unique in
                          the repository outside the Reviewer's quotation of
                          it. Zero hits in the named specification and one
                          real hit elsewhere is not an ambiguity — it is a
                          mis-attribution.
    a execution-layer     A line-wrap hazard, and it nearly hid the answer. A
                          single-line search returns only the review and the
                          report that mention the typo, because in the
                          specification itself "a" ends line 286 and
                          "execution-layer" begins line 287. The true
                          location was found only by searching across the
                          line break. Reported because a single-line search
                          would have produced the false conclusion that the
                          string does not exist in any specification.

**Rule.** "A check that cannot state its exclusions is performed by reading."
Both M3 searches were confirmed by opening the surrounding lines and reading
them.

---

## §1a — Non-objectives, all honoured

This task did not, and this report does not:

    author any review, including the retrospective reviews of ruling 4 —
      M5 recorded the "not supplied" branch and created no review content
    settle the CONVENTIONS.md protection model —
      M4 identified where the OPEN finding belongs; the model is not settled
      here and no protection model is asserted
    rewrite, re-file, or amend any historical reviews/pi/ record —
      M2 read all three and recorded their blob ids; all three are unchanged
    re-adjudicate A-EXT-01 or H-EXT-01, or alter any landed exact statement —
      M8 confirms both statement pins unchanged
    integrate science/recon-proj-01 or science/recon-01b-b0-scope —
      neither ref was touched
    perform the INCONCLUSIVE backsweep —
      §6 was not reached
    modify any file under scripts/ —
      no file under scripts/ was read for modification or written

Because nothing was landed, every non-objective is honoured trivially as well
as deliberately. The list is retained because a non-objective honoured only
by an abort is not evidence that it would have been honoured otherwise, and
the measurements above show it would have been.

---

## §4 — Push scope

§4 sets the push scope as "the integration branch and `refs/heads/main` and
no other ref". No merge occurred and nothing lands, so `refs/heads/main` does
not move and is not pushed; `origin/main` remains
`4257e2a22f2460f1669e1869c82130623fd7295b`. The single ref pushed is
`science/gov-housekeep-01`, carrying the specification, its review, and this
report.

`docs/BRANCHING_POLICY.md:34-37` — "Source branch, session branches and
unrelated refs must not move" — is satisfied: no other ref was written.

---

## Stops and clarifications

### S-1 — `SPECIFICATION_DEFECT`: both §5.4 corrections are mis-attributed

**Category:** `SPECIFICATION_DEFECT`.

Ruling 7 names two locations. Neither contains the text it quotes. The
strings are real, are unique in the repository, and both live in a different
specification than the one named:

    W-1  named   P2-POLE-B0-INTEG-02, §11
         actual  P2-REGISTRY-SPLIT-01, §11
                 specs/2026-08-19T1723Z_registry-split-01.md:327
         the section number is right; the specification is wrong

    W-2  named   P2-REGISTRY-SPLIT-01, C7
         actual  P2-RECON-EXT-01-INTEG, C7
                 specs/2026-08-19T1141Z_integrate-recon-ext-01.md:286-287
         the criterion label is right; the specification is wrong

The symmetry is worth noting: in each case the *sub-location* is correct and
the *document* is not, and P2-REGISTRY-SPLIT-01 appears in both — as the
wrongly-named document for `W-2` and as the true document for `W-1`. That is
consistent with a transposition when the ruling was transcribed, not with a
disagreement about what should be corrected.

**Returned to the PI, not decided here.** Two questions:

1. **Re-issue naming the true locations.** The corrections themselves are
   unambiguous and the executor has no objection to either; only the
   addresses are wrong. A re-issued §5.4 naming
   `specs/2026-08-19T1723Z_registry-split-01.md` and
   `specs/2026-08-19T1141Z_integrate-recon-ext-01.md` would be applicable as
   given.
2. **Whether a landed specification may be edited at all.** Both true
   locations carry live `Reviewed specification SHA-256` bindings, verified
   above. An edit breaks the binding. The repository does not say whether
   that is permitted, forbidden, or requires re-review. **This question must
   be answered before either correction can be applied anywhere**, and it is
   the PI's to answer. It is separable from question 1 and larger than this
   task.

A third option exists and is recorded without recommendation: the PI may
rule that non-substantive maintenance of landed specifications is not worth
the cost to their bindings, and withdraw ruling 7 rather than re-issue it.
The `W-2` typo is an article; the `W-1` clause is already qualified in the
Reviewer's own record at
`reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:185`, which reads it
"in the context of reviewed scientific propositions and decisions" — so the
narrowing ruling 7 asks for is, in substance, already on the record beside
the passage.

### S-2 — the ruling-2 open finding and the ruling-4 obligation remain owed

Neither was written. Both are fully measured and neither is blocked by
anything but the abort:

- the ruling-2 OPEN finding belongs in `docs/GOVERNANCE-DEBT.md` as `G-13`;
- the ruling-4 obligation is the three records and blob ids under `M2`, at
  status REVIEW PENDING.

A re-issue that fixes §5.4 needs no other change to §5.1, §5.2 or §5.3.

### S-3 — no `OBSERVATION_METHOD_ERROR` is claimed, and one was nearly made

Recorded because it bears on how much weight the M3 result can carry. The
first `W-2` search was single-line and returned only the review and the
report that *mention* the typo, with no hit in any specification. Read
literally that would have supported a stronger and false claim: that the
string exists nowhere in any specification. It exists; it is line-wrapped.
The reported location rests on the two-line search and on reading
`specs/2026-08-19T1141Z_integrate-recon-ext-01.md:280-295` directly.

The `W-1` result does not depend on this: it was established by enumerating
all four P2-POLE-B0-INTEG-02 artifacts from the file index and searching each
one, then reading that specification's §11 in full.

---

## Layering

This report is measured at commit N−1 (`48a759f5…`). It records what was
measured before the abort and what the abort prevented. Nothing measured
after this commit is written back into it; any such evidence is returned to
the Reviewer in chat.
