# Report — `P2-XI-B0a`: what the repository establishes about the induced ξ ledger

    Specification   specs/2026-08-22T0156Z_xi-b0a.md
    Review          reviews/chatgpt/2026-08-22T0156Z_xi-b0a.md
    Artifact        derivations/P2-XI-B0a_induced-xi-scope-assessment.md
    Branch          science/xi-b0a
    Base            6da1f7cb8ea1d28d7deadb8a938c67365b28384c
    Merge           NONE. `main` did not move.
    Push scope      this task's branch only.

---

## 0. Execution location and worktree identity (Amendment D step 0)

    execution location    /home/user/2-emergent-gravity
    worktree toplevel     /home/user/2-emergent-gravity
    branch at start       main
    resolved HEAD at start 6da1f7cb8ea1d28d7deadb8a938c67365b28384c
    observed origin/main   6da1f7cb8ea1d28d7deadb8a938c67365b28384c
    worktree at start      clean (`git status --porcelain` empty)
    task branch            science/xi-b0a, created at 6da1f7cb

All measurements read committed blobs at the base
(`git cat-file blob 6da1f7cb…:<path>`), not the working tree, so every
statement below is clone-invariant.

---

## 1. Bindings verified before execution (Rule 18, Amendment N)

    occurrences of "Reviewed specification SHA-256" in the review    2
                                                                    (lines 4 and 111)
    sha256 of the specification bytes as supplied
      8251d7f11c6b1d754755e0d082aeb0af0edb51ba1f4fd4f99922bfa3fd48b955
    the review's bound SHA
      8251d7f11c6b1d754755e0d082aeb0af0edb51ba1f4fd4f99922bfa3fd48b955
    sha256 of the COMMITTED specification blob (HEAD~2:specs/2026-08-22T0156Z_xi-b0a.md)
      8251d7f11c6b1d754755e0d082aeb0af0edb51ba1f4fd4f99922bfa3fd48b955

The field is PRESENT and EQUAL to the sha256 of the committed bytes.
Review verdict: `APPROVE FOR EXECUTION`.

Commit order (Rule 15):

    9ae2445  spec(P2-XI-B0a)      the specification
    e7e561f  review(P2-XI-B0a)    the bound review
    5f69287  science(P2-XI-B0a)   the assessment artifact
    (this)   report(P2-XI-B0a)    this report, last

---

## 2. What was measured

Full evidence is in the artifact. Summarised here, not restated.

    M1  34 landed statements of the induced ξ result, each with path, line,
        which of value / scaling / regime it carries, and the KIND of
        artifact carrying it. Kinds found: subject paper under verification,
        archived earlier version of it, repository derivation notes, gate
        register, claim register, decision log, results READMEs, a
        machine-generated raw JSON, script sources, a session log, a
        provenance register, a reproduction record, task reports, status
        documents, and one concept note landed as NOT REGISTERED.

        **The answer to M1's required question is NO.** No landed statement
        of the induced ξ result is carried by a reviewed derivation with a
        specification and a review. Every carrier line was introduced
        between 2026-07-17 and 2026-08-03; the earliest file under `specs/`
        is 2026-08-06T0456Z and the earliest ChatGPT review is
        2026-08-09T1801Z. The one review naming `NORM-01`
        (`reviews/claude/2026-07-19-paper2-followup.md`) reviews a branch
        state and carries no `Reviewed specification SHA-256` field —
        verified by searching the file for that string and finding none.

    M2  CLAIM 1  REFUTED
        CLAIM 2  UNDETERMINED BY READING   (`A4` fired)
        CLAIM 3  REFUTED
        CLAIM 4  REFUTED
        Limb-level evidence is recorded for every claim, and separately for
        limbs whose support differs. No claim is `ESTABLISHED`.

    M3  The term "clean regime" IS landed, in the subject paper only
        (`paper/emergent_gr_paper_v2_15.tex:120`, `:1610`, and in the
        archived earlier version), with its sense glossed at `:1241-1243` as
        the logarithmically induced regime `L ≫ 1`. Four search forms with
        their exclusions recorded, against 741 occurrences of the string
        `clean` at the evidence base. No landed statement defines what the
        regime EXCLUDES — recorded as silence.

    M4  `Q5 = NOT ANSWERABLE` (`A4` fired). Three candidate delimiting
        statements were found and each is recorded with why it does not
        settle the question.

    M5  `SI-2` scope reproduced verbatim from `GATES.md:1120-1123`.
        Determination: PARTLY WITHIN, with four numbered reasons — two
        placing the question inside, two outside.

    M6  Eight model-level questions listed. None answered.

    M7  `A-EXT-01` ca8e5a87…a522378 (`CONVENTIONS.md:129`);
        `H-EXT-01` e5dd8a28…a239a47 (`assumptions/H-EXT-01.md:53`).
        Both unchanged; neither file modified —
        `git diff 6da1f7cb -- CONVENTIONS.md assumptions/H-EXT-01.md` is
        empty.

---

## 3. Abort conditions

    A1  DID NOT FIRE   observed origin/main equals §0's base
    A2  DID NOT FIRE   `main` at 6da1f7cb at start and at end
    A3  DID NOT FIRE   SI-2 scope statement located at GATES.md:1120-1123
    A4  FIRED TWICE, NARROWLY
                       on `M2.2` — CLAIM 2's submeasurement stopped, the
                       model-level question recorded as `Q-M8`, the claim
                       classified `UNDETERMINED BY READING`, and the
                       remaining claims continued;
                       on `M4` — the question recorded as `Q-M1` and `Q5`
                       answered `NOT ANSWERABLE` per §0c, with the question
                       given as the reason.
    A5  FIRED          `M5` established overlap. The remaining measurements
                       were completed and the assessment produced; what
                       stopped is the proposal of any ordering or follow-on
                       work. The overlap is returned for a PI ruling.
    A6  DID NOT FIRE   `git status --porcelain` showed exactly one path,
                       the artifact; no path outside the §0e manifest was
                       modified.

`A4` and `A5` are the narrow ones and each still yielded a recorded value, so
no acceptance criterion was left unsatisfiable.

---

## 4. Acceptance criteria

Each verified by a probe that reports the text it matched, under one
normalisation applied to both sides — blockquote prefixes stripped, code and
emphasis delimiters stripped, whitespace including line breaks collapsed.

    C1   PASS  `M1` present; artifact kind recorded per statement (§1a's
               fourth column, 34 rows), plus §1b separating the adjacent ξ
               quantities that are not the induced ξ result.
    C2   PASS  All five claims of §0b appear as stated — verified by
               normalised substring match against the committed
               specification's own §0b bytes — and each of CLAIMS 1–4 is
               followed by its classification, verified by offset ordering.
               CLAIM 5 appears as stated and carries no classification,
               because §0c removes it from `M2`; §2.5 records that
               explicitly and points to `Q5`.
    C3   PASS  Exactly three classification strings occur: `REFUTED` ×3,
               `UNDETERMINED BY READING` ×1, `ESTABLISHED` ×0. A regex over
               `Classification: <VALUE>` found no other value. Silence is
               recorded as `UNDETERMINED BY READING` in `L3b` and in
               CLAIM 2's `L2b`, never as `REFUTED`. The compound rule is
               applied to all four claims with limb-level evidence.
    C4   PASS  `M3` records four search forms and, for each, the text
               matched and what it excluded, with per-sense counts.
    C5   PASS  `Q5 = NOT ANSWERABLE`, one of §0c's two forms, with `M4`'s
               evidence — three candidate delimiting statements and what
               was searched and not found.
    C6   PASS  `GATES.md:1120-1123` reproduced byte-exactly, verified by
               comparing against the committed blob; the determination
               carries four numbered reasons rather than an assertion.
    C7   PASS  `M6`'s eight questions are listed and none is answered.
               Read-through for recommendation language
               (`recommend`, `should`, `next step`, `follow-on task`,
               `propose`, `XI-B0b`) returns zero occurrences.
    C8   PASS  §9 carries §5 item 7's statement with each named item:
               no claim established by failure to find contrary evidence
               (item 1); `UNDETERMINED BY READING` is not refutation
               (item 2); nothing computed (item 3); provenance kind
               recorded and not ranked (item 4).
    C9   PASS  No new physics quantity is computed. §8 lists every
               numerical value with the landed statement it is cited from,
               and the artifact's header states the same. The only number
               this task produced is a count of occurrences of a string in
               landed files, disclosed as such in §8's last row.
    C10  PASS  Both Statement SHAs recorded and unchanged; neither file
               modified (§2 above).
    C11  PASS  One path modified, `derivations/P2-XI-B0a_induced-xi-scope-
               assessment.md`, inside the §0e manifest as `P1`; the spec,
               review and this report are `P2`. `main` unmoved at
               6da1f7cb. Only `science/xi-b0a` pushed.

---

## 5. Environment

    python 3.11.15, numpy 2.4.6, sympy 1.14.0, pytest 9.1.1, ruff 0.16.3
    scipy ABSENT — as on the preceding tasks; nothing here needs it
    validators: 332 passed, 2 deselected

No environment restoration was needed this task; the declared environment was
already in place from earlier in this session.

---

## 6. Stops and clarifications (Amendment B)

**Primary category: `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`.**

Three items are returned rather than decided. None is a defect finding against
the specification, the review, or the repository.

1. **The `SI-2` overlap, per `A5`.** `M5` determines the request's
   per-channel question to be PARTLY WITHIN `SI-2`'s landed scope: within on
   the decomposition object and on the input to `SI-2`'s honest prior,
   outside on the sense of "channel" and on the verdict form. `A5` requires
   this to be returned for a PI ruling and forbids this task from proposing
   any ordering. **It is returned, and nothing is proposed.**

2. **The two model-level questions `A4` fired on.** `Q-M1` — which ξ
   observable the ledger is of — is itself landed as an open question at
   `derivations/P2-FIERZSUM-01.md:559-562`. `Q-M8` — at what level "sign
   cancellation" is to be assessed — depends on it. Both are recorded in
   `M6` and answered by nobody here.

3. **`M1`'s provenance finding, recorded as kind and not as a defect.** No
   landed statement of the induced ξ result is carried by a reviewed
   derivation with a specification and a review. The specification's §2
   states that this task records what carries what and does not rank the
   kinds, and §9 item 4 of the artifact says so in the artifact itself.
   **Whether that finding warrants any action is not this task's to say,
   and the artifact says nothing about it.**

**No `INCONCLUSIVE` was recorded**, so Rule 22's subclass-and-resolution-path
requirement has no subject in this task. Every measurement returned a value:
four claim classifications, one `Q5` answer, one overlap determination, one
provenance answer, and two unchanged digests.

**Nothing measured after this report will be written back.** Anything the
Reviewer raises is returned in chat.

---

## 7. Push scope

`refs/heads/science/xi-b0a` only. `main` is not pushed and did not move.
No merge, no force-push, no `--force-with-lease`, no branch deletion, no
history rewrite.

**A stop hook may ask for the session branch to be pushed. It is declined**,
per the specification's §4 push-scope clause and `docs/BRANCHING_POLICY.md:37`.

---

## 8. Governance checker (`P1`–`P9`)

Run over `6da1f7cb..HEAD` with `specs/2026-08-22T0156Z_xi-b0a.md` as the
subject specification.

    P1  NOT_PARSEABLE   the specification declares no `add:` scope total; it
                        uses the §0e path manifest form instead. The checker's
                        own `does_not_establish` field states that
                        NOT_PARSEABLE "is not a pass and is not a finding
                        about that specification's scope."
    P2  PASS            Rule 15 commit order
    P3  PASS            append-only, on both measures
    P4  PASS            superseded branches not merged
    P5  NOT_APPLICABLE  no merge commit in range — this task merges nothing
    P6  PASS            commit-message hygiene
    P7  PASS            gate integrity
    P8  PASS            Rule 15 placement and specification-first
    P9  PASS            report carries "Stops and clarifications"

    overall  INCOMPLETE, on `P1` alone

**Recorded honestly rather than smoothed:** `P3` and `P7` are `NOT_DECLARED`
when run with the specification alone, because this specification declares
neither an append-only set nor an authorised-gate set. They read `PASS` above
because the sets were supplied to the checker by this task —
`append_only_paths` as `["DECISION_LOG.md"]` and `authorised_modified_gates`
as the empty set, the empty set meaning "nothing may change". **The checker
states it is silent about whether a supplied set is the right one or
complete.** Both results are consistent with the fact this task modifies
neither register nor gate: the only path it modifies is the artifact.
