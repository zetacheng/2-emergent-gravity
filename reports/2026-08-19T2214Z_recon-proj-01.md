# Execution report — `P2-RECON-PROJ-01`

    OUTCOME     ADJUDICATED. No abort fired. No landing; main unmoved.
                Q1  INCONCLUSIVE, reason UNDETERMINED BY READING
                Q2  Component 5 — SPECIFICATION ONLY, unchanged; ground narrowed
                Q3  Component 9 — a specification gap, decisively NOT
                    physical completeness

**Specification:** `specs/2026-08-19T2214Z_recon-proj-01.md`
**Review:** `reviews/chatgpt/2026-08-19T2214Z_recon-proj-01.md`
**Artifact:** `derivations/P2-RECON-PROJ-01_projection-adjudication.md`, 579 lines
**Evidence base:** `4257e2a22f2460f1669e1869c82130623fd7295b`

**Measurement head: commit 3, `63ba16a4964b6414a90f890f03ce6010e4a6d293`.**
`C10`'s push half is INTENDED and measured in the post-report layer.

**Nothing was computed.** No `β` of any kind, no ratio, no `k`-scan, no lattice
evaluation.

---

## §0 — Binding SHA (MEASURED, no A1 abort)

    observed origin/main   4257e2a22f2460f1669e1869c82130623fd7295b
    §0 declared base       4257e2a22f2460f1669e1869c82130623fd7295b

**Equal.** §0's sequencing note anticipated that the governance housekeeping
might land first and make this a stale-base stop. **It did not; the base is as
declared and this task ran first.**

**Review binding (Rule 18, Amendment N).** Field present, once, populated —
checked before its value.

    sha256 of the specification bytes as committed
      758d0b97332d07ea903064de1a79f0d3367027a3f5b9dd0e0daa542a0e263deb
    the review's bound SHA
      758d0b97332d07ea903064de1a79f0d3367027a3f5b9dd0e0daa542a0e263deb

**MATCH.** Verdict `APPROVE FOR EXECUTION`.

---

## Environment (MEASURED)

**Amendment D step 0.** Ref reads in `/home/user/2-emergent-gravity`; commits
in the linked worktree `…/scratchpad/rproj`, created at the evidence base.

**Rule 13's diagnostic order.** Not shallow; 423 commits on HEAD, 595 across
all refs; Python 3.11.15; pytest 9.1.1; numpy 2.4.6; sympy 1.14.0; ruff 0.15.8;
**`scipy` ABSENT.** **No environment failure occurred, so neither of Rule 13's
two diagnostic orders was exercised.**

---

## Q1 — `INCONCLUSIVE`, and the location that made it so

**`paper/emergent_gr_paper_v2_15.tex:770-788` satisfies Part 1 beyond
question** — it is a derivation from a Ward identity established at `:752-768`
from diffeomorphism invariance and Symanzik power counting, combined with a
trace identity, applied in the Barnes–Rivers projector basis. It defines
nothing.

**It also carries the physics conclusion Part 2 asks for:** non-TT structures
enter at `O(p⁴/Λ²)` while the TT block carries the `p²` term — and the `p²`
coefficient is the target observable, which `P2-NORM-01:35-37` records as
exactly *"the coefficient of `p²` in the transverse-traceless graviton
self-energy"*.

**What is not stated anywhere is that the manuscript's "non-TT" set is the
complement the axis-TT projection discards.**

**MEASURED: three repository locations mention both the axis-TT projection and
the Barnes–Rivers decomposition, and all three tell the reader to distinguish
them** —
`derivations/P2-RECON-EXT-01_discarded-external-space.md:116` and
`specs/2026-08-19T0223Z_pole-b0-milestone-scope.md:191`, *"Distinguish an
axis-TT projection from a Barnes–Rivers spin decomposition if both appear"*;
`reports/2026-08-19T0223Z_pole-b0-milestone-scope.md:307`, the heading *"the
axis-TT versus Barnes–Rivers distinction"*; and
`…discarded-external-space.md:124` calls the Barnes–Rivers projectors *"acting
on a different object."*

**Supplying the identification would be performing the derivation myself, which
§3 forbids.** The outcome is therefore `INCONCLUSIVE` with reason
**`UNDETERMINED BY READING`**, per §4 and R1 — a completed measurement, not a
failure.

**A second, independent gap is recorded at the artifact's §4.2**, because it
would survive resolution of the first: `:770-788` derives its suppression for
the **IR effective kernel** under Symanzik power counting for the **improved**
stress tensor up to contact terms, while `EXT-01` measured a **finite-spacing,
unimproved lattice bubble**. Whether the conclusion transfers is a second
question reading does not answer. **The two are recorded as concerning
different objects and different regimes, and are NOT presented as
contradicting.**

**`L2` is nonetheless recorded as the closest existing material**, so a later
derivation knows where to start: it already carries the conclusion in the
Barnes–Rivers language, and what it lacks is the bridge to the basis the
extraction uses.

---

## §2 — Measurements

**M1 — sixteen locations, every one opened and read**, with path, line span and
content in the artifact's §2.1.

**The corpus is defined by a substring separation that is the largest single
effect in this task's evidence.** §8 names `complement` as matching unrelated
prose, and it does:

    matching lines for "complement"                  278
      "complement root" — the P2-PHASE-01 C1 line      84
      "complementary"                                  14
      "external complement", the §3 sense               12

**Twelve lines of two hundred and seventy-eight.** A count would have been
96% noise.

**M2 — every location classified against both parts**, in a sixteen-row table.
**Zero satisfy both. One cannot be settled by reading.**

**M3 — five near-misses**, each with which part failed and why: `:752-768` (the
Ward identity itself, whose conclusion is transversality); `:816-833` (gauge
redundancy → common `κ`, a conclusion about coupling universality);
`:1130-1138` and `mlog_coeff.py:1-13` (which part of the *slope* is universal —
**this is the ground `EXT-01`'s M7 identified**, and §3's test shows why it is
not a ground for completeness); and `betav_discriminating_power.md:20-30`.
**None promoted.**

**M4, M5, M6, M7** — below and in the artifact.

---

## Q2 — Component 5: state unchanged, ground narrowed by measurement

**The classification is `SPECIFICATION ONLY`, taken from (i) and (ii) alone.**
(iii) is recorded and explicitly marked as not contributing — the component is
not downgraded because completeness is unproven, and not upgraded because the
recipe is documented and implemented.

**One half of the landed ground does not hold as measured.** The landed
assessment at `…RECON-01_scope-assessment.md:526-531` gives two reasons: the
gate requires pre-registration with targets kept out of code and tests, **and
"the existing recipes live inside target-bearing recovered modules."**

**MEASURED on `scripts/recovered_2026/mlog_coeff.py`, the module that DEFINES
`TT_RECIPES`:**

    (k+2)  0      k+2  0      -3  0      beta_V  0      β_V  0

**It carries no RECON anchor.** The one benchmark in its docstring is
`beta_Dirac / beta_real-min-scalar = 2`, labelled in place a *"convention-free
ratio test"* — a heat-kernel species ratio, a different quantity. **The
target-bearing module is `proca_loop.py:18`, which imports `TT_RECIPES` from
`mlog_coeff`, not the other way round.**

**What survives and is decisive:** `GATES.md:376` names `mlog_coeff.py` as
**recovered**, so the clean-room objection stands on provenance; and
`GATES.md:748` requires a **pre-registered** projection which **MEASURED does
not exist** — no RECON pre-registration note is in `derivations/` (the only
`prereg` file belongs to the `BETAV-CAMPAIGN` line), and
`…cleanroom_reconstruction.md:31` still lists pre-registration as to be done.

**The specification side is materially stronger than it was.**
`derivations/P2-RECON-EXT-01_discarded-external-space.md §1` now reproduces the
five recipes verbatim **in a target-free artifact**, identifies the
unit-Frobenius normalisation from the repository's own comment, and verifies
orthonormality and completeness. **That did not exist when component 5 was last
classified.** It is not a pre-registration for the gate, and `:748` remains
unmet.

---

## Q3 — Component 9: a specification gap, and not the one the question names

**The unmet requirement is a numerical tolerance value or rule.** The *form* is
stated — `…RECON-01_scope-assessment.md:319-322`, *"the `RECON-01` derivation
states the form the tolerance must take … but no value or rule"* — and the
correlation's origin is derived at
`…ASSEMBLY-01_bookkeeping_regression.md:33-39`, where numerator and denominator
are the same integral `C` times different rational prefactors so that `C`
cancels and the tolerance is a propagated *ratio* error.

**`(b)` physical completeness is excluded cleanly.** A propagated ratio-error
tolerance would be required whatever `H-EXT-01`'s status. **Every repository
line bearing on component 9 was enumerated — eleven of them — and none mentions
the projection at all.**

**The boundary the two-way question does not name, recorded rather than
forced.** `(a)` is *"specification of the observable"*, and the observable's
definition **is** supplied — by `A-EXT-01`, and by `CONVENTIONS.md:20` before
it. What component 9 lacks is an **acceptance tolerance for a comparison
performed on that observable**: neither the observable's definition nor the
projection's completeness.

**Recorded as `(a)` on the axis the question draws, with that qualification.**

**Consequence for the premise `Q3` carries:** **`A-EXT-01` did not change
component 9's readiness state**, and the reason is now evidence-backed rather
than assumed — **the gap was never the observable's definition, so supplying
that definition could not close it.**

---

## M6 — and a baseline fact worth stating

    Component 5    SPECIFICATION ONLY    unchanged
    Component 9    SPECIFICATION ONLY    unchanged

**MEASURED: `P2-RECON-01B-B0_scope-assessment.md` is NOT present at this
evidence base, and `science/recon-01b-b0-scope` is NOT an ancestor of it** —
`git merge-base --is-ancestor` returns exit 1. **That re-measurement was pushed
and never integrated.** The landed baseline is therefore
`P2-BETAV-RECON-01_scope-assessment.md:479`, `:483`.

**The comparison is unaffected** — the unlanded assessment also classified both
`SPECIFICATION ONLY` — **but the fact is recorded so the baseline is not
mistaken for the more recent document.**

---

## §7 — Acceptance criteria

    C1   PASS   M1 present, sixteen locations with path and line span for each.
    C2   PASS   M2 classifies all sixteen against both parts of §3, in a table
                giving Part 1, Part 2 and the classification per row.
    C3   PASS   M3 lists five near-misses with reasoning, each naming which
                part failed and why. None promoted.
    C4   PASS   Q1's outcome is INCONCLUSIVE — one of §4's three — with the
                location named, what would settle it recorded, and
                UNDETERMINED BY READING recorded as the reason and not as an
                outcome.
    C5   PASS   M4 separates (i)/(ii) from (iii); (iii) is marked as not
                contributing and the classification is taken from (i)/(ii)
                alone. Verified by reading.
    C6   PASS   M5 reaches (a) with the boundary stated, and excludes (b) with
                the evidence shown — eleven lines enumerated, none mentioning
                the projection.
    C7   PASS   Verified by reading every "block", "gate", "prerequisite" and
                "precondition" occurrence. The artifact contains NO statement
                that any Q1 outcome blocks, gates, or is a prerequisite for
                RECON-01b. The occurrences are: one negation at :18 saying no
                outcome does; "TT block" and "scalar block" in quoted
                manuscript text; "GATES.md" and "the RECON-01 gate" as names;
                and "pre-registration". Satisfied by absence.
    C8   PASS   M6 records both classifications, that neither differs from the
                last landed assessment, and the cause for each.
    C9   PASS   Both Statement SHAs recorded and recomputed on the branch:
                A-EXT-01 ca8e5a87… and H-EXT-01 e5dd8a28…, both matching.
                Neither file was modified — neither appears in the diff.
    C10  PASS on the manifest half — the diff lists three paths, all inside
                §1c: the artifact (P1), and this task's spec and review (P2),
                with this report joining as P2. Zero scripts/ paths.
                INTENDED on the push half.
    C11  PASS   No computed numerical result and no β value. The two `beta_V`
                and `(k+2)` occurrences are search terms in §6.1's table,
                shown with their measured count of ZERO in mlog_coeff.py. The
                two numeric quotations — m = 0.3 and exponents in
                [1.9887, 1.9982] — are quoted from the landed EXT-01 artifact
                with attribution, to make §4.2's point; nothing was computed
                here. Every other decimal is a section number.

---

## §8 — Substring hazards, and how each was handled

**`complement` matches unrelated linear-algebra prose — and this was the
largest effect in the task.** 84 of 278 matching lines are the P2-PHASE-01 C1
line's *complement root*, an unrelated root-finding sense; 14 are
*complementary*; **12 are the §3 sense.** The corpus was built by reading the
matched lines, not by counting them.

**`definition` matches "definitional", "defined" and "by definition", and §3
turns on whether a statement is definitional — so it was never counted.**
Each location's Part 1 status was decided by reading what the location does,
which is why `L5`, `L7`, `L9`, `L10`, `L12` and `L13` are classified as
definitional or requirement-stating without any token test.

**`TT` matches `tt_check` and `TT_RECIPES` — different objects.** The artifact
writes `TT_RECIPES` in full with its file and line; `tt_check` does not appear.

**`ground` matches "background" and "grounding".** The §3 sense is narrow and
every candidate was read; no location was admitted or excluded on the token.

**`projection` does not match `projector`.** Both appear in the evidence and
are kept apart — the projection is the object adjudicated, the Barnes–Rivers
projectors are the basis `L2` decomposes in.

**`A-EXT-01` / `H-EXT-01` share a suffix.** Both were read in full; neither was
located by a token search.

---

## §1b — Non-objectives, all honoured

**Nothing was computed.** **`A-EXT-01` was not re-adjudicated and no landed
exact statement was altered** — C9 recomputes both digests and neither file
appears in the diff. **`H-EXT-01` was not resolved in either direction**, and
the artifact says in its own §9 that `INCONCLUSIVE` is not evidence of falsity.
**No criterion or threshold was set.** **The EXT-01 diagnostic was not
extended** — that is `D-2`. **`RECON-01b` was not executed.** **No file under
`scripts/` was modified** — zero `scripts/` paths in the diff. **Nothing was
merged and `main` did not move.**

---

## Stops and clarifications

**`SPECIFICATION_DEFECT`** — **none.** Three provisions did real work and are
worth naming, because each changed the outcome.

**§3's "the examples in Part 1 are illustrative and not a closed list."**
`L2`'s derivation runs through Symanzik effective-theory power counting applied
to a lattice-broken Ward identity — a route the taxonomy does not name. **Under
a closed list it would have failed Part 1 and the strongest location in the
repository would have been recorded as no derivation at all.** The review's §2
identified this in a prior revision; the repair is what let the location be
assessed on its merits.

**§4's three-outcome vocabulary with `UNDETERMINED BY READING` as a reason.**
The honest result here is neither `FOUND` nor `NOT FOUND`: a location carries
the conclusion and the bridge to the extraction's basis is unstated. **A
two-outcome question would have forced a false `NOT FOUND`.**

**§3's instruction that a location the executor believes should count but which
fails a part is recorded, not promoted.** I did believe `L2` should count, and
§3 is why it is recorded as `INCONCLUSIVE` with the gap named rather than
promoted to `FOUND` on an identification I would have supplied.

**`ENVIRONMENT`** — `scipy` absent for the twenty-first consecutive task;
nothing failed and nothing needed it. **No environment failure occurred, so
neither of Rule 13's two diagnostic orders was exercised.**

**`OBSERVATION_METHOD_ERROR`** — **none.** The `complement` separation is the
case that would have produced one: a corpus built by counting would have been
96% unrelated prose and would have buried the twelve relevant lines.

**`REPOSITORY_DEFECT`** — **none newly found.** Two observations, recorded and
not classified.

**First, and it is a correction to a landed statement of mine.** The landed
component-5 ground says the recipes *"live inside target-bearing recovered
modules"*. **Measured, the defining module carries no RECON anchor.** The
conclusion — not potentially applicable — stands on provenance and on the
absent pre-registration, but **one of the two stated reasons does not hold as
measured**, and a later reader relying on it would be relying on something
inaccurate.

**Second**, `GATES.md:304-306` records in the repository's own gate text that
the axis-TT-slope normalization map is *"taken on the paper's word (not
text-derivable to the factor-of-2 level)"*. **The repository already records
that a piece of the axis-TT apparatus is inherited rather than derived.** It is
not a ground and not a near-miss; it points the same way as `Q1`'s outcome and
is recorded so it is not lost.

**`UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`** — **four, recorded in the
artifact's §10 as open.** Whether the Barnes–Rivers non-TT blocks and the
axis-TT discarded complement are the same space — the reason `Q1` is
`INCONCLUSIVE`; whether `L2`'s suppression transfers to a finite-spacing
unimproved bubble — independent of the first and surviving its resolution;
whether a clean-room re-expression of `TT_RECIPES` counts as reuse, now
narrowed but not settled; and whether *"specification of the observable"* was
meant to include the acceptance tolerance.

---

## Layering

**Everything above is measured at commit 3,
`63ba16a4964b6414a90f890f03ce6010e4a6d293`**, except `C10`'s push half.

**Returned to the Reviewer in chat, not written back:** the push exit status,
the remote read-back, and confirmation that `main` is still `4257e2a2`.

**This task performs no landing.** Any register consequence of the two
classifications follows in a later integration specification, per `§1c`.
