# Execution report — `P2-GOV-HOUSEKEEP-02`

    OUTCOME     MERGED AND LANDED. No abort fired.
                Rulings 1, 2 and 4 landed; the INCONCLUSIVE disposition
                mechanism landed as CONVENTIONS.md rule 22 with its PI
                decision record; ruling 7 disposed of as errata with zero
                specs/ files modified.

                M3 RE-MEASURED both W- locators and both MATCH the files
                §0a names. A2 did not fire. The re-issue's corrected
                locators are confirmed, not assumed.

**Specification:** `specs/2026-08-19T2324Z_gov-housekeep-02.md`
**Review:** `reviews/chatgpt/2026-08-19T2324Z_gov-housekeep-02.md`
**Base:** `4257e2a22f2460f1669e1869c82130623fd7295b`
**Supersedes:** `P2-GOV-HOUSEKEEP-01`, `science/gov-housekeep-01 @ e44920b6`,
stopped `A2`, **not integrated and not touched by this task**.

**Measurement head: the merge product,
`55b28783296f232173ff4e6ce5e65dea070aafd1`.** This report is the commit after
it. `C16` is INTENDED and measured in the post-report layer.

---

## §0 — Binding SHA (MEASURED, no A1 abort)

    observed origin/main   4257e2a22f2460f1669e1869c82130623fd7295b
    §0 declared base       4257e2a22f2460f1669e1869c82130623fd7295b

**Equal.** The aborted predecessor left `main` unmoved, as its stop report
said it would.

**Review binding (Rule 18, Amendment N).** The
`Reviewed specification SHA-256:` field was checked for PRESENCE before its
value was compared. Present, once, populated, at
`reviews/chatgpt/2026-08-19T2324Z_gov-housekeep-02.md:4`.

    sha256 of the specification bytes as committed
      70bff4b4a54cd1e968b76899b91fa70e6b804e860df9bbd1be017e8924eb0461
    the review's bound SHA
      70bff4b4a54cd1e968b76899b91fa70e6b804e860df9bbd1be017e8924eb0461

**MATCH.** Verdict `APPROVE FOR EXECUTION`, with one non-blocking observation
at the review's §19, answered under `M-obs` below.

---

## Environment (MEASURED)

**Amendment D step 0.**

    execution location   /tmp/claude-0/-home-user-2-emergent-gravity/
                         30ed7c63-4aac-52db-8b0d-16eb01e07bca/scratchpad/
                         gov2   (task branch)
                         igov2  (integration branch)
                         base02 (detached at 4257e2a2, for comparison)
    worktree identity    three real git worktrees, confirmed by
                         git worktree list
    resolved HEAD        science/gov-housekeep-02      434b61f
                         science/integrate-gov-h...02  55b28783 (merge product)
    ref reads            /home/user/2-emergent-gravity

    commits reachable from base    558
    commits across all refs        602

    Python    3.11.15 (main, Mar  3 2026, 09:26:23) [GCC 13.3.0]
    pytest    9.1.1
    numpy     2.4.6
    sympy     1.14.0
    ruff      0.15.8
    scipy     ABSENT — ModuleNotFoundError: No module named 'scipy'

`scipy` has been absent for twenty-four consecutive tasks. Nothing measured
here imports it.

**Rule 13.** No environment failure occurred. Neither diagnostic order was
exercised.

**§4's clause, located before use**, at `docs/BRANCHING_POLICY.md:25-40`:
`science/*` is a recognized branch class; `--no-ff` into a dedicated
integration branch; squash/rebase prohibited; "During landing, only the
integration branch and `refs/heads/main` may be pushed"; "Source branch,
session branches and unrelated refs must not move"; `main` advances only by
fast-forward.

---

## §2 — Measurements

**Nothing was carried.** `P2-GOV-HOUSEKEEP-01`'s stop report measured `M1`,
`M2`, `M4`, `M5`, `M8` and the two locators. **Every value below was measured
afresh at `4257e2a2`**, and one of them came out differently.

### `M1` — the PROVISIONAL passages in `decisions/README.md` (MEASURED)

**Five passages, not four.** The predecessor's stop report recorded four. Read
afresh, a fifth belongs to the same disposition:

    :27        heading
               ## When a PI decision takes effect — PROVISIONAL, and not a
               settled rule
    :29-31     the disposition's framing
               "**The following is a PROVISIONAL execution-layer disposition.
               It is reversible by PI adjudication and is NOT a permanent
               repository rule.** It is recorded so that this directory can
               be used before the question is formally settled."
    :33-39     the quoted disposition, opening
               "> **PROVISIONAL.** A review of a PI decision is recorded, not
               gating. …"
    :41-50     the pending-adjudication block, opening at :41
               "**The adjudication this stands in for is owed and is named
               here so it is not lost:**"
    :52        the closing warning
               "**A reader must not mistake the disposition above for a
               settled rule.**"

`:52` is a passage stating the PROVISIONAL disposition's status and is
meaningless once the disposition is superseded. It is inside §5.1's
replacement scope and was replaced with the rest.

**Substring hazard `provisional` — handled by reading.** The token appears in
both the superseded text and the sentence marking it superseded; a count
cannot distinguish them. The spans were established by reading the section
whole. **This is exactly how the fifth passage was found and the predecessor's
count corrected** — a count of the token would have found neither.

### `M2` — the three `reviews/pi/` records (MEASURED)

    blob id                                   path                                                                    lines
    930748ed27736c3e369b956b27ec26eb4bff90e8  reviews/pi/2026-08-03-governance-tools-environment-authorization.md        30
    dad02415788f153e2c26266f75d6d7a32c7c6001  reviews/pi/2026-08-03-outcome-based-task-specification-amendment.md       337
    1e9be1f8dbbffb09b416a483c3b0410da1f4c5fa  reviews/pi/2026-08-03-p2-dual-pipeline-probe-repin.md                      80

Header and operative content read for each:

    #1  A PI authorization record for one task's external environment —
        interpreter, a venv of pytest/ruff/numpy/sympy, and a safe.directory
        entry. Closes at :29-30: "This is not a general installation
        precedent." Task-scoped; carries no standing rule.

    #2  The RATIONALE RECORD for the rules 8-12 adoption. States its own
        standing at :7-11: "It is not itself the normative text and is not an
        executable instruction… where the two differ in wording, **the text
        in `CONVENTIONS.md` governs**." The rules it discusses are in force;
        this record is not the thing that carries them.

    #3  A PI amendment record for a mid-task probe re-pin. Its "## Standing"
        section at :73-78: "This amendment governs the LANDING-ROUTED task
        only. It sets no precedent…"

**None of the three carries a live rule through itself.** A retrospective
review of any is provenance work, not the reopening of an operative rule —
which is what makes ruling 4 discharge-able without touching a rule in force.

### `M3` — the two `W-` locators, RE-MEASURED (this is what `A2` turns on)

**Method.** Whitespace was normalised across each file — every run of
whitespace, newlines included, collapsed to one space — with a
character-to-line map retained so a match spanning a line break still reports
its true line span. The search ran over **every file `git ls-files` returns**,
excluding only this task's own two artifacts, so the result is a uniqueness
statement and not a spot check.

    W-1  "carries its review with it"
         2 hits repository-wide:
           specs/2026-08-19T1723Z_registry-split-01.md:327-328   the passage
           reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:185
                                                                the Reviewer
                                                                quoting it
         located file      specs/2026-08-19T1723Z_registry-split-01.md
         §0a's locator     specs/2026-08-19T1723Z_registry-split-01.md
         MATCH.

    W-2  "as a execution-layer disposition"
         1 hit repository-wide:
           specs/2026-08-19T1141Z_integrate-recon-ext-01.md:286-287
         located file      specs/2026-08-19T1141Z_integrate-recon-ext-01.md
         §0a's locator     specs/2026-08-19T1141Z_integrate-recon-ext-01.md
         MATCH.

**`A2` does not fire.** Both re-issued locators are confirmed by measurement.

**Surrounding text and enclosing section, recorded.**

`W-1`, at `specs/2026-08-19T1723Z_registry-split-01.md:322-328`, under
`## 11. What this task does not establish`:

> A directory layout is not a scientific result. This produces no `β_V`, moves
> no gate, and does not advance `P2-PHASE-01`. What it changes is that a reader
> can tell a definition from a hypothesis from a decision from a specification
> review by where the file sits, and that every one of them carries its review
> with it.

`W-2`, at `specs/2026-08-19T1141Z_integrate-recon-ext-01.md:286-288`, under
`## 7. Acceptance criteria`:

>     C7   `D-1`, `D-2`, `D-3`, `O-1`, `O-2` exist, each identified as a
>          execution-layer disposition or an open finding, with identifiers
>          drawn from the selected register's own sequence.

**The line-break hazard, and why the method mattered.** `W-2`'s string spans
`:286`/`:287` — "a" ends one line, "execution-layer" begins the next. A
single-line search returns two hits, both in documents that *mention* the typo
rather than contain it, and zero hits in any specification. **That false
negative is how the original misattribution survived**, and §9 names it. The
normalised search is what turns it up.

**Why the `W-2` hit count is 1 and not 3.** The two downstream mentions —
`reviews/chatgpt/2026-08-19T1141Z_integrate-recon-ext-01.md:165` and
`reports/2026-08-19T1141Z_integrate-recon-ext-01.md:318` — quote the phrase as
`a execution-layer disposition`, without the leading `as `. §0a's string
includes it, so they do not match. Recorded because a reader comparing counts
against the predecessor's report will otherwise think a hit went missing.

### `M4` — which register admits which item (MEASURED)

Three registers exist. Their stated scopes, read from their own text:

    derivations/P2-DEFERRED-ITEMS.md
        Science-side. ":1-6" — a P2-PHASE-01 register that "records decisions,
        computes nothing, registers no gate and changes no status"; its own
        text says entries are added by PI decision. Admits none of the three.

    derivations/P2-PHASE-01_C-CHECK_OPEN-ITEMS.md
        Science-side. ":3-5" — "a register of open items arising from the
        C-check line", the follow-up checks C1, C2 and C3. Admits none of the
        three.

    docs/GOVERNANCE-DEBT.md
        The governance-side register. Its ":16-19" records that the two above
        "already existed at the evidence base and both are science-side" and
        that "There was no governance-side register."

**`docs/GOVERNANCE-DEBT.md` admits all three.** `M4` allowed them to differ;
measured, they do not. Its declared dispositions at `:21-32` are REPAIRABLE,
SPECIFIABLE, NOT REPAIRABLE HERE, RULED, METHOD NOTE, OPEN, and **"Each entry
carries exactly one."** Entries ran `G-01`…`G-12`; the next in its own sequence
is `G-13`.

**Disposition assignment is bookkeeping, and it was the executor's, so it is
stated as such.** The specification names no disposition for any of the three.
Assigned:

    G-13  ruling-2 finding      OPEN
          §5.2's own heading reads "protection model, OPEN". Not a judgement.
    G-14  ruling-4 obligation   RULED
          "a PI ruling settled it" — the question whether the three are
          reviewed IS settled. The work is not done, and the entry says so at
          status REVIEW PENDING. RULED describes the question, not the work.
          `REVIEW PENDING` is not one of the register's six dispositions, so
          it is carried inside the entry rather than as its disposition.
    G-15  §7 errata            REPAIRABLE
          "a repair is known and blocked only by scope" — the corrected
          wording is known and applying it is blocked by G-13.

**A Researcher who prefers different dispositions can change all three without
touching any ruled text**, which is why they are separated from it here.

### `M5` — retrospective-review artifacts (MEASURED)

**None supplied.** Two artifacts were supplied to this task: the specification
and its pre-execution review. Neither is, nor contains, a retrospective review
of a `reviews/pi/` record.

Per §5.3 the **"not supplied"** branch applies, and per the specification
absence "is an expected outcome, not a fault". The obligation is registered as
`G-14` at status `REVIEW PENDING`, naming all three records by path and blob
id. **No review content was authored.**

The same measurement governs the §6.4 decision file's Part 2: no review of
that decision was supplied, so Part 2 reads `REVIEW PENDING`. The
pre-execution review of this specification is explicitly recorded there as
**not** being that review.

### `M6` — SHA-256 of each located file, and what binds it (MEASURED)

    specs/2026-08-19T1723Z_registry-split-01.md
      sha256  6a90c815d4e1912c431e827e0887eb723f2d6bde507782e6245238ad94a49bad
      bound   reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:4
                  Reviewed specification SHA-256
              reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:195
              reports/2026-08-19T1723Z_registry-split-01.md:30, :32

    specs/2026-08-19T1141Z_integrate-recon-ext-01.md
      sha256  ac0ec0b59e96094bf285d7d1c2e2ebe0528ab425ceb46699c783ee3c0cda4697
      bound   reviews/chatgpt/2026-08-19T1141Z_integrate-recon-ext-01.md:4
                  Reviewed specification SHA-256
              reviews/chatgpt/2026-08-19T1141Z_assumption-review_a-ext-01_h-ext-01.md:21, :146
              assumptions/H-EXT-01.md:106
              reports/2026-08-19T1141Z_integrate-recon-ext-01.md:34, :36

**Stronger than the predecessor found.** The second file is named by digest in
**three distinct documents besides its own review**, one of them
`assumptions/H-EXT-01.md` — a landed assumption record, and a *second* review
artifact. A change to its bytes would leave three separate documents naming a
digest the file no longer has. This is the evidence §5.2 records beneath the
ruled question and §7.2 reason 2 relies on.

### `M7` — validators, both sides (MEASURED)

    merge product 55b28783   332 passed, 2 deselected   exit 0
    base 4257e2a2            332 passed, 2 deselected   exit 0

Run in real git worktrees, not extracted trees. Identical. **`A3` does not
fire.** Rule 14's contract is satisfied on both: process started, completed
without timeout, exit status 0, no phase skipped or aborted; the 2 deselections
are the standing configured deselection present at the base.

**`ruff check .` returns 13 errors on the merge product and the SAME 13 at the
base**, all in `scripts/euclidean_reconstruction.py`, a file this task did not
touch (`I001`, `E702`, `E501`). Pre-existing and unchanged; recorded because a
reader running it will meet them.

### `M8` — `git diff --name-only 4257e2a2..55b28783` (MEASURED)

    M  CONVENTIONS.md                                        P3
    A  decisions/2026-08-19-inconclusive-disposition.md       P2
    M  decisions/README.md                                    P1
    M  docs/GOVERNANCE-DEBT.md                                P4
    A  reviews/chatgpt/2026-08-19T2324Z_gov-housekeep-02.md   P6
    A  specs/2026-08-19T2324Z_gov-housekeep-02.md             P6

Six paths, each mapped to its §1b slot. **Zero paths under `scripts/`.** `A5`
does not fire; `C15` holds.

### `M9` — `A-EXT-01` and `H-EXT-01` statement pins (MEASURED)

    A-EXT-01  ca8e5a870b5c7734321a9b6b97f3844046d8ceb689aece0ca65082b70a522378
              CONVENTIONS.md, present once, unchanged
    H-EXT-01  e5dd8a28eaff7623af23ab11404ef2d43dc8053599807162863cf38aca239a47
              assumptions/H-EXT-01.md, present once, unchanged

`git diff 4257e2a2..55b28783 -- assumptions/H-EXT-01.md` is **empty**, and the
`CONVENTIONS.md` diff touches no `Statement SHA` line. **`C14` holds.**

---

## §3 — Abort determination

    A1  DID NOT FIRE.  origin/main equals the §0 base.
    A2  DID NOT FIRE.  Both §5 rulings applied as given, and M3 located both
                       W- strings at the files §0a names. The condition was
                       retained and exercised even though it was expected to
                       pass, because a condition retained only while it is
                       expected to fail is not a check.
    A3  DID NOT FIRE.  M7 identical on both sides.
    A4  DID NOT FIRE.  The merge was conflict-free; working tree clean after.
    A5  DID NOT FIRE.  M8's six paths are all inside the §1b manifest.
    A6  DID NOT FIRE.  No reviews/pi/ record modified — C5 confirms all three
                       blob ids byte-identical on the merge product. No file
                       under specs/ modified — M8 shows one specs/ path and
                       its status letter is A, not M. No append-only file's
                       history rewritten: DECISION_LOG.md and CONVENTIONS.md
                       are the two files declaring append-only discipline;
                       DECISION_LOG.md is untouched, and CONVENTIONS.md's
                       change is a pure insertion plus one count correction,
                       with zero deleted lines of any rule.

---

## §5, §6 and §7 — what was landed

### §5.1 — ruling 1

`decisions/README.md`'s PROVISIONAL section is replaced by
`## When a PI decision takes effect`, carrying the rule verbatim as a **PI
RULING, adopted verbatim** block.

**Provenance is preserved, not overwritten.** A `### Provenance` subsection
records that the disposition was first landed as PROVISIONAL by
`P2-REGISTRY-SPLIT-01`, that the Researcher lacked authority to fix it, that
the PI adjudicated it, and that the rule is that adjudication. **Both
superseded blocks are reproduced verbatim and marked NON-OPERATIVE** — the
PROVISIONAL disposition and the PENDING PI ADJUDICATION block — followed by
the sentence that the adjudication recorded as pending is no longer pending.

**The rule's first operation is recorded with it**: the Reviewer recommended
against ruling 4's retrospective reviews, that recommendation does not suspend
ruling 4, and ruling 4 proceeds — "recorded as the rule's first operation, not
as a dispute".

### §5.2 — ruling 2, `G-13`

The ruled question is registered **verbatim**, as a quoted PI RULING block,
disposition OPEN.

`M6`'s evidence is recorded **beneath** it, under the heading "Evidence added,
without altering the ruled text", stating that the question arises for a second
class of record the ruling does not name, and that **whether the answer is the
same for both classes is part of what is open.** The ruled text is not
rewritten.

**`C4` — no protection model is asserted.** Verified by reading, with
exclusions stated: a search for `append-only`, `append only`, `may be edited`,
`may not be edited`, `must not be edited`, `editable`, `edited in place` over
**every line this task ADDED** — the `+` lines of `git diff
4257e2a2..55b28783 -- CONVENTIONS.md decisions/ docs/GOVERNANCE-DEBT.md` —
returns **exactly one** hit, "question is whether a reviewed definitional
statement may be edited in", which is inside the ruled question itself and is
the occurrence §9 names as legitimate. **Scoping to added lines is the point:**
`CONVENTIONS.md` carries five pre-existing `append-only` occurrences, none of
them this task's and none of them touched. Nothing landed says any file is or
is not append-only, or that a bound record may or may not be edited.

### §5.3 — ruling 4, `G-14`

Discharged in `M5`'s **not-supplied** branch. `G-14` names all three records by
path and blob id at status `REVIEW PENDING`, carries the required label
verbatim, and records that a retrospective review is landed beside a record,
never into it. **No review content was authored** — `C6`.

### §6 — the mechanism

Landed as **`CONVENTIONS.md` rule 22**, `### 22. Disposition of INCONCLUSIVE
results`, inserted before the `C-a` consolidation record. It carries: the PI
ruling verbatim; both subclass names with their definitions; "Assigning a
subclass is required, not optional"; the `Resolution path` field with §6.3a's
question verbatim; "The executor defines the path. The executor does not walk
it."; the symmetry paragraph ending "A `Resolution path` naming only the
confirming outcome is incomplete."; §6.3b's workflow including "Its outcome is
not predetermined"; §6.2's unchanged prohibition; and §6.5's prospective
limitation with "how many existing records carry a constructive gap is
unmeasured and is not estimated."

**Marker: `RULE + MECHANISM DEFERRED`**, with the reason stated in the rule —
it imposes two obligations a machine could test and **no such check exists**.
Chosen over `RULE-ONLY` on that ground: rule 21 is RULE-ONLY because there is
nothing there to check, which is not this rule's situation.

**The `C-a` census at `CONVENTIONS.md` was NOT updated, deliberately.** Its
marker counts are scoped to "the four amendments and three rules adopted on
2026-08-14" over "Thirteen source items". Rule 22 is a 2026-08-19 PI ruling
outside that closed set. **Adding it to that census would misreport what the
census counts.** Recorded because a reader who sees six deferred markers and a
census reading five will otherwise think the census stale.

The PI decision record is `decisions/2026-08-19-inconclusive-disposition.md`,
Part 1 / Part 2 per `decisions/README.md`. Part 1 carries what was decided,
who and when, effect and scope, the provenance that the Researcher's proposed
form was superseded by the PI's, what the decision does not change, and the
motivating evidence. **Part 2 is `REVIEW PENDING`.**

### §7 — ruling 7, `G-15`, and the departure

**No file under `specs/` was modified.** Both corrections are recorded as one
errata entry carrying `M3`'s **measured** locations — not §0a's quoted ones,
per §10 — the exact current text, the correction, and the statement that the
landed bytes are deliberately unmodified. For `W-1` it records that the
narrowing was already on record beside the passage at
`reviews/chatgpt/2026-08-19T1723Z_registry-split-01.md:185`.

**The cost §7.2 states honestly is recorded in the entry itself**, together
with the observation that it is `G-03` exactly — corrections are not
discoverable from what they correct — and that this entry does not escape that.

**On the departure from the Reviewer's recommendation.** §7.1 records that the
Reviewer had recommended an in-place edit for `W-2` with a new maintenance
review binding. The specification departs from it, flags the departure for
review on its merits, and the Reviewer's review of *this* specification passed
it at §3 and §4. The executor implemented the specification; the departure was
adjudicated before execution, not during it.

---

## §8 — Acceptance criteria

    C1   SATISFIED   decisions/README.md carries the §5.1 rule verbatim, as a
                     quoted PI RULING block.
    C2   SATISFIED   Both superseded blocks present verbatim, marked
                     NON-OPERATIVE, under a Provenance heading naming
                     P2-REGISTRY-SPLIT-01 and the Researcher's lack of
                     authority.
    C3   SATISFIED   The first-operation note is present under its own
                     subheading.
    C4   SATISFIED   Ruled text verbatim; M6 evidence beneath it; question not
                     rewritten; no protection model asserted — measured for
                     absence with exclusions stated above.
    C5   SATISFIED   All three reviews/pi/ blob ids byte-identical on the
                     merge product: 930748ed…, dad02415…, 1e9be1f8….
    C6   SATISFIED   §5.3's not-supplied branch, matching M5. No review
                     content authored anywhere.
    C7   SATISFIED   Rule 22 carries §6.3 verbatim with both subclass names
                     and Resolution path as a required field.
    C8   SATISFIED   Symmetry statement and workflow landed, including "Its
                     outcome is not predetermined".
    C9   SATISFIED   §6.2's unchanged prohibition and "defines the path, does
                     not walk it" both landed with the mechanism.
    C10  SATISFIED   decisions/2026-08-19-inconclusive-disposition.md, Part 1
                     complete, Part 2 REVIEW PENDING.
    C11  SATISFIED   Prospective limitation landed; the audit's outcome is
                     stated to be unmeasured and is not estimated.
    C12  SATISFIED   G-15 carries M3's measured locations for both and the
                     unmodified-bytes statement.
    C13  SEE BELOW   Measured literally: ONE path under specs/ appears in M8.
    C14  SATISFIED   Both Statement SHAs unchanged; H-EXT-01 diff empty.
    C15  SATISFIED   All six M8 paths map to §1b slots P1–P4 and P6.
    C16  INTENDED    Measured in the post-report layer.

### `C13`, measured literally and reported as measured

**`C13` says "Zero files under `specs/` appear in `M8`." Measured, one does:
`specs/2026-08-19T2324Z_gov-housekeep-02.md`, this task's own specification,
with diff status `A`.**

**`C13` as literally written cannot be satisfied by any task that lands its own
specification**, and §1b `P6` authorises exactly that. The criterion and the
manifest disagree.

**The intended reading is satisfied, and it is the one §7.3 and `A6` state.**
Both say *modified*: §7.3 "No `specs/` file is modified"; `A6` fires if applying
an item "would require modifying … any file under `specs/`".

    specs/ paths in M8 with status M or D   0
    specs/ paths in M8 with status A        1   (this task's own spec)

**Zero existing specifications were modified.** Reported this way rather than
marked SATISFIED because the criterion's own words were not met, and a
criterion is not discharged by an executor deciding what it meant to say. **The
wording is returned to the Researcher as `S-1`.**

---

## §9 — Substring hazards, and how each was handled

    review          Named as matching reviews/, Review SHA, the Statement
                    SHA neighbour field, pre-execution review, retrospective
                    review, assumption review, maintenance review. M5 turns
                    on whether a RETROSPECTIVE review was supplied; the two
                    supplied artifacts were distinguished by reading each
                    one's own header, not by matching the word. The decision
                    file records explicitly that this task's pre-execution
                    review is NOT the review its Part 2 owes.
    provisional     Decisive for M1, and it is why the count came out five
                    rather than the predecessor's four. Read, never counted.
    append-only     C4 turns on its ABSENCE as an assertion while the phrase
                    legitimately appears inside the ruling-2 question. The
                    check was performed by reading, with its one permitted
                    hit named by file and line above.
    a execution     Decisive for W-2. The string spans a line break; a
                    single-line search returns a false negative, which is how
                    the original misattribution survived. Handled by
                    normalising whitespace with a line map retained.
    errata          Named as not matching "error" or "erratum" consistently.
                    G-15 is titled "errata" and located by heading, not by
                    substring; no count over the word was used anywhere.

**Rule.** "A check that cannot state its exclusions is performed by reading."
Every negative criterion above states its exclusions.

---

## §1a — Non-objectives, all honoured

    1  author any review
       NOT DONE. M5's not-supplied branch; G-14 registers the obligation and
       contains no review content. The decision file's Part 2 is REVIEW
       PENDING, not a review.
    2  settle the protection model for review-bound landed records
       NOT DONE. G-13 is OPEN. C4's absence check is recorded above with its
       exclusions. §7 is written so as not to pre-empt it, and G-15 says so.
    3  integrate science/gov-housekeep-01, science/recon-proj-01, or
       science/recon-01b-b0-scope
       NOT DONE. None of the three is an ancestor of the merge product; none
       of the three refs was written. Verified in the post-report layer.
    4  alter any landed exact statement, or any landed specification's bytes
       NOT DONE. M9: both Statement SHAs unchanged. M8: zero specs/ paths
       with status M or D.
    5  perform the retrospective audit
       NOT DONE. §6.5's prospective limitation is landed; §13's item 1 names
       the audit as a separate task and no estimate of its outcome appears.
    6  modify any file under scripts/
       NOT DONE. M8 lists zero paths under scripts/.

---

## Stops and clarifications

### `S-1` — `SPECIFICATION_DEFECT`, non-blocking: `C13` contradicts `§1b P6`

`C13` requires zero `specs/` paths in `M8`; `§1b P6` authorises this task's own
specification, which lives under `specs/`. No task can satisfy both.

**Not an abort.** `C13` is an acceptance criterion, not an abort condition, and
`A6` — which is the abort — says *modifying*, which measured zero. Execution
was not blocked and no ruling was unapplicable.

**Suggested wording, for the Researcher and not applied here:** "Zero files
under `specs/` appear in `M8` with status `M` or `D`." Recorded as a suggestion
because rewording an acceptance criterion is the Researcher's act.

### `S-2` — a landed clause contradicted ruling 4, and was marked rather than deleted

**Measured, and not anticipated by the specification.**
`decisions/README.md`'s "Historical PI records" paragraph, at `:81-85` on the
merge product with the clause at `:83`, says the three `reviews/pi/` records
are "not moved, not rewritten, and **not retrospectively reviewed**". Ruling 4 requires each to receive a retrospective
review. Landing ruling 4 while leaving that clause unmarked would have produced
a file contradicting itself in adjacent sections.

**What was done, and why it is the minimum.** The paragraph is left **verbatim**
and a subsection beneath it records that the clause "not retrospectively
reviewed" is no longer operative, that the rest of the paragraph stands, and
that the ruling took effect when issued under the rule at the head of the file.
Nothing was deleted. **This is the same treatment §5.1 mandates for the
PROVISIONAL text** — reproduce, mark non-operative, attribute — applied to a
clause §5.1 did not name, because ruling 4 superseded it and the executor met
the consequence.

**Returned to the PI.** If the intent was that the clause survive — that the
three are reviewed *and* the paragraph continue to say otherwise — that is not
a reading the executor can construct, and the marking should be reverted by a
task that says so. Recorded as a finding rather than left silent.

### `S-3` — `M6` found a broader binding surface than either specification states

§5.2 and §7.2 both reason from `specs/` files carrying review bindings.
Measured, `specs/2026-08-19T1141Z_integrate-recon-ext-01.md` is named by digest
in **four** documents: its own review, a *second* review artifact (the
assumption review), a landed assumption record `assumptions/H-EXT-01.md:106`,
and its own execution report.

**This strengthens §7.2's reason 2 rather than changing it**, and it is
recorded in `G-13` as measured. It also means the class of records affected by
`G-13` is wider than "definitional records plus specifications": an assumption
record can carry a digest of a specification. **No conclusion is drawn from
that here** — `G-13` is OPEN and this is evidence for it, not an answer to it.

### `S-4` — rule 22 and rule 17 interact, and §13 item 3 sits on the seam

`CONVENTIONS.md` rule 17 says an integration "MUST NOT add a governance or
epistemic classification the reviewed results did not carry". §13's item 3
proposes that `P2-RECON-PROJ-01`'s subclass and `Resolution path` "can be added
at integration without amending a landed record".

**Those two are in tension, and §13 items are named and not performed here**, so
nothing turned on it in this task. Recorded so that whoever writes that
integration specification meets the question before the executor does. **No
resolution is proposed**: whether assigning a subclass to a result at
integration is "adding a classification it did not carry", or is the completion
of a field the result was always required to carry once rule 22 exists, is a
governance question and not the executor's.

### `M-obs` — the Reviewer's §19 non-blocking observation, answered

The Reviewer observed that the unintegrated `P2-RECON-01B-B0` result is
"primarily workflow carry-forward" and not the same failure mode as an
epistemically inconclusive result with a constructive gap, with `PROJ-01` the
cleaner example.

**The observation is recorded in the landed decision file**, which states in
its own words that the two pieces of evidence are not the same failure, that
the re-measurement is a workflow carry-forward while `Q1` is the epistemic
case, and that **only the second is what the subclasses distinguish.** The
Reviewer's point is landed, not merely noted.

### `M-obs2` — a pre-existing count was stale and is now corrected

`CONVENTIONS.md`'s closing line described `docs/GOVERNANCE-DEBT.md` as "an
eleven-entry record". **It was already wrong at the base: the register held
twelve.** This task adds three, making fifteen, and the line now reads
"a fifteen-entry record".

Recorded rather than left silent because it is a change to `CONVENTIONS.md`
that no ruling directed. **It asserts no rule and settles nothing** — it is a
count of another file, made false in part by this task's own additions. The
register's own disposition table and its two "twelve entries" sentences were
updated on the same ground, the latter to "the first twelve entries", which
keeps the original claim true rather than restating it.

---

## Layering

This report is measured at the merge product `55b28783296f232173ff4e6ce5e65dea070aafd1`,
which is commit N−1. `C16`, the ref-push scope, is INTENDED at the time of
writing and is measured in the post-report layer; that evidence is returned to
the Reviewer in chat and is not written back into this file.
