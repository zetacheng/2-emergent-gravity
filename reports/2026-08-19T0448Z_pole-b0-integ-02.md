# Execution report — `P2-POLE-B0-INTEG-02`

    OUTCOME     MERGED AND LANDED. The A4 condition that aborted INTEG-01
                passed against the amended policy. No abort fired.

**Specification:** `specs/2026-08-19T0448Z_pole-b0-integ-02.md`
**Review:** `reviews/chatgpt/2026-08-19T0448Z_pole-b0-integ-02.md`
**Landing record:** `reports/2026-08-19T0448Z_pole-b0-integ-02_landing-record.md`
**Base:** `4b30e7855a657674aecf46d4cc76d68244f385a9`
**Source:** `science/pole-b0-milestone-scope @ 3f78acfebd7e994460eb30ac54ffd31a5cf3c21f`

**Measurement head for everything below the landing section: commit 5,
`b300c43f0cd2593ba5f9c508a8ff01158e347960`.** Figures depending on commit 6 or
on the push are labelled **INTENDED** or returned in the post-report layer.

---

## §0 — Binding SHAs (MEASURED, no A1 abort)

    origin URL, verbatim   https://github.com/zetacheng/2-emergent-gravity
    observed origin/main   4b30e7855a657674aecf46d4cc76d68244f385a9
    §0 declared base       4b30e7855a657674aecf46d4cc76d68244f385a9
    observed source tip    3f78acfebd7e994460eb30ac54ffd31a5cf3c21f
    §0 declared source     3f78acfebd7e994460eb30ac54ffd31a5cf3c21f

**Both match. Neither has advanced.**

**Review binding (Rule 18, Amendment N).** Field
`Reviewed specification SHA-256:` present, once, populated — checked before
its value.

    sha256 of the specification bytes as committed
      49afad6a62671fda0c8cb2c5ff81428c016e8ee482dc7314e02208fc16b6139e
    the review's bound SHA
      49afad6a62671fda0c8cb2c5ff81428c016e8ee482dc7314e02208fc16b6139e

**MATCH.** Verdict `APPROVE FOR EXECUTION`.

---

## Environment (MEASURED)

**Amendment D step 0.** Ref reads in `/home/user/2-emergent-gravity`; all
commits in the linked worktree `…/scratchpad/ipole2`, created at the base.
Resolved HEAD `4b30e785…` at creation.

**Rule 13's diagnostic order.** Not shallow; 423 commits on HEAD, 566 across
all refs; Python 3.11.15; pytest 9.1.1; numpy 2.4.6; sympy 1.14.0; ruff
0.15.8; **`scipy` ABSENT.** **No environment failure occurred, so neither of
Rule 13's two diagnostic orders was exercised.** §1a.6 and §12.3 place the
`execution_environment.md` clarification outside this task; it was not
actioned.

---

## §2 — Pre-execution measurements

### M6 — the clause whose absence aborted INTEG-01

**Located on the base at `docs/BRANCHING_POLICY.md:25-40`, under the heading
`## Science branch integration`, with a `### Retroactive note` subsection at
`:42-47`. Read, not searched** — §8 names `science` as matching `scientific`
and `scientific-task`, so a file-wide count would have been uninformative.

    :27  `science/*` is a recognized scientific-task branch class.
    :29  Approved science branches integrate by `--no-ff` into a dedicated
    :30  integration branch.
    :32  Squash/rebase integration prohibited.
    :34  During landing, only the integration branch and `refs/heads/main` may be
    :35  pushed.
    :37  Source branch, session branches and unrelated refs must not move.
    :39  `main` advances only by fast-forward from its reviewed evidence base to the
    :40  completed integration head.

**As read, the clause states:**

    merge mode           --no-ff into a dedicated integration branch;
                         squash and rebase integration prohibited
    allowed-ref scope    the integration branch and refs/heads/main only;
                         source branch, session branches and unrelated refs
                         must not move
    main-advance rule    fast-forward only, from the reviewed evidence base
                         to the completed integration head

**This matches §4's stated shape in every particular. A4 does not fire.**

**The taxonomy also carries the class:** `science/<scientific-task>` at
`:11` of the branch-name block. `INTEG-01` aborted because both were absent;
both are present now.

### M1 — Test suite

    invocation   python3 -m pytest -q

    merge product 4102344b   332 passed, 2 deselected in 42.03s, exit 0
    base 4b30e785            332 passed, 2 deselected in 41.99s, exit 0

**Identical. No A3 abort.**

**Both runs were taken in real git worktrees**, confirmed by
`git rev-parse --is-inside-work-tree` returning `true` before the run. **This
is what M1's own text requires and why:** the `INTEG-01` execution first
measured its suite in a `git archive` extraction, which is not a git
repository, and six git-history-dependent governance tests failed as a result.
Nothing here was measured outside a worktree.

### M2 — `P2-*` artifacts on the merge product

    command   git ls-tree -r --name-only HEAD derivations/ | grep -c '/P2-'
    observed  52

    base 4b30e785, same command   51

**52, one more than the base.** The addition is
`derivations/P2-POLE-B0_milestone-scope.md`, the arriving artifact.

### M3 — Ancestry before the merge

    git merge-base --is-ancestor 3f78acfe refs/remotes/origin/main
      exit 1

**Not an ancestor. The branch was unlanded**, which is the expected
pre-integration state and confirms the merge is real work.

### M4 — Dry-run merge

    git merge-tree --write-tree <base+spec+review> 3f78acfe
      exit 0
      tree 05ec8acfce616e97d8894ce2f3a145024ce4586b
      conflict lines 0

**Conflict-free. No A2 abort.**

### M5 — `git diff --name-only 4b30e785..<head>`

Measured at commit 5, after every write this task performs:

    DECISION_LOG.md
    derivations/P2-POLE-B0_milestone-scope.md
    reports/2026-08-19T0223Z_pole-b0-milestone-scope.md
    reports/2026-08-19T0448Z_pole-b0-integ-02_landing-record.md
    reviews/chatgpt/2026-08-19T0223Z_pole-b0-milestone-scope.md
    reviews/chatgpt/2026-08-19T0448Z_pole-b0-integ-02.md
    specs/2026-08-19T0223Z_pole-b0-milestone-scope.md
    specs/2026-08-19T0448Z_pole-b0-integ-02.md

    count 8

**All eight are inside the §1b manifest. No A6 abort. C10 passes.**

    P1  DECISION_LOG.md                     the register selected under M7
    P2  the 0448Z spec, review and landing record; this report joins them
    P3  the four 0223Z / P2-POLE-B0 paths arriving by the merge

**§1b's note is borne out:** `P2` is present because Rule 15 commits the spec
and review before execution, and `P3` is present because it is the merge this
task exists to perform. An abort condition omitting either would have fired on
correct execution.

### M7 — Register scopes, read

**Six candidates were read. Five do not admit an item of §6 `R-1`'s class, and
each says so in its own words.**

    derivations/P2-DEFERRED-ITEMS.md
      :12-17  "holds work that has been CONSIDERED and consciously
               postponed… not a list of open questions or of things not yet
               thought about"
      :191    "Entries are added by PI decision."
      -> DOES NOT ADMIT. §6 rules this out directly, and the register's own
         scope agrees: POLE-B0's finding is that the construction is not yet
         scoped, which is the other side of that distinction.

    derivations/P2-PHASE-01_C-CHECK_OPEN-ITEMS.md
      :3-5    "a register of open items arising from the C-check line — the
               follow-up checks C1, C2 and C3 commissioned by
               derivations/P2-PHASE-01_microscopic_parameter_domain.md"
      -> DOES NOT ADMIT. POLE-B0 is not a C-check item.

    derivations/P2-PHASE-01_input_admissibility_contract.md   (OPEN-AC-1..5)
      the phase input / admissibility standard for P2-PHASE-01
      -> DOES NOT ADMIT. §6 distinguishes R-1 from an open
         acceptance-criterion item, which is exactly this register's class.

    derivations/P2-PHASE-01_microscopic_parameter_domain.md   (OPEN-PD-1..5)
      the microscopic parameter domain for P2-PHASE-01
      -> DOES NOT ADMIT. R-1 is not a parameter-domain item.

    docs/GOVERNANCE-DEBT.md                                   (G-01..G-12)
      :3-6    "It records what the rules, amendments and task reports already
               carry, and it creates, modifies and explains no obligation."
      -> DOES NOT ADMIT. Governance debt is a gap in rules and mechanisms;
         R-1 is a scientific construction question.

    DECISION_LOG.md
      :3-4    "This log is append-only. New decisions must use the entry
               template below"
      -> ADMITS. P2-DEFERRED-ITEMS.md:19-22 names it as one of the three
         places open questions live: "DECISION_LOG.md entries that open an
         item as UNESTABLISHED". The precedent is :1326, "Open derivation
         item: generator-sum criticality is UNESTABLISHED", carrying
         "Status: UNESTABLISHED."

**`DECISION_LOG.md` is the register whose stated scope admits the item**, and
it was selected by reading the scopes rather than by preference. **No register
semantics were coined.**

---

## §5 — The landing record

Committed as commit 4, `reports/2026-08-19T0448Z_pole-b0-integ-02_landing-record.md`,
234 lines. Its content is not restated here; the criteria below record what was
verified in it and how.

---

## §6 — The register entry

**Appended to `DECISION_LOG.md` as commit 5**, in the register's own dated
form and following the `:1326` precedent's structure exactly:

    ## 2026-08-19 — Open construction item: the POLE milestone's construction
       layer is UNESTABLISHED

with `Date`, `Decision owner`, `Effect`, and the sections `Decision`,
`Reason`, `Consequences`, `Related gate`, `Related branch and files`.

**The identifier is drawn from the register's own sequence.** `DECISION_LOG.md`
carries no `ID-NN` scheme; its entries are dated headings, and the new entry is
the thirty-fourth in that sequence. **`R-1` is the specification's label for
the item, not a register ID**, and §6 says so.

**Status token: `UNESTABLISHED`**, which is the register's own vocabulary for
an item opened and not settled — used at `:1343` for the one prior open item.
**It expresses "raised, not scoped" in the register's words rather than in
mine.**

**The entry carries §6's required content**: it is placed on the OPEN side with
the reason stated (deferred means examined and postponed; this was not), the
class is named as a programme-design / construction question rather than an
acceptance-criterion item, the roadmap `POLE-B0 scope → POLE construction /
specification layer → executable pole test` appears verbatim, and the note that
**clearing the rulings is necessary and not sufficient** is stated twice — once
in the quoted item and once, without markup, in `Consequences`.

**Append-only, verified on both measures:**

    base bytes                     89541
    head bytes                     93754
    base is byte prefix of head    True
    deleted lines base to head     0

**No R-2 entry was created.** `INTEG-01`'s proposed `scipy` debt entry was
conditional on a measurement that did not reproduce the contradiction. **The
landing record notes that it was pre-registered, was conditional, and did not
fire.**

---

## §7 — Acceptance criteria

    C1   INTENDED — post-landing; measured value in the post-report layer.
    C2   PASS   The landing record carries `## LAYER 1 — RULING BLOCKERS`
                at :62 and `## LAYER 2 — CONSTRUCTION BLOCKERS` at :91, as
                two separately headed sections, and the sentence
                `Ruling R1–R4 in full would not clear Layer 2` as a heading
                at :105 with four paragraphs establishing it. Verified by
                reading both headings and that section.
    C3   PASS   The accounting matches the branch report at 3f78acfe. The
                frozen text reads `N_total = N_both + N_impl + N_spec +
                N_neither` / `12 = 0 + 3 + 3 + 6`; the record carries
                `12 = 0 both + 3 impl + 3 spec + 6 neither` and quotes the
                frozen form beneath it. Numerals and category labels agree.
                No A5 abort.
    C4   PASS   Both dependency-transfer statements are present and separate:
                the POLE milestone's UNCONDITIONAL inheritance of R4 through
                the expectation-value bracket, and SRC-B0's CONDITIONAL
                inheritance through the choice of Γ-defined or S-defined
                stress tensor. They are two block quotations under two
                labels, not one claim.
    C5   PASS   All three delimitations present: Barnes–Rivers projectors are
                channel decomposition and not correlator, pole extraction or
                residue analysis; tt_check.py computes four channels and the
                caller retains the first; RECON-01a is a Γ⁽²⁾-class response
                and is not to be renamed an interacting stress-tensor
                correlator.
    C6   PASS   The §5.8 correction is present. It states the location as
                :42-43 rather than :43-44; states that the branch content at
                3f78acfe is unchanged by it; states that the verdict is
                unaffected, with the reason — the scipy remark sits outside
                the twelve-component accounting and outside every dependency
                finding; and records the pyproject.toml ↔
                execution_environment.md relationship as OPEN while stating
                explicitly that no inconsistency is asserted. Verified by
                reading.
    C7   PASS   M1-M7 recorded above with observed values and the commands
                or reads used.
    C8   PASS   The entry exists in DECISION_LOG.md, the register selected
                under M7, carrying an identifier from that register's own
                sequence — a dated heading, the form all 34 entries use.
    C9   PASS   Verified by reading every occurrence. `R1`–`R4` appears at
                three places in the landing record: :20, the PARTIAL verdict
                line, which is explicitly marked as applying to part of the
                scope only and NOT the primary verdict; :64, Layer 1's own
                definition, which §5.1 requires; and :105, the heading of the
                section establishing that a ruling would NOT clear Layer 2.
                **None of the three summarises the position as "blocked
                pending R1–R4"**, and the third is that phrase's refutation.
    C10  PASS   M5 lists eight paths and all eight are in the §1b manifest.
    C11  PASS   scripts/recon2026/proca_curved.py blob observed as
                03f46905e5798fb7f6880dfae9ed5a1931be895b at the merge product.
                Recorded as observed; no expected value is asserted here, per
                §9. §1a.5 and §12.2 were honoured — the file was not touched.
    C12  INTENDED — the push is below; measured values in the post-report
                layer.

---

## §4a — Push scope, and the observation the specification returned

**This task pushes the integration branch and `refs/heads/main`. No other
ref.**

**That is a narrower set than the immediately preceding task pushed**, and the
specification's §4a records the divergence as a policy question rather than
normalising it. The `P2-BRANCHING-SCIENCE-01` execution pushed three refs —
`main`, its integration branch, and the task branch carrying its spec and
review. **I was that executor**, and the clause I had just landed names two.
The third was harmless: no rewrite, and the ref was already an ancestor of the
integration branch, so the policy's `:29` recoverability condition was met by
the merge parent regardless.

**This execution has no separate task branch at all.** §4's commit order puts
all six commits on the integration branch, so conforming strictly to the clause
costs nothing here and **no required artifact became unreachable** — the
condition §4a names as an `A4` return did not arise.

**Whether the clause should name the task branch is not decided here**, and
this task takes no position beyond conforming to the text as landed.

---

## §1a — Non-objectives, all honoured

The POLE-B0 verdict was **not** re-adjudicated and POLE-B0 was **not** re-run;
§5.2's accounting was transcribed and A5 checked it against the frozen text.
`R1`–`R4` were **not** ruled — all four remain `DEPENDENCE ESTABLISHED` against
nodes carrying `STATUS OPEN`. `docs/BRANCHING_POLICY.md` and `CONVENTIONS.md`
were **not** amended; neither appears in M5. **No file on
`science/pole-b0-milestone-scope` was edited** — the branch was merged as it
stands at `3f78acfe`, its tip is preserved as parent 2, and §5.8's correction
is recorded at landing rather than written back. `proca_curved.py` was **not**
touched and C11 records its unchanged blob. The
`docs/local/execution_environment.md` clarification was **not** actioned. **The
POLE construction layer was not scoped, designed or begun** — the register
entry opens it and defines nothing.

---

## Merge topology

    commit 3   4102344bc405eb99c8786a2a8bacfd6e337bd4ab   the --no-ff merge
      parent 1 0a59ee475d5929e88c4f5e5acc99b998cedd4cd7   the review commit
      parent 2 3f78acfebd7e994460eb30ac54ffd31a5cf3c21f   the source tip

    merge-base 11af14a792c5858b368180d99ab9ee4692a7f698

**The merge-base is not the evidence base**, and the difference is this task's
own history: the source branched from `11af14a7`, and `P2-BRANCHING-SCIENCE-01`
then advanced `main` to `4b30e785`. **The commit that made this task possible
is the commit that separated its base from its merge-base.** Nothing between
the two touched an arriving path — M5's eight entries are all additions except
`DECISION_LOG.md`, which this task appended to — and M4 found no conflict.

**The source tip is preserved as parent 2**, which is what the clause requires
and what makes the branch's tip recoverable without a separate ref.

---

## Stops and clarifications

**`SPECIFICATION_DEFECT`** — **none.** Two observations on the positive side.
`A4` was retained unchanged from `INTEG-01` even though it was expected to
pass, on the stated ground that *"a condition retained only while it is
expected to fail is not a check"* — and it was exercised as a real read of the
landed clause, not assumed. And §1b's note that `P2` and `P3` are necessarily
in the diff meant `A6` did not fire on correct execution, which is the failure
mode the manifest was rewritten to avoid.

**`ENVIRONMENT`** — `scipy` absent for the sixteenth consecutive task; no
validator imports it and nothing failed. **The `scipy` question is not filed
here as an environment defect**, because §5.8's measurement establishes that
`proca_curved.py:42-43` is true against the document that declares this
environment's packages. The `pyproject.toml` ↔ `execution_environment.md`
relationship is recorded **OPEN** in the landing record and is §12.3's separate
task. **No environment failure occurred, so neither of Rule 13's two diagnostic
orders was exercised.**

**`OBSERVATION_METHOD_ERROR`** — **none in this task, and one corrected from a
previous one.** M1 was measured in real git worktrees on both sides, which is
the correction `INTEG-01` had to make mid-execution after six
git-history-dependent tests failed in a non-git extraction. §8's four named
collisions were handled by reading: the `science` clause was located by heading
and read in full rather than counted, since `science` matches `scientific` and
`scientific-task`; `C9` was established by reading all three `R1`–`R4`
occurrences rather than by counting them, which §9 explicitly excludes as
uninformative.

**`REPOSITORY_DEFECT`** — **none newly found.** The defect that aborted
`INTEG-01` — no `science/*` clause in the branching policy — was repaired and
registered as `G-12` by `P2-BRANCHING-SCIENCE-01`, and M6 confirms the repair
holds on this base. **The original `scipy` repository contradiction is recorded
NOT REPRODUCED**, and the conditional entry that depended on it did not fire.

**`UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY`** — **none blocking, two
returned by the specification and left as it left them.** The push-scope
question of §4a is a PI decision and this task conformed to the text as landed
without taking a position. The `pyproject.toml` ↔ `execution_environment.md`
relationship is `OPEN` and belongs to §12.3.

**One correction to my own prior work, already carried in the landing record
and restated once here so it is not only in the record:** the `POLE-B0` report
at `3f78acfe` cites `proca_curved.py:43-44` and calls the `pyproject.toml`
statement a contradiction of it. **The location is `:42-43` and it is not a
contradiction** — the two statements have different subjects. The branch was
merged unedited, so that report still carries the error; **the landing record
is where the correction lives**, and the verdict is unaffected.

---

## Layering

**Everything above is measured at commit 5,
`b300c43f0cd2593ba5f9c508a8ff01158e347960`**, except C1 and C12, labelled
INTENDED.

**Returned to the Reviewer in chat, not written back:** the `--is-ancestor`
verification and its exit status, the push exit statuses, the remote read-back,
C1 and C12 as measured, and confirmation that
`science/pole-b0-milestone-scope` still points at `3f78acfe`.
