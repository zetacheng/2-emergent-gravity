# Execution report — `P2-GAPB-INTEG`

    OUTCOME     MERGED AND LANDED. No abort fired.
                The GAP-B finding is on main as the reviewed result left
                it. R-2's consequence for its reach stays open.

                M4's SUBJECT SET IS NOT EMPTY THIS TIME — one path, and it
                is the one that mattered: DECISION_LOG.md, append-only,
                where main had advanced and the source had not. The merge
                product carries main's blob.

                M10 MEASURED A CLAIM THIS LINE MADE THREE TIMES AND IT IS
                FALSE. All four obligations recorded as having no
                admissible register are admitted by DECISION_LOG.md's
                stated scope. So is a second figure carried beside it.

                THE REVIEWER'S §19 AUTHORITY CHECK FIRES. Three items §13
                describes as "ruled" have no such ruling in authoritative
                repository state. Returned to the PI at S-3.

**Specification:** `specs/2026-08-20T1139Z_gapb-integ.md`
**Review:** `reviews/chatgpt/2026-08-20T1139Z_gapb-integ.md`
**Landing record:** `reports/2026-08-20T1139Z_gapb-integ_landing-record.md`
**Base:** `55deaec9ef49f197307e5e41f712a484473a4964`
**Source:** `science/gapb-bridge-01 @ 46b4791152fd87ced7e718df5ec3d91d394883ac`

**Measurement head: `b3f6111`,** the landing commit. This report is the commit
after it. `C2` and `C15` are INTENDED and measured in the post-report layer.

---

## §0 and `M1` — Binding SHAs, and the abbreviation (MEASURED, no A1 abort)

    git ls-remote origin refs/heads/science/gapb-bridge-01
      -> 46b4791152fd87ced7e718df5ec3d91d394883ac

    §0's abbreviation    46b47911
    M1's full value      46b4791152fd87ced7e718df5ec3d91d394883ac
    begins with it?      YES

    main   observed 55deaec9ef49f197307e5e41f712a484473a4964   declared identical

**`A1` does not fire on either limb**, and no command in this task was given
the abbreviation.

**Review binding (Rule 18, Amendment N).** The
`Reviewed specification SHA-256:` field was checked for PRESENCE before its
value was compared. Present, once, populated, at
`reviews/chatgpt/2026-08-20T1139Z_gapb-integ.md:4`.

    sha256 of the specification bytes as committed
      4d49f63cc35a3035f36edd330351789a55e04c82206f003b26dfeb3b019c102d
    the review's bound SHA
      4d49f63cc35a3035f36edd330351789a55e04c82206f003b26dfeb3b019c102d

**MATCH.** Verdict `APPROVE FOR EXECUTION`, with two notes at the review's §18
and §19, both acted on below.

---

## Environment (MEASURED)

**Amendment D step 0.**

    execution location   …/scratchpad/igapb    integration branch
                         …/scratchpad/base07   detached at 55deaec9
    worktree identity    two real git worktrees
    resolved HEAD        science/integrate-gapb-01, b3f6111
    ref reads            /home/user/2-emergent-gravity

    Python 3.11.15   pytest 9.1.1   numpy 2.4.6   sympy 1.14.0   ruff 0.15.8
    scipy  ABSENT — ModuleNotFoundError: No module named 'scipy'

Thirty-third consecutive task without `scipy`. Nothing here imports it.

**Rule 13.** No environment failure occurred. Neither diagnostic order was
exercised.

---

## §2 — Measurements

### `M2` — validators, both sides (MEASURED)

    merge product aaca40d3   332 passed, 2 deselected   exit 0
    base 55deaec9            332 passed, 2 deselected   exit 0

Real git worktrees. Identical. **`A3` does not fire.**

### `M3` — dry-run merge (MEASURED)

    exit 0, "Automatic merge went well; stopped before committing as requested"
    conflicting paths (--diff-filter=U): 0

**`A2` does not fire.**

### `M4` — the revert hazard: ONE path, and it is the one that mattered (MEASURED)

**Fork point: `f23a0e1e1a24398d082a9597444ff9f750ed38e1`.**

    paths on main 55deaec9   587
    paths on source 46b47911 583
    paths at the fork        579
    present on BOTH at DIFFERING blobs:  1

    path              fork          source        main          product
    DECISION_LOG.md   8155a4ca0695  8155a4ca0695  78919cc4b020  78919cc4b020

**Direction established, not inferred:** source = fork, and main ≠ fork.
**The source did not touch it; `main` advanced.**

**The merge product carries `main`'s blob. `A5` does not fire.**

**What was at stake, and it was not hypothetical.** `main` advanced on that
path **in the immediately preceding landing** — the `R-1`…`R-4` entry
`P2-OBS-IDENT-INTEG` appended. `DECISION_LOG.md` **declares itself append-only
at its `:3-4`.** A merge product carrying the source's blob would have **deleted
four open records from an append-only file**, and — measured — the merge was
conflict-free and both validator runs passed. **Nothing but the blob comparison
distinguishes the two outcomes.**

**How `main` advanced, recorded because `M4` asks for the manner and not only
the fact.** `git diff --name-status <fork>..55deaec9` returns nine paths: eight
additions and **one modification**, `DECISION_LOG.md`. **This is neither of the
two manners the preceding two integrations reported** — not "main did not
advance", and not "advanced purely additively". **It advanced with one
modification, and that is the case the check exists for.**

### `M5` — registers, per record (MEASURED)

**`R-5` is admitted by `DECISION_LOG.md`**, on its stated scope at `:3-4` and
its precedent at `:2346-2360`, quoted in the landing record's §6. Landed as one
entry dated 2026-08-20 using the template.

**`DECISION_LOG.md` was read among the registers**, as `M5` requires — the
requirement exists because the three preceding integrations did not read it.

**Append-only verified on both measures:**

    line measure   git diff --numstat  ->  62 added, 0 deleted
    byte measure   the new file's first 109667 bytes are byte-for-byte the
                   old; 112339 after

**No register was created and none was used by convenience.**

### `M6` — the governing clause (MEASURED)

`docs/BRANCHING_POLICY.md:25-40`. Merge mode `--no-ff` into a dedicated
integration branch, squash/rebase prohibited; "only the integration branch and
`refs/heads/main` may be pushed"; "Source branch, session branches and
unrelated refs must not move"; "`main` advances only by fast-forward".
**No contradiction with §4. `A4` does not fire.**

### `M7` — two measurements, and they differ (MEASURED)

**Base-relative — EIGHT paths:**

    M  DECISION_LOG.md                                     P2
    A  derivations/P2-GAPB-BRIDGE-01_regime-transfer.md     P1
    A  reports/2026-08-20T0408Z_gapb-bridge-01.md           P1
    A  reports/2026-08-20T1139Z_gapb-integ_landing-record.md P3
    A  reviews/chatgpt/2026-08-20T0408Z_gapb-bridge-01.md   P1
    A  reviews/chatgpt/2026-08-20T1139Z_gapb-integ.md       P3
    A  specs/2026-08-20T0408Z_gapb-bridge-01.md             P1
    A  specs/2026-08-20T1139Z_gapb-integ.md                 P3

**Source's own contribution, fork-relative — FOUR paths, all `A`.**

**THEY DIFFER: eight against four.** The four extra are this task's own three
artifacts and the register entry.

**And here the naive comparison would have misled, unlike last time.**
`git diff 55deaec9..46b47911` would report `DECISION_LOG.md` as a difference
and imply the source contributes a change to it. **It does not** — the source
is unchanged from the fork on that path. **The two measurements are kept
separate for exactly this case.**

**`A6` does not fire**: every base-relative path maps to `P1`, `P2` or `P3`.

### `M8` — the reviewed result, transcribed (TRANSCRIBED)

Transcribed in the landing record's §1–§3 and §5:

    outcome          INDETERMINATE — UNDETERMINED BY READING
    subclass         NONE assigned to GAP-B's own outcome. See C5 below.
    mismatches       five, each with its classification AND reasoning:
                     MM-1 species (iv); MM-2 regime (i); MM-3 improvement
                     (iv); MM-4 contact terms (ii); MM-5 covariance state
                     (iv). Plus L-7 met by GAP-A within its momentum
                     condition.
    the measurement  specified in the reviewed result and NOT run: the same
                     ten-component decomposition at two or more spacings at
                     fixed physical m and q, with what would decide each way.
    why not
    MEASUREMENT
    REQUIRED         MM-1 and MM-5 are prior to MM-3 and are not measurement
                     questions; a continuum-limit study of the Proca object
                     would report on the Proca object.
    method finding 1 the species mismatch the pre-registered question did not
                     name.
    method finding 2 "unimproved" and "a = 1" appear nowhere in EXT-01's
                     artifact, report, script or proca_loop.py; both were
                     established by reading the action.

### `M9` — the two Statement SHAs (MEASURED)

    A-EXT-01  ca8e5a870b5c7734321a9b6b97f3844046d8ceb689aece0ca65082b70a522378
    H-EXT-01  e5dd8a28eaff7623af23ab11404ef2d43dc8053599807162863cf38aca239a47

Each present once; `git diff --stat 55deaec9..b3f6111 -- CONVENTIONS.md
assumptions/H-EXT-01.md` is **empty**. **`A7` does not fire.**

---

## §6 and `M10` — the re-measurement

**Enumerated by reading the three landing records that recorded them**, not by
carrying a count.

    O-1   PROJ-01-INTEG landing record :198-203, "REGISTER: none".
          OUTSTANDING.                     ADMITTED by DECISION_LOG.md.
    O-2   PROJ-01-INTEG landing record :205-208, "REGISTER: none".
          DISCHARGED BY EXECUTION.         ADMITTED.
    O-3   PROJ-01-INTEG landing record :210-211, "REGISTER: none".
          DISCHARGED by P2-RECON-01B-B0-INTEG.  ADMITTED.
    R-2   GAPA-INTEG landing record :196, "REGISTER: none — see below".
          RESTATES O-2, per that record's own :198-202.  ADMITTED with O-2.

**All four admitted. None "not admitted"; none "not determinable from the
stated scope".** `DECISION_LOG.md`'s stated scope is quoted in the landing
record's §6, with the 2026-08-19 precedent in which `O-1` and `O-2` — different
items, the same shape — were registered there as open findings by an executor
for an integration.

**The standing claim is false as measured.**

**A second figure, reached by the same reading.** The three records carry a
count beside the claim. **`O-3` was discharged by the very task that restated
the count**, `O-2` is discharged by this landing, and `R-2` restates `O-2`.
**One of the four is outstanding: `O-1`.**

**THIS TASK PLACED NONE OF THEM.** `M10` establishes admission; placement is a
separate act with its own authority question, and §6 says so. `R-5` records the
result and places nothing — the Reviewer's §14 boundary, honoured.

---

## §3 — Abort conditions

    A1  DID NOT FIRE.  Both §0 SHAs equal; M1's full value begins with §0's
                       abbreviation.
    A2  DID NOT FIRE.  Dry-run conflict-free.
    A3  DID NOT FIRE.  M2 identical on both sides.
    A4  DID NOT FIRE.  M6 found the clause; no contradiction with §4.
    A5  DID NOT FIRE.  M4's one differing path carries main's blob on the
                       merge product.
    A6  DID NOT FIRE.  Eight base-relative paths, all in the §1a manifest.
    A7  DID NOT FIRE.  C3, C11 and M9 below.

---

## §8 — Acceptance criteria

    C1   SATISFIED   46b47911… is an ancestor of the integration head and is a
                     merge parent: aaca40d3 parents a5dfe34f 46b47911.
    C2   INTENDED    Fast-forward from 55deaec9, measured post-report.
    C3   SATISFIED   Four arriving paths, four byte-identical blobs, per path.
    C4   SATISFIED   M4's set has one member, established by comparison, with
                     the manner of main's advance recorded — one modification,
                     which is neither manner the preceding two reported. No
                     silent revert.
    C5   SATISFIED   §5.1 present. The reviewed result assigns NO rule 22
                     subclass to its own outcome; its absence is recorded and
                     none is supplied. Verified by reading — see below.
    C6   SATISFIED   All five mismatches present with classification AND
                     reasoning; the landing record's count is five and M8's
                     is five.
    C7   SATISFIED   §5.3 and §5.5 present; §5.5 carries what established the
                     two properties.
    C8   SATISFIED   See below, with the search stated.
    C9   SATISFIED   M10 recorded per obligation, all four in the "admitted"
                     value, with DECISION_LOG.md's stated scope quoted.
    C10  SATISFIED   R-5 discharged per M5's admitting branch.
    C11  SATISFIED   No landed artifact describing EXT-01's measurement or
                     Q1's classification is modified — diff over six such
                     paths returns zero.
    C12  SATISFIED   Both Statement SHAs unchanged; neither file modified.
    C13  SATISFIED   The base-relative list contains no path outside §1a.
    C14  SATISFIED   M7 records both and states that they differ, 8 against 4.
    C15  INTENDED    Measured in the post-report layer.

### `C5`, verified by reading

**`GAP-B`'s own outcome token is `INDETERMINATE — UNDETERMINED BY READING`**,
one of the four its governing §5 pre-registered. **It is not a rule 22
subclass.**

Measured across the reviewed artifact: every occurrence of `INCONCLUSIVE` — at
`:232`, `:233`, `:492` — refers to **`Q1`**, never to `GAP-B`'s result. `:233`
carries `Q1`'s subclass, which is transported as `Q1`'s.

**No subclass is supplied.** The Reviewer's §18 note governs here: the rule 22
fallback applies to **this task's verification**, not as authority to add a
subclass to a landed finding that carries none.

### `C3`, per path

    path                                              source        product
    derivations/P2-GAPB-BRIDGE-01_regime-transfer.md   521b3b8f6853  521b3b8f6853  IDENTICAL
    reports/2026-08-20T0408Z_gapb-bridge-01.md         55f43be368ee  55f43be368ee  IDENTICAL
    reviews/chatgpt/2026-08-20T0408Z_gapb-bridge-01.md 0dd637beefe9  0dd637beefe9  IDENTICAL
    specs/2026-08-20T0408Z_gapb-bridge-01.md           06e912c4be92  06e912c4be92  IDENTICAL

### `C8`, with the search stated

The landing record was searched for `narrow`, `widen`, `re-read`, `in light
of`, `now means` and `restricted to`. **Three hits, and none narrows,
widens or re-reads a mismatch:**

    :5    "Nothing is added, reclassified or re-read" — a denial
    :149  "No mismatch above is narrowed, widened, or re-read in its light" —
          the §5.4 denial
    :208  "GAPA-INTEG's R-2 narrowed THIS OBLIGATION to GAP-B's half" — about
          an OBLIGATION in the M10 census, not a mismatch. The parenthesis
          naming the distinction was added before commit, because a verifier
          scanning for the word would otherwise have to reconstruct it.

**And the `P2-OBS-IDENT-01` consequence.** The landing record's §4 states the
relation exists, cross-references `R-2` as the open record, and **draws
nothing.** The outcome is neither upgraded nor downgraded, and no mismatch is
re-read in its light.

---

## §9 — Substring hazards, and how each was handled

    mismatch    MM-1..MM-5 and ordinary prose. C6's count was taken over the
                landing record's `### MM-` headings, not over the word.
    transfer    Matches "transverse" loosely and "transfer matrix", a
                different object here. Neither decoy occurs above; the
                transfer question was reached from M8's transcription.
    species     TWO SENSES, and this finding turns on the second: the SPECIES
                COEFFICIENT β_s of the locked conventions, and the
                FERMION/BOSON sense of MM-1. Both occur in this task's
                vicinity — OBS-IDENT is about the first, MM-1 about the
                second — and every use above names which.
    improved    The Symanzik sense in MM-3 versus ordinary usage. Only the
                first occurs above; MM-3's finding is that the object carries
                no improvement term of any kind.
    register    THE REGISTER FILES and "registered" as a verb, and M10 turns
                on the first. No count over the word was used; the four
                obligations were enumerated by reading the three landing
                records that recorded them.
    scope       Three senses in this task: the REGISTER'S STATED SCOPE (M5,
                M10), a task's scope, and R-2's scope question. Every use
                above names which.

**Rule.** "A check that cannot state its exclusions is performed by reading."

---

## §1 — Non-objectives, all honoured

    1  re-scope GAP-B, MM-1, MM-3 or MM-5, or narrow any claim in light of
       P2-OBS-IDENT-01
       NOT DONE. C8, with the search stated.
    2  attempt any mismatch, or open the specified measurement
       NOT DONE. The measurement is transported as specified and not run.
    3  change Q1's verdict, subclass or Resolution path, or GAP-A's outcome
       or momentum condition
       NOT DONE. C11 — zero paths in the diff.
    4  resolve H-EXT-01
       NOT DONE. Unchanged.
    5  amend any landed description of EXT-01's measurement
       NOT DONE. C11.
    6  compute anything, or modify any file under scripts/
       NOT DONE. The scripts/ diff is empty.
    7  modify any file arriving from the source
       NOT DONE. C3, per path.

---

## Stops and clarifications

### `S-1` — `M4` earned its keep, on a path the last two integrations had none

The preceding two integrations reported empty subject sets — one because
`main`'s advance was purely additive, one because `main` had not advanced at
all. **This one has a member, and it is `DECISION_LOG.md`.**

**The exposure was real and one landing old.** `main`'s blob on that path is
the entry `P2-OBS-IDENT-INTEG` appended in the immediately preceding landing,
carrying `R-1` through `R-4`. The source forked before it. **A merge product
carrying the source's blob would have removed four open records from a file
that declares itself append-only** — and the merge was conflict-free, and both
validator runs passed, so **no other check in this task would have seen it.**

**The manner matters and is recorded.** `M4` asks how the set's state was
established. Here `main` advanced **with one modification**, which is neither
of the two manners previously reported. **The check that would have been a
formality twice was load-bearing on the third.**

### `S-2` — `M10` measured a claim I made three times, and it is false

**The claim, in three landed records**: that no register's stated scope admits
`O-1`, `O-2`, `O-3` or `P2-GAPA-INTEG`'s `R-2`.

**Measured: `DECISION_LOG.md` admits all four**, on its stated scope and on its
own 2026-08-19 precedent, in which two items of exactly that shape were
registered there as open findings by an executor for an integration.

**And the count beside the claim does not survive either.** `O-3` was
discharged by the same task that restated the figure; `O-2` is discharged by
this landing; `R-2` restates `O-2`. **One is outstanding.**

**This is my error, three times, and the specification's §6 is what caught
it** — by requiring the claim be measured rather than inherited, and by
declining to restate either figure in its own prose so that the measurement
could not be anchored to them.

**What is NOT concluded.** Whether any of the four should now be entered in
`DECISION_LOG.md` is **undecided**, and this task placed none. Whether the
three landing records that carry the claim should be corrected is **also
undecided** — they are landed reviewed records, and `R-3` of the 2026-08-20
entry already registers the general question of what to do about a landed
description that later work bears on.

### `S-3` — `SPECIFICATION_DEFECT`, non-blocking: three items §13 calls "ruled" have no landed ruling

**The Reviewer's §19 directs this check**: where the specification describes an
outstanding item as already ruled, execution must verify the ruling exists in
authoritative repository state and cite that authority, and **return the
condition to the PI if it does not.**

**Checked. It does not.**

    §13.1  R-4 — "A cross-reference measurement, RULED TO RUN before the
           claim-reach work"
    §13.2  R-3 — "the claim-reach enumeration, then the narrowing, BOTH RULED"
    §13.3  R-1 — "D-2 RETIRED AND SPLIT: the mass part into the primary
           extraction, the volume part standing separately with its priority
           raised"

**The only landed authority on `R-1`, `R-3` and `R-4` is the `DECISION_LOG.md`
entry dated 2026-08-20**, landed by the immediately preceding task, which
records all four as **OPEN and undecided** and states in terms: "Nothing is
settled by these four records. `R-1` re-specifies nothing". `R-4`'s entry says
"is not decided"; `R-3`'s says "is undecided, and so is the mechanism".

**A repository-wide search for the phrases "ruled to run", "both ruled" and
"retired and split" returns exactly two files for each: this task's own
specification, and this report quoting it.** Nothing else in the repository
carries them.

**Nothing was acted on.** §13 is "Outstanding after this task", and this task
performed none of the three. **The defect is in the description, not in an
action**, which is why this is non-blocking.

**Returned to the PI, not adopted.** Per the Reviewer's §19, an integration
specification cannot create a ruling by describing an item as ruled. **Either
the rulings exist and are not landed — in which case they need landing before a
task relies on them — or they were not made.** This task does not choose
between those, and it did not carry the descriptions forward into any landed
artifact: the landing record and the `R-5` entry state that placement and reach
questions are undecided, which is what the repository says.

### `S-4` — the two method findings were transported, and one of them is why `M10` exists

`M8`'s second method finding is that "unimproved" and "a = 1" entered `GAP-B`'s
frozen pre-registration on a prior artifact's word and were checked against the
object only when the bridge task read it.

**`M10` is the same failure in a different register**: a claim about the
repository was asserted, repeated across three landings, and not measured until
a specification required it. **The first happened to hold. The second did
not.**

**Both are transported as method findings** — the first because §5.5 requires
it, the second because §6 commissioned the measurement — **and neither is
generalised into a rule here.** The specification's §13 lists an `S-2`
characterisation rule as not yet landed; **this report adds a second instance
to the case for it and does not land it either.**

---

## Layering

This report is measured at `b3f6111`, the landing commit, which is commit N−1.
`C2` (the fast-forward) and `C15` (the ref-push scope) are measured in the
post-report layer; that evidence is returned to the Reviewer in chat and is not
written back into this file.
