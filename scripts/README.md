# Scripts

Store reproducible symbolic and numerical implementations here. Each script
must document its inputs, outputs, assumptions, regulator, cutoff,
normalization, seeds, operating point, and associated gate and derivation note.
Scientific dependencies will be added only when a specified gate requires them.
