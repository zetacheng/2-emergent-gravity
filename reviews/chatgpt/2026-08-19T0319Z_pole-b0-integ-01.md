# Review — P2-POLE-B0-INTEG-01

**Reviewed artifact:** `P2-POLE-B0-INTEG-01(1).md`  
**Reviewed specification SHA-256:** `6ee7c8154516b94b9f9a4f6fee6c3b2a51a6fbf48777cf02e717d30d832634d1`  
**Review date:** 2026-08-19  
**Review verdict:** `APPROVE FOR EXECUTION`

## 1. Integration purpose — PASS

The specification correctly treats this as an integration and landing task for the completed POLE-B0 scope assessment.

It does not convert the negative scope result into evidence for the physical programme. Landing the result closes and records a task; it does not establish the existence of a graviton pole.

## 2. Primary scientific verdict — PASS

The integration preserves the primary POLE-B0 verdict:

`REQUIRES A CONSTRUCTION NOT YET SCOPED`

with `TRACTABLE BUT BLOCKED PENDING A RULING` applying only in part.

This hierarchy is necessary because open rulings and absent construction are different blocker classes.

## 3. Blocker separation — PASS

The specification explicitly preserves:

`RULING BLOCKERS != CONSTRUCTION BLOCKERS`

and prevents the landed result from being compressed into merely `blocked pending R1-R4`.

That is scientifically important. Even if R1-R4 were ruled immediately, the missing construction components would remain missing.

## 4. Component accounting — PASS

The POLE-B0 accounting is preserved as:

`N_total = 12 = 0 + 3 + 3 + 6`

with:

- `N_both = 0`
- `N_impl = 3`
- `N_spec = 3`
- `N_neither = 6`

The key finding is not merely the total. No milestone component presently has both an applicable implementation and a specification.

The integration correctly treats the six `NEITHER` components as a construction deficit rather than a ruling deficit.

## 5. R4 dependency — PASS

The specification preserves the distinction between the source-side dependency found in SRC-B0 and the dependency of the POLE correlator.

For the pole milestone, the object contains an expectation-value bracket. The microscopic ensemble/state-space/functional-measure question therefore enters at the correlator-definition level rather than merely through a particular definition of the stress tensor.

The integration must retain that distinction rather than importing SRC-B0's conditional classification wholesale.

## 6. Existing machinery is not promoted into pole machinery — PASS

The specification correctly prevents existing Barnes-Rivers projectors, `tt_check.py`, RECON-01a, heat-kernel machinery, or related implementations from being described as an already executable pole/residue pipeline.

Channel decomposition is not pole extraction.

A one-loop/free-kernel contraction is not automatically an interacting stress-tensor correlator.

A projected quantity is not automatically a residue measurement.

These distinctions are correctly protected.

## 7. M6 repository-consistency re-measurement — RESOLVED / PASS

The material issue identified in the previous review has been repaired.

M6 now requires the executor to read the relevant scipy statements on the actual merge product and record their current locations and substantive meanings.

Historical line references are orientation only and are not accepted as facts.

Accordingly, R-2 is conditional:

- if M6 reproduces a genuine contradiction between the dependency declaration and the source statement, register `R-2` as a repository consistency defect;
- if M6 does not reproduce it, do not create R-2 merely because an earlier report said it existed.

Failure to reproduce R-2 does not block the scientific POLE-B0 landing.

This satisfies the programme's re-measurement discipline.

## 8. M7 register vocabulary — RESOLVED / PASS

The revised specification also resolves the secondary status-vocabulary concern.

M7 requires the executor to inspect the deferred-items register's actual legal status vocabulary before creating R-1.

The integration specification therefore does not invent a new semantic meaning for `PROPOSED` or any other status token.

If no existing status faithfully represents `raised but not scoped`, the executor must return the issue rather than silently inventing register semantics.

## 9. Deferred-item registration — PASS

R-1 is appropriately treated as a governance consequence of the landed scientific result: the programme lacks a scoped construction for the decisive pole milestone.

Registration must preserve the negative-result character of the finding.

R-2 is governed separately by M6 and must not be created unless the contradiction is reproduced at execution time.

## 10. Merge-product measurement — PASS

The specification correctly requires scientific and repository conclusions to be checked on the merge product rather than blindly transcribed from the source report.

This is particularly important for:

- arriving-path scope;
- register state;
- dependency/status vocabulary;
- repository consistency statements;
- checker and validator results.

The integration therefore distinguishes source-task evidence from landing-time repository state.

## 11. Regression protection — PASS

The specification appropriately treats unexpected arriving modifications, especially any arriving `scripts/` path, as requiring explicit Reviewer attention.

A scope-assessment integration should not quietly land executable scientific changes.

The base comparison and manifest checks are appropriate safeguards.

## 12. PI decisions remain outside this task — PASS

The integration does not opportunistically adjudicate unrelated open scientific choices such as the `r = 1` issue or the unresolved `Z_M` input.

Those remain PI/research decisions.

Their presence must not be used as an excuse to broaden this landing task.

## 13. Epistemic effect of landing — PASS

The specification correctly states the effect of the merge:

`Landing a negative result adds no support to the programme. This merge closes a task.`

That is the correct interpretation.

The landing can improve the programme's map of what remains to be constructed without increasing evidential support for the physical claim.

## 14. Acceptance structure — PASS

The revised acceptance criteria are internally consistent.

In particular:

- R-1 uses the vocabulary established by M7;
- R-2 exists only if M6 reproduces the contradiction;
- the primary POLE-B0 verdict is not weakened if R-2 is absent;
- ruling blockers and construction blockers remain separately reported;
- post-execution verification can distinguish the conditional R-2 branch without turning it into a landing blocker.

## 15. Remaining specification defects

None found at STOP level.

The previous review's substantive blocker — registering the scipy contradiction without re-measuring its two repository statements — is resolved.

The secondary concern about independently assigning register status semantics is also resolved by M7.

## Final verdict

**`APPROVE FOR EXECUTION`**

`P2-POLE-B0-INTEG-01(1).md` may be executed.

This approval is bound exclusively to the exact uploaded specification bytes with SHA-256:

`6ee7c8154516b94b9f9a4f6fee6c3b2a51a6fbf48777cf02e717d30d832634d1`

The central scientific constraint to preserve during execution and landing is:

**POLE-B0 has both ruling blockers and a separate missing-construction problem. Resolving R1-R4 would not by itself make the decisive pole milestone executable, and landing this negative result must not be represented as positive evidence for the physical programme.**
