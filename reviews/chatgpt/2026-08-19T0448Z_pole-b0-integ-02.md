# Review — P2-POLE-B0-INTEG-02

**Reviewed artifact:** `P2-POLE-B0-INTEG-02(1).md`  
**Reviewed specification SHA-256:** `49afad6a62671fda0c8cb2c5ff81428c016e8ee482dc7314e02208fc16b6139e`  
**Review date:** 2026-08-19  
**Review verdict:** `APPROVE FOR EXECUTION`

## 1. Re-issued integration purpose — PASS

The specification correctly re-issues the POLE-B0 integration after the prior A4 governance abort.

The previous abort did not reject the scientific verdict; it exposed a missing repository policy clause for integrating `science/*` branches.

That governance gap has now been repaired and landed.

## 2. Binding base and source — PASS

The integration base is pinned to:

`4b30e7855a657674aecf46d4cc76d68244f385a9`

The source branch is pinned to:

`science/pole-b0-milestone-scope @ 3f78acfebd7e994460eb30ac54ffd31a5cf3c21f`

The task must stop if either has advanced.

This is appropriate for a reviewed integration authorization.

## 3. `science/*` merge authority — PASS

The specification correctly delegates merge mechanics to the newly landed `science/*` clause in `docs/BRANCHING_POLICY.md`.

The expected execution shape is:

- pinned remote `science/*` source tip;
- non-fast-forward merge into a dedicated integration branch;
- squash and rebase prohibited;
- source tip preserved as a merge parent;
- authoritative `main` advanced only by fast-forward to the completed integration head.

A4 remains active as a real check: if the base policy does not actually contain a matching clause, execution stops.

## 4. Push-scope handling — PASS

The specification correctly follows the currently landed policy literally: only the integration branch and `refs/heads/main` may be pushed during landing.

It does not silently extend that authority to the task/source branch.

The observation that the immediately preceding governance task pushed an extra task branch is recorded as a policy question rather than normalized into a new rule.

This is the correct governance treatment.

## 5. Scientific verdict preservation — PASS

The primary POLE-B0 verdict is preserved as:

`REQUIRES A CONSTRUCTION NOT YET SCOPED`

The partial ruling-related classification:

`TRACTABLE BUT BLOCKED PENDING A RULING`

is explicitly prevented from replacing the primary verdict.

This is necessary because ruling blockers and missing-construction blockers are different.

## 6. Two-layer blocker structure — PASS

The landing record must preserve two separately headed layers:

- `RULING BLOCKERS`
- `CONSTRUCTION BLOCKERS`

The specification explicitly requires the statement that ruling R1-R4 in full would not clear Layer 2.

This prevents the negative result from being incorrectly compressed into `blocked pending R1-R4`.

## 7. Component accounting — PASS

The component accounting is preserved as:

`12 = 0 both + 3 impl + 3 spec + 6 neither`

with `N_both = 0`.

The task treats this as a transcription from the frozen POLE-B0 branch report, not as an invitation to re-partition the milestone.

A mismatch triggers A5.

## 8. Load-bearing construction finding — PASS

The integration correctly preserves the consequence of `N_both = 0`:

no milestone component presently has both an applicable implementation and a specification.

Therefore the construction layer itself is not established.

This is the reason the primary verdict is a construction verdict rather than merely a ruling verdict.

## 9. R4 dependency distinction — PASS

The specification correctly keeps two dependency statements separate.

For the POLE milestone, the correlator contains an expectation-value bracket, so ensemble/state-space/functional-measure structure enters the definition of the observable itself.

For SRC-B0, the measure dependence is conditional on whether the source stress tensor is defined through Γ or through the classical action.

These must not be merged into a single statement.

## 10. Existing machinery is not promoted — PASS

The specification correctly prevents existing components from being misrepresented as a completed pole pipeline.

Barnes-Rivers projectors provide channel decomposition but not an interacting correlator, pole extraction, or residue analysis.

`tt_check.py` contains useful channel machinery but does not thereby implement the milestone.

RECON-01a provides a curved bosonic determinant/operator construction and Γ^(2)-class response, not the interacting stress-tensor correlator requested by the manuscript's pole milestone.

## 11. SciPy correction — RESOLVED / PASS

The earlier POLE-B0 report's claimed direct repository contradiction is correctly recorded as **not reproduced**.

The specification distinguishes:

- `pyproject.toml` declaring a project dependency; from
- `proca_curved.py` speaking about the currently declared execution environment.

These statements have different subjects.

The line-location correction is also preserved without modifying the frozen source branch.

## 12. Environment-document relationship — PASS

The revised specification no longer asserts a second, unverified inconsistency between `pyproject.toml` and `docs/local/execution_environment.md`.

Instead, it records an open documentation/environment relationship question:

whether the environment document is intended to mirror the full project dependency set or only a minimum validated execution environment.

That question is explicitly left to a separate task and is not registered here.

This resolves the substantive issue identified in the previous review.

## 13. R-1 open construction item — PASS

The POLE construction/specification layer is correctly placed on the OPEN side rather than the deferred side.

The rationale is sound: deferred work has been examined and consciously postponed, while POLE-B0 found that the construction is not yet scoped.

The item is classified as an open programme-design/construction question rather than an acceptance-criterion item.

The executor must read existing register scopes and use an existing identifier sequence; if no register admits this class, the task returns to the Researcher instead of inventing register semantics.

## 14. No R-2 entry — PASS

The specification correctly creates no SciPy repository-consistency debt entry.

The prior R-2 proposal was conditional on a contradiction that did not reproduce.

The landing record may document that the pre-registered conditional item did not fire.

## 15. Merge-product measurement discipline — PASS

M1-M7 are appropriately re-measured at execution time.

The task explicitly forbids carrying figures from prior handovers or reports as targets.

The test suite must be run in a real Git worktree, addressing the observation-method error recorded by INTEG-01.

## 16. Authorised path manifest — PASS

The specification defines the authorised path manifest once and requires A6/C10 to reference the same definition.

It includes:

- the selected open register;
- this task's spec/review/landing-record/report artifacts;
- the files arriving through the POLE-B0 merge itself.

This avoids a self-triggering scope contradiction.

## 17. Acceptance criteria — PASS

The acceptance structure is internally coherent.

It separately verifies:

- source ancestry;
- blocker-layer preservation;
- component accounting;
- dependency distinctions;
- negative delimitations;
- SciPy correction;
- M1-M7 measurements;
- the open register entry;
- scope containment;
- frozen `proca_curved.py` blob observation;
- exact allowed-ref push set.

No criterion depends on a search pattern that is guaranteed to match the compliance text itself.

## 18. Epistemic effect of landing — PASS

The specification correctly states:

`Landing a negative result adds no support to the programme. This merge closes a task.`

The landing improves the programme's map of what is missing.

It does not establish a massless spin-2 pole and does not advance `P2-PHASE-01`.

## 19. Remaining specification defects

None found at STOP level.

The prior review's sole substantive issue — prematurely asserting a `pyproject.toml` versus `execution_environment.md` inconsistency — is resolved.

## Final verdict

**`APPROVE FOR EXECUTION`**

`P2-POLE-B0-INTEG-02(1).md` may be executed.

This approval is bound exclusively to the exact uploaded specification bytes with SHA-256:

`49afad6a62671fda0c8cb2c5ff81428c016e8ee482dc7314e02208fc16b6139e`

The central scientific constraint to preserve during landing is:

**POLE-B0 has both ruling blockers and a separate missing-construction problem. Resolving R1-R4 would not by itself make the decisive pole milestone executable, and the SciPy/environment clarification remains a separate documentation question rather than part of the POLE scientific verdict.**
