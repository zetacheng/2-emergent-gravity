# Review — P2-BETAV-EXTCOMP-01

**Reviewed specification:** `P2-BETAV-EXTCOMP-01(1).md`  
**Reviewed specification SHA-256:** `0b97e9d32b656a7daaac9455c3b795c09e6de3b57a6c9518b13f66e9ba0619b6`  
**Review date:** 2026-08-21  
**Reviewer:** ChatGPT  
**Review verdict:** `APPROVE FOR EXECUTION`

## 1. Review binding

This review is bound exclusively to the exact specification bytes identified by:

`0b97e9d32b656a7daaac9455c3b795c09e6de3b57a6c9518b13f66e9ba0619b6`

It does not authorize execution of any other version of the specification.

## 2. Overall protocol architecture — PASS

The revised specification adequately freezes the analysis choices that could materially affect the component-level mass-log measurement before result-bearing execution.

The protocol separates pre-registration, measurement, blind-target protection, numerical validity handling, and later interpretation.

## 3. Retained assembly and weights — PASS

The prior substantive blocker is resolved.

The retained aggregate is defined through the landed pipeline rather than by assuming unit weights:

`B_R = sum_i w_i beta_i`

The executor must establish the retained weights or equivalent normalization from authoritative landed code/definitions during M1 and cite the fixing location.

If the landed assembly cannot be determined, A8 requires STOP rather than inference.

If the landed assembly is not representable as a simple weighted sum, the formulas must be rewritten during pre-registration to reproduce the landed structure before result-bearing measurements occur.

## 4. Discarded assembly — PASS

Because no authoritative discarded-space assembly is already defined, the discarded weights are explicitly identified as a task-level methodological choice rather than repository fact.

Their functional form is tied to the retained-side assembly as specified, preventing post-result normalization selection.

## 5. Aggregate quantities — PASS

The two requested aggregate diagnostics are mathematically frozen, including the distinction between signed cancellation and component magnitudes.

The protocol therefore distinguishes the analogue of:

`R_signed = |B_D| / |B_R|`

from the corresponding weighted sum-of-magnitudes ratio.

The assembly and normalization needed for these quantities must be fixed in the M2 pre-registration commit.

## 6. Residuals and uncertainty — PASS

The revised specification correctly distinguishes deterministic numerical evaluation from statistical sampling uncertainty.

Residuals may diagnose fit quality or model mismatch but are not interpreted as probabilistic uncertainty on a component coefficient.

The reported uncertainty-like spread is restricted to the frozen analysis-variant sensitivity structure.

## 7. Numerical fit validity — PASS

The numerical validity rules are sufficiently pre-registered.

Non-finite outputs and solver-reported rank deficiency are failures. Finite full-rank fits remain reportable even when poorly conditioned, with conditioning recorded rather than subjected to an after-the-fact threshold.

Residual magnitude is diagnostic and is not converted into an unregistered failure threshold.

## 8. Component failure handling — PASS

A per-variant failure may be reported as not evaluable under the frozen protocol.

A required component is not silently dropped, set to zero, or replaced by a surviving subset. If the required component cannot produce the needed coefficient across the frozen variants in the manner specified, the stated abort condition governs.

## 9. Blinding — PASS

The task does not compute `beta_B` and therefore does not structurally assemble the blinded `beta_V / beta_B` target.

The protocol also bars target/anchor comparison inside this task and limits result-bearing outputs accordingly.

This is stronger than merely instructing the executor not to inspect a known comparison.

## 10. Pre-registration order — PASS

The result-bearing sequence is appropriately constrained:

`M1 -> M2 PRE-REGISTRATION COMMIT -> M3 -> M4 -> M5 -> M6`

The pre-registration commit must freeze the mass windows, fit forms, variant definitions, assembly/weights, numerical treatment, and other specified analysis choices before the measurements that could reveal the result.

The committed M2 blob is itself subject to verification for absence of result values.

## 11. Mass windows and estimator selection — PASS

The light and heavy windows are both retained under the frozen protocol, with neither designated primary.

The specification therefore does not select an estimator after observing target-bearing behaviour.

Structural discussion of finite-volume and longitudinal-mode limitations remains distinct from blind-target comparison.

## 12. Variant sensitivity — PASS

The sensitivity variants are required to be named and frozen before result-bearing execution.

Any pre-registered prediction concerning relative variant spread is a testable methodological prediction. If it fails, the report should state that it failed rather than reinterpret the result post hoc.

Observed relative spread alone should not be promoted into a stronger causal claim about cancellation of systematics unless separately established.

## 13. Execution-time caution on retained normalization

The retained weights/normalization are load-bearing.

If the authoritative implementation encodes the assembly indirectly through averaging, trace normalization, projector normalization, multiplicity, or an equivalent code structure rather than an explicit `weights` object, the executor must establish that structure from the landed implementation.

Apparent algebraic simplicity is not authority to substitute unit weights. If the structure cannot be established, A8 governs.

## 14. Scope and interpretation boundary

This task measures whether the discarded external-index space contributes to the mass-log observable under the frozen protocol.

It does not perform the later blinded comparison, does not compute `beta_B`, and does not by itself settle broader GAP-B transfer questions beyond what its measurements directly establish.

## 15. Branch, commit, and push controls

Execution remains subject to the specification's exact base, branch, manifest, abort, commit, and push-scope requirements and to the repository branching policy.

No session or harness branch gains authority merely because it is the executor's current branch.

## Final verdict

**`APPROVE FOR EXECUTION`**

`P2-BETAV-EXTCOMP-01(1).md` is approved for execution subject to all stated controls.

**Reviewed specification SHA-256:** `0b97e9d32b656a7daaac9455c3b795c09e6de3b57a6c9518b13f66e9ba0619b6`
