# PI amendment record — geometry-response probe re-pin (LANDING-ROUTED)

Status: PI amendment. Records a change of authoritative artifact hash
that occurred mid-task, so that the branch's three-commit history is
auditable rather than unexplained.

## What happened

| Stage | Artifact hash | Outcome |
|---|---|---|
| Original supply (LANDING-ROUTED-v1) | `efc68382822fd6b06fc09d10ea1eaf0f23f906a839663dcbec20200304a0f025` | Rejected: failed repository lint (`E702` semicolons, `E501` long lines). Executor stopped correctly. |
| First re-pin (v1.1 reply) | `5fec6084ff923ea05a178ffbc299a1f5c938b7ad13b119b2fbb4c94db82b9317` | Landed in `4fbd87d`. Then failed the frozen validator: `I001` (unsorted imports). Executor stopped correctly. |
| Second re-pin (v2.2 reply) | `850fdcdca97412f56359e37b3bd5c22301d944e562055219c7d3f13ca664eb85` | Landed in `9ee30ab`. **Current authoritative artifact.** |

## Why the earlier pins were withdrawn

Both failures were prompt defects, not repository or executor defects:

1. The validation used for the first re-pin did not apply this
   repository's complete configured rule set (`select = ["E", "F", "I"]`
   at `line-length = 88`, per `pyproject.toml`); consequently `I001` was
   not exercised before the artifact was pinned. The full command output
   from that check was not retained, so no stronger claim about how the
   configuration resolved at the time is made here.
2. The prompt simultaneously demanded byte-for-byte preservation of a
   pinned artifact AND a clean run of a validator that artifact could
   not pass — an unsatisfiable combination. The executor stopped and
   reported both times, as `CONVENTIONS.md` requires.

**`5fec6084…` is formally withdrawn.** Restoring it is not an available
remedy: it fails the repository's own frozen validator, so restoring it
would reintroduce the same contradiction.

## What changed between `5fec6084…` and `850fdcdca…`

Exactly one added blank line after `import numpy as np` (PEP 8
two-blank-line separation before the first `def`), generated by
`ruff --fix`. No code, no constant, no comment, no docstring changed.

## Re-verification of the authoritative artifact

Performed against the file as landed on branch
`concepts/p2-dual-pipeline` @ `9ee30ab3`:

- `ruff check --select E,F,I --line-length 88` → **All checks passed**
  (rule set and line length read from this repository's
  `pyproject.toml`, not assumed).
- Executed from a clean clone of the branch; the numbers cited in
  `derivations/P2-LATTICE-ROUTE-01.md` §3 all reproduce exactly:
  `dE·L²/N` = −0.000844 at L = 24 and −0.000905 at L = 144;
  `dE/ε²` stable across ε = 0.02/0.04/0.08 (−7.422e-2, −7.424e-2,
  −7.433e-2); `b/ε²` = −2.6594e-2, −3.2556e-2, −5.8856e-2 at
  t₂ = 0.0/0.2/0.4; fit residuals 2.8e-4, 9.2e-6, 1.5e-5.
- Numeric output is token-for-token identical to the `5fec6084…`
  version (80 numeric tokens, zero differences).
- Neither landed concept note pins any script hash, so no reference in
  them is made stale by this re-pin.

## Amendments to the original task specification

1. The authoritative probe artifact is `850fdcdca…`; `efc68382…` and
   `5fec6084…` are withdrawn.
2. The branch carries **three** commits, not the two originally
   specified. The third is an additive corrective commit; no history was
   rewritten and no force-push occurred, consistent with merge
   discipline.
3. The accurate scope statement for this landing is: *no pre-existing
   repository file changed; the newly landed probe differed from the
   originally supplied artifact and was corrected and re-pinned on the
   branch before merge.* The shorter phrasing "no artifact digest
   changed" is true of pre-existing repository artifacts only.

## Standing

This amendment governs the LANDING-ROUTED task only. It sets no
precedent for altering a pinned artifact after registration: the probe
is unregistered, non-canonical exploratory material. **A hash-pinned
artifact belonging to a registered gate may not be re-pinned this way** —
that requires a registry amendment with its own review, which is the
open question for `scripts/euclidean_reconstruction.py`.
