# P2-GAPA-INTEG — Land the basis identification, with its scope condition

    Status            SPECIFIED — not executable until Reviewer approval is committed (Rule 15)
    Author role       Researcher
    Executor          sole write-access holder
    Verifier          Researcher, from a clean clone, no git writes
    Kind              INTEGRATION. Transport only.

---

## 0. Binding SHAs

    Integration base (main at authorship)   f76387474204a4cadda5275995eaa031ccdca8bf
    Source                                  science/gapa-bridge-01
                                            02a834030000000000000000000000000000000

**The source SHA above is abbreviated in the record this specification was
written from.** `M1` reads the full ref value from the remote and records it;
if it does not begin `02a83403`, that is an `A1` return. **No merge proceeds
against an abbreviation.**

If either SHA has advanced when execution begins, execution does not proceed.

---

## 1. Objective

Land the `GAP-A` derivation. **Add nothing.**

The result is the first positive derivation this line has produced, and it
arrives with a condition. Both travel together or the landing has
misrepresented it.

## 1a. Rule 17 governs, and one boundary needs stating

The reviewed result records the identification, its scope, and its own
statement of what it does not license. **All three are transported.**

**What is not done here:** no new rule, convention, or citation policy is
created. A statement of the form "this result must always be cited with its
scope condition" would be a governance rule the reviewed result does not
carry, and rule 17 forbids this task adding one. The reviewed result's own
sentence about what it does not license is transported verbatim instead, and
the question whether a standing citation rule should exist is registered under
§6, not answered.

## 1b. Non-objectives

This task does **not**:

1. attempt, scope, or prejudge `GAP-B`;
2. change `Q1`'s verdict, its subclass, or its `Resolution path`;
3. alter `A-EXT-01` or `H-EXT-01`, or any exact statement — **any edit would
   break a `Statement SHA` and void a pinned review**;
4. draw any consequence for `H-EXT-01` — see §5.4, which requires the opposite;
5. update any readiness register — `O-1`, unowned;
6. compute anything, or modify any file under `scripts/`;
7. modify any file arriving from the source.

## 1c. Authorised path manifest — defined once

    P1   every path arriving from the source by the merge
    P2   every register selected under M5, if any admits the §6 records.
         **More than one may be written; none may be created.**
    P3   this task's own spec, review, landing-record and report artifacts

`A6` and `C12` both refer to this manifest and neither restates it.

**Self-check performed on this specification**, because its omission has
produced repeated defects in this line: no manifest entry assumes a count; no
entry says "the register" where more than one may apply; and **no number
appearing in this specification's prose is asserted rather than measured** —
every count is produced by an `M` step. The defect count itself is
deliberately not stated as a figure here, for the same reason.

---

## 2. Measurements

Nothing is carried, including from the source's execution report and from the
Researcher's independent reconstruction of its result.

    M1   Read the full source ref value from the remote. Record it.
    M2   Test suite on the merge product and at `f7638747`, in real worktrees.
    M3   Dry-run merge: conflict-free or not; conflicting paths if any.
    M4   **The revert hazard.** For every path present on both `f7638747` and
         the source at differing blobs, record the blob at the source's fork
         point, on the source, on `main`, and on the merge product. A merge
         product carrying a source blob where `main` had advanced fires `A5`.
         **Establish the direction of each difference; do not infer it.**
    M5   Read the registers' stated scopes and record, PER RECORD in §6,
         which register admits it. **These may differ, and none may be
         created.** Record the answer either way.
    M6   Locate the `science/*` integration clause; record its line span and
         the merge mode, allowed-ref scope and main-advance rule.
    M7   `git diff --name-only f7638747..<merge product>`, and separately the
         source's contribution measured against its own fork point. Record
         both and record that they differ.
    M8   Transcribe, from the reviewed result: the outcome; the residuals and
         tolerance of both methods; the block-by-block correspondence of the
         second conjunct; the momentum direction at which the identification
         holds; and the observed magnitude of the failure at other directions.
    M9   Record the `Statement SHA` of `A-EXT-01` and `H-EXT-01`; confirm
         unchanged, and confirm neither file is modified.

---

## 3. Abort conditions

    A1   a SHA observed differs from §0, or `M1` returns a value not beginning
         with the abbreviation §0 records
    A2   `M3` reports conflicts
    A3   `M2` shows a failure not also present at `f7638747`
    A4   `M6` finds no governing clause, or one contradicting §4
    A5   `M4` finds the merge product carrying a source blob at a path where
         `main` had advanced
    A6   a path outside the §1c manifest appears in `M7`
    A7   landing any §5 item would require editing `A-EXT-01`, `H-EXT-01`, or
         any file arriving from the source

---

## 4. Merge mechanics

Governed by the clause located at `M6`. `--no-ff` merge of the source into a
dedicated integration branch, preserving the source tip as a merge parent;
`main` advances by fast-forward only; push scope is the integration branch and
`refs/heads/main` and no other ref; the source branch does not move.
Prohibited: squash, rebase, force-push, `--force-with-lease`, branch deletion,
history rewrite.

---

## 5. Landing record — required content

### 5.1 The result and its condition, in the same statement

The outcome is recorded **with its momentum condition attached to it**, not in
a separate paragraph. `M8`'s direction and failure magnitude appear alongside
the outcome. `C5` fails if the outcome appears anywhere in the landing record
without its condition.

### 5.2 Both conjuncts

The subspace equality, and the block-by-block correspondence of the discarded
components. **The second conjunct is recorded as the correspondence the
reviewed result establishes, not as a statement about dimension.**

### 5.3 What the reviewed result says it does not license

Transported verbatim, including its statement about reading the recipes as
covariant.

### 5.4 What this result does NOT establish — required, and the reason it is required

The record states each of the following:

- **`H-EXT-01` is unchanged.** It remains `UNESTABLISHED` and `NOT ASSUMED BY
  RECON-01b`. `GAP-A` identifies which subspace the discarded components
  occupy; it says nothing about whether they contribute to the physically
  relevant observable.
- **`Q1` is unchanged.** It remains `INCONCLUSIVE` with its subclass and
  `Resolution path` intact. `GAP-B` survives, as the classification recorded
  in advance that it would.
- **The apparent chain is not closed.** A reader may assemble: the discarded
  components are the non-TT blocks; the manuscript states non-TT structures
  are suppressed; therefore the discarded space is negligible. **The step from
  the second to the third is `GAP-B` and is unestablished.** The record says
  so explicitly.
- `A-EXT-01` is unchanged and remains a definitional convention.

**This section is required because the result makes a wrong inference easier
than it was before**, not because anyone has drawn it.

### 5.5 Method provenance

That the two methods share the projector convention read from the repository
and the recipes read from `mlog_coeff.py`, and are therefore two independent
computational checks of the same formal inputs rather than independent
evidence sources. The reviewed result's own record of the symbolic-float
incident is transported as part of this.

---

## 6. Records registered, per `M5`

    R-1   Whether a standing citation rule should require the momentum
          condition to accompany this result wherever it is cited.
          **Registered as undecided.** This task does not create such a rule.

    R-2   That `GAP-B` is now the sole remaining gap named by `Q1`'s
          `Resolution path`, and is unowned pending PI direction.

If `M5` finds no register whose stated scope admits a record, that is recorded
in the landing record with the scopes read, and **no register is created and
none is used by convenience**. `C9` accepts both branches.

**The homeless-obligation count is not incremented by a record that restates
an existing obligation.** If `R-2` is `O-2` under another name, it is recorded
as such and the count is unchanged.

---

## 7. Acceptance criteria

    C1   The source tip is an ancestor of the new `main` and is a merge parent.
    C2   `main` reached its tip by fast-forward from `f7638747`.
    C3   Every arriving path's blob is byte-identical to its source blob,
         reported per path.
    C4   `M4` recorded per differing path with the direction established; no
         silent revert.
    C5   The outcome does not appear anywhere in the landing record without
         its momentum condition. Verified by reading every occurrence.
    C6   Both conjuncts present per §5.2.
    C7   §5.3 transported verbatim.
    C8   All four items of §5.4 present.
    C9   §6 discharged in one of its branches, matching `M5`.
    C10  §5.5 present.
    C11  `M9`'s two Statement SHAs unchanged and neither file modified.
    C12  `M7`'s base-relative list contains no path outside the §1c manifest.
    C13  `M7` records both measurements and states that they differ.
    C14  Refs pushed are exactly the integration branch and `refs/heads/main`.

---

## 8. Substring hazards

    TT              matches tt_check, TT_RECIPES, and the Barnes–Rivers TT
                    block; this result is precisely about two of them
                    coinciding, so a count cannot stand for a reading
    spin-2          matches the manuscript's channel discussion, which
                    concerns a different object
    identification  matches "identify" in unrelated prose
    holds           matches "household", "stronghold", and "holds that"
    axis            matches "axis-TT" and ordinary geometric prose

A check that cannot state its exclusions is performed by reading.

## 9. Criterion satisfiability

`C5` is checked by reading every occurrence of the outcome, a finite set fixed
once the landing record exists.

`C9` accepts both branches of `M5`, so a correct execution passes whether or
not a register admits the records.

`C11` compares digests that exist before the merge.

---

## 10. Post-execution verification (Researcher)

1. re-run `C1` and confirm the merge parent by id;
2. compare arriving blobs against source blobs;
3. **independently reconstruct the projectors and repeat both conjuncts** —
   a landed derivation is not exempt from re-derivation;
4. re-run `M4` on every differing path;
5. read every occurrence of the outcome for its momentum condition;
6. read §5.4 for all four items, and specifically for the third;
7. recompute `M9`'s digests;
8. confirm `C14` by `git ls-remote`;
9. anything unevaluable is recorded **INCONCLUSIVE**, with a subclass and a
   `Resolution path` per rule 22.

---

## 11. What this task does not establish

It lands a derivation already made, reviewed and independently reproduced. It
produces no new result, no `β_V`, moves no gate, and `P2-PHASE-01` is
unchanged.

`Q1` remains `INCONCLUSIVE`. `H-EXT-01` remains `UNESTABLISHED`. What changes
is that one of the two gaps `Q1`'s `Resolution path` names is closed, and the
line's outcome now turns on the other.

---

## 12. Next

1. **`GAP-B`** — the regime transfer. PI has directed it as the next task.
2. **`O-1`**, and the obligations register that would hold it and its
   companions.
3. **`RECON-01b`** itself, after the `Q1` line resolves or is set down.
