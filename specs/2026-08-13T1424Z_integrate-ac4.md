# Task specification — integrate the AC-4 determination, and land it

Specification evidence base: `1b569851a914589242024c4dde7d2eb020e3800c`

    Branch to create   science/integrate-ac4
    Cut from           authoritative main @ 1b569851…
    Source             science/ac4-symmetry-goldstone
                       f4adc2d458a44d10b368a96f5dbb494cedd220ab

Classification: **MATERIAL**. Governed by Rule 15 and Rule 18.

**This is the integration authorization AND the landing authorization.**
§6 carries the landing clause; **no second task is required.**

**One merge, measured clean.** Dry run from the evidence base with the
specification and review commits in place: **no conflict**, merge-base
`1b569851…`, **6 additions and 0 modifications at the merge commit.**
**Any conflict is an immediate STOP.**

**Nothing is modified by this task.** **No pin moves, no gate changes,
no draft is touched.** The source adds four files and nothing else.

---

## 0. What lands

**One determination artifact**, and the three task records that produced
it.

    derivations/P2-PHASE-01_AC4_symmetry_and_goldstone.md

**It carries two verdicts:**

    Verdict A   A-NO-EXACT-CONTINUOUS-BREAKING, for a uniform
                flavour-singlet scalar condensate under the exploratory
                Wilson-form kernel — and for nothing else
    Verdict B   B-NOT-CLOSABLE — OPEN-AC-4 does not close

## 1. What this does NOT establish, and the reading it must not receive

**`OPEN-AC-4` REMAINS OPEN after this merge.** **Verdict A is a
conditional result about one kernel and one candidate class**; **verdict
B is the answer to the question `OPEN-AC-4` actually asks**, and it is
negative.

- **A flat-direction count of zero is not a stability statement.** It
  says which stability statement would be the right one to make. **The
  full condensate-space Hessian has still never been computed.**
- **No candidate is named as passing or failing `C-i`, `C-ii` or
  `C-iii`**, in the arriving artifact or anywhere else, and **this task
  must not name one either.**
- **The programme has NOT selected Wilson fermions.** The canonical
  lattice Dirac operator is delegated to `D-pre`
  (`P2-LATTICE-MICROSPEC-01`, recorded as *not created*), and the choice
  among naive, Wilson, staggered and overlap is recorded as **a choice
  of the theory's matter content.**
- **`C-iii` therefore cannot be evaluated, and standard C cannot be
  completed, until `D-pre` freezes that operator.** **State this; do not
  recommend anything about it.**

**The single most likely misreading of this merge is that the Goldstone
question is settled.** It is settled for one kernel the programme has not
adopted, and one candidate class among several.

## 2. The amend, ratified

**During execution the source task's third commit was first written with
a `Co-Authored-By` trailer and a session URL** — a `P6` violation, caught
by the checker at exit 2, on an **unpushed** branch. **The executor
amended the commit, re-ran all four checker invocations to PASS, and
recorded both the rejected and the replacement commit ids in its
report.**

**PI RULING, recorded verbatim as the ratification this integration
rests on:**

> **The amend is ratified as a compliant repair.** An unpushed commit may
> be amended solely to remove a mechanically detected commit-hygiene
> violation before any branch publication, provided both the rejected and
> the replacement commit ids are recorded in the task report. **This does
> not authorise rewriting any pushed or reviewed history.**

**Two limits the Researcher records beside the ruling, not inside it.**

**First: `mechanically detected` is load-bearing.** The violation here
was reported by the checker with a non-zero exit; **the executor exercised
no judgement about whether a violation existed.** **A ruling that
permitted amending on the executor's own opinion of a message would
authorise something different.**

**Second: the general principle belongs in `CONVENTIONS.md`, not here.**
**This ruling ratifies THIS instance.** **Drafting the durable rule — and
deciding whether it must require every affected check re-run rather than
only the failing one — is task `C`'s**, and this task neither writes it
nor implies its wording.

**Verified from a clean clone:** the rejected commit `b41178d1…` **is not
present and is not an ancestor of the source head**; the four commit
messages on the source branch carry **zero** forbidden trailers, URLs or
tool attributions.

## 3. Two findings arrive unrepaired, and this task repairs neither

**`F1` — the harness conflicts with `P6` structurally.** The executor's
harness supplies generic git guidance that adds the forbidden trailer;
`P6` forbids it. **This will recur on every task** and is currently
caught by a criterion each specification must remember to write.
**Report it as arriving; do not fix it.**

**`F2` — `scripts/p2_phase01_scalar_exploratory.py` line 73 reads
`for the frozen Wilson D`.** **The exploratory script calls the Wilson
operator frozen** — precisely the conflation the arriving artifact was
written to reject. **The word is used loosely at lines 321, 353 and 410;
line 73 is the only place it attaches to `D`.** **Report it as arriving;
do not fix it** — `scripts/` is protected by A8 and editing it here would
be authoring content during an integration.

## 4. Acceptance criteria

**A1 — Refs.** Read from the remote: `refs/heads/main` resolves to
`1b569851a914589242024c4dde7d2eb020e3800c` and
`science/ac4-symmetry-goldstone` to
`f4adc2d458a44d10b368a96f5dbb494cedd220ab`. **Any mismatch → STOP.**
**Also confirm `b41178d1…` is NOT an ancestor of the source head**, and
report the exit status.

**A2 — This task's pre-execution review committed, unedited**, per Rule
18, **carrying `reviewed specification SHA-256:` filled in.** **If blank
or naming a different digest, STOP and say which.** Report both digests
equal.

**A3 — Merge parentage, three separately derived measurements.**

    parent 1 = this task's pre-execution review commit (commit 2)
    parent 2 = f4adc2d458a44d10b368a96f5dbb494cedd220ab
    merge-base(parent 1, parent 2)
             = 1b569851a914589242024c4dde7d2eb020e3800c

**Commit 1 MUST be an ancestor of parent 1**; verify and report that too.
**The merge-base equals the evidence base and NOT parent 1**, because
parent 1 already carries two commits of this task's own.

**A4 — No conflict.** Report the merge's conflict list. **It must be
empty.** **Any conflict is a STOP** — the dry run measured none, and a
conflict would mean something moved.

**A5 — Scope, frozen manifest. Final base-to-head scope: 7 additions and
0 modifications.**

    stated: 7 additions, 0 modifications
    base: 1b569851a914589242024c4dde7d2eb020e3800c
    head: <commit 4>
    mode: exact
    add:
      derivations/P2-PHASE-01_AC4_symmetry_and_goldstone.md
      reports/2026-08-13T1239Z_ac4-symmetry-goldstone.md
      reports/2026-08-XXT{HHMM}Z_integrate-ac4.md
      reviews/chatgpt/2026-08-13T1239Z_ac4-symmetry-goldstone.md
      reviews/chatgpt/2026-08-XXT{HHMM}Z_integrate-ac4.md
      specs/2026-08-13T1239Z_ac4-symmetry-goldstone.md
      specs/2026-08-XXT{HHMM}Z_integrate-ac4.md
    modify: []
    forbidden_operations:
      delete, rename, copy, type_change, unmerged, unknown

**`modify:` is `[]` and must remain so.** **A single modification
anywhere is a STOP.**

**At the merge commit, before the report exists, the count is 6 additions
and 0 modifications.** **Report which head each figure was measured at.**

**A6 — Arriving artifacts intact.** After the merge, the four arriving
paths are blob-identical to the source branch. **Report all four
comparisons.**

**A7 — Both pins unchanged and matching.** For each occurrence of
`` (sha256 `<64 hex>`) `` in `GATES.md`, identify the artifact path named
immediately above it, measure that path's SHA-256 at the head, and report
the pair. **Expected: two pins, both matching, both byte-identical to
their values at the evidence base** — this task moves neither. **Assert
the count is at least one**, and **report the count found.**

**A8 — Protected paths.** **Every path existing at the evidence base is
blob-identical at the head** — there are no authorised modifications.
**In particular `GATES.md`, both DRAFT files, the exploratory script, the
results file, `CONVENTIONS.md`, `DECISION_LOG.md` and
`docs/BRANCHING_POLICY.md`.** Compare path by path and report the count.

**A9 — Gate invariants.** `^## P2-` count **14**; `P2-PHASE-01` reads
`Status: PROPOSED`; both prerequisites read `SATISFIED`; every `Status:`
line textually identical to the evidence base. **Report all four.**

**A10 — Superseded branches not merged, all six.** No commit in the
register is an ancestor of the head:

    52f65117  ebd531ab  40168469  7146a093  10c260b9  d64cd912

**Six separate exit statuses**, before and after the advance.

**A11 — The checker over this task's own range**, base `1b569851…`, head
**commit 3, the merge commit** — not commit 4, which is the report that
must carry this output. Two runs:

    RUN 1  default subject selection, observational, governs nothing
    RUN 2  specification_paths naming ONLY
           specs/2026-08-XXT{HHMM}Z_integrate-ac4.md

**Config for both runs, stated so that you supply no value of your own:**

    append_only_paths          ["DECISION_LOG.md"]
    authorised_modified_gates  []
    prospectivity              boundary ce86b534…, both readings run
    register_path              docs/BRANCHING_POLICY.md

**`append_only_paths` is NOT `[]`.** **`authorised_modified_gates` IS
`[]`, and here that is truthful**: no gate may change.

**`P7` will return `PASS` and it is evidence of nothing.** `GATE_HEADING`
matches zero of the fourteen real gate headings. **This task changes no
gate, so nothing rests on it; A8 and A9 establish that.** **The `P7`
repair is the next task in the queue and is not performed here.**

**RUN 2 is stop-governing; any failure is a STOP.** **Both configs and
both JSON outputs verbatim.** **Report the subject set RUN 1 actually
selected, as measured.**

**A11-final, post-report evidence:** re-run RUN 2 at commit 4, **before
the landing.** **If it fails, STOP before advancing `main`.**

**A12 — Validators, exit status 0.** Report pass and deselect counts
before and after. **Expected: unchanged at 280 passed, 2 deselected** —
this merge adds no test and changes none.

**A13 — Commit-message hygiene** on all four commits including the merge:
proposed message inspected before, stored message after; **no
`Co-Authored-By`, no session identifier or URL, no tool attribution.**
**Report per commit whether any trailer was suppressed and which.**

**This criterion has teeth in this task specifically.** §2 records that
the source task's harness supplied exactly such a trailer. **If yours
does the same, the ruling of §2 does NOT cover you**: it covers a
mechanically detected violation on an unpushed commit, and it is a
ratification of one past instance, not a standing permission. **Report
what happened either way.**

## 5. Commit order and evidence layering

    commit 1  specs/2026-08-XXT{HHMM}Z_integrate-ac4.md
    commit 2  reviews/chatgpt/2026-08-XXT{HHMM}Z_integrate-ac4.md
    commit 3  --no-ff merge of f4adc2d4…
    commit 4  reports/2026-08-XXT{HHMM}Z_integrate-ac4.md
    then      fast-forward refs/heads/main to commit 4, and push

`{HHMM}Z` is a UTC token fixed once by commit 1 and reused; `XX` is the
day at execution. **You choose no path.** **Commit 2 precedes the
merge**, per Rule 15's timing clause.

**Committed report — measured at commit 3:** A1–A10, A12 and A13 for
commits 1–3; **A11's two runs with both configs verbatim**; commit 1–3
SHAs and stored messages; commit 4's intended message; **A5's final scope
stated as INTENDED, with the measured 6/0 figure at commit 3.**

**Post-report evidence, NOT written back:** A5's final scope measured
base-to-commit-4; A11-final; A7 and A10 re-run after the advance; A13 for
commit 4; validators at the pushed `main`; the push; remote `main` read
back; final ancestry confirmation.

**Nothing in the committed report may claim to measure commit 4.**

## 6. The landing clause

**This task ends with authoritative `main` at its own final report
commit.** The target is named as **commit 4**, not as a SHA: any SHA
naming a commit that carries this task's review is unreachable as a
landing target, because Rule 15 places commits after it.

**The advance is a fast-forward. Verified available:** `1b569851…` is the
base of this branch. **Verify `--is-ancestor` before the push and report
the exit status as a measurement.** **If a fast-forward is not available,
STOP** — do not convert the landing into a merge.

**Push without `--force` and without `--force-with-lease`.**

**`science/ac4-symmetry-goldstone` is not deleted and does not move.**
Verify and report its tip after the advance.

## 7. Rule 16 assessment

**Rule 16 is operative.** State what the assembled set does NOT
establish, **naming the junction or reporting a search.**

**Three junctions, all three required in the report.**

**First.** `main` will carry a determination whose verdict A reads as an
answer about symmetry. **A reader may take it for the answer to
`OPEN-AC-4`.** **Verdict B says `OPEN-AC-4` does not close.** **Say where
a reader meets both, and say that verdict A is conditional on a kernel
the programme has not adopted.**

**Second.** **The arriving artifact and this task both state that the
canonical lattice Dirac operator is not frozen. `scripts/…_exploratory.py`
line 73 says `frozen Wilson D`.** **Both are now on `main` and they read
in opposite directions.** **Name that, and name `F2` as the open item.**

**Third.** `P7` returns `PASS` over two empty maps. **This task changes
no gate, so nothing rests on it — but the green will look the same on the
next task that does.** **The repair is next in the queue and is not this
task's.**

## 8. Invariants and prohibitions

- Executor-writable: this specification, its review, and its report.
  **Everything arriving by merge is integrated exactly as reviewed.**
- **Modify no existing file, for any reason.** There are no authorised
  modifications in this task.
- **Do not fix `F1` or `F2`.** Report them.
- **Do not name any candidate as passing or failing `C-i`, `C-ii` or
  `C-iii`.**
- **Do not state that `OPEN-AC-4` is closed**, and do not describe the
  Goldstone question as settled.
- **Do not state or imply that the programme has adopted Wilson
  fermions.**
- **Do not recommend opening `D-pre`.** Stating that the blocker moves
  there is required; recommending it is not this task's.
- **Do not write a superseded-register entry.** Nothing is superseded.
- **Do not adjust the config to make RUN 2 pass.**
- **Do not describe `P7` as having checked gate integrity.**
- Merge commit only for the integration: no fast-forward there, no
  squash, no rebase. **The landing is a fast-forward or a stop.**
- No force-push, no history rewrite, no branch deletion. **§2's
  ratification applies to a past instance on another branch and confers
  nothing here.**
- Environment: `CONVENTIONS.md` Rule 13's diagnostic order applies.
  **Rule 13 carries two such orders, a known open item; if no
  environment failure occurs, say neither was exercised rather than
  naming one.**
- If any instruction here is inconsistent with a repository rule or with
  another instruction, stop and report; do not decide which prevails.

## 9. Report contract

- everything in §5 under its correct layer, **each committed figure
  labelled MEASURED or INTENDED**;
- **A3's three values, separately derived**, with the method for each;
- **A4's conflict list**, confirmed empty;
- **A5's two scope figures**, with the head each was measured at;
- **A6's four blob comparisons**;
- **A7's pin table**, the count found, and confirmation both are
  unchanged from the evidence base;
- **A8's path count**, and **explicit confirmation that zero
  modifications occurred**;
- **A9's four gate invariants**;
- **A10's six exit statuses, before and after the advance**;
- **A11's two runs**, both configs verbatim, the measured RUN 1 subject
  set, and the `P7` statement;
- **A13 per commit**, and **whether your own harness attempted a
  forbidden trailer** — §2 says the ratification does not cover you;
- **`F1` and `F2` reported as arriving and unrepaired**;
- **the landing**: the pre-advance is-ancestor exit status, the exact
  push command, remote `main` read back, and the source tip unchanged;
- **§7's three Rule 16 junctions**;
- **whether `main` now reads as though `OPEN-AC-4` were closed.** It is
  not;
- a **Stops and clarifications** section using the five primary
  categories — `SPECIFICATION_DEFECT`, `ENVIRONMENT`,
  `OBSERVATION_METHOD_ERROR`, `REPOSITORY_DEFECT`,
  `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY` — one primary per stop,
  secondary findings separate, included even if there were none;
- anything ambiguous, unsatisfiable, or that you would have specified
  differently.

## 10. Pre-issue literal verification record

**Executed by the specification author before issue, per Amendment H.**
**Every line was produced by running the stated method in a clean
clone.** **No measurement was taken through a truncated view.**

    target      refs
    method      git fetch; git rev-parse against origin
    MEASURED    main = 1b569851a914589242024c4dde7d2eb020e3800c;
                science/ac4-symmetry-goldstone =
                f4adc2d458a44d10b368a96f5dbb494cedd220ab; the source is
                NOT an ancestor of main

    target      the rejected commit of §2
    method      git cat-file -e b41178d1; git merge-base --is-ancestor
    MEASURED    the object is NOT present in a clean clone and is NOT an
                ancestor of the source head. The published history
                carries only the replacement.

    target      commit-message hygiene on the source branch
    method      git log --format=%B over the four commits, searched for
                co-authored-by, http(s), and tool attributions
    MEASURED    zero matches across all four.

    target      the merge
    method      dry run from 1b569851 with a placeholder specification
                commit and a placeholder review commit, then
                git merge --no-ff of the pinned ref
    MEASURED    CLEAN, no conflict. parent 1 = the placeholder review
                commit; parent 2 = f4adc2d4; merge-base = 1b569851.
    MEASURED    at the merge commit: 6 additions, 0 modifications.
                With a placeholder report added: 7 additions, 0
                modifications. 1b569851 is an ancestor of that head, so
                the landing fast-forward is available.

    target      pins at the merged head
    method      grep 'sha256 `[0-9a-f]{64}`' over GATES.md in the dry
                run tree, then sha256sum each named target
    MEASURED    TWO pins, both MATCH. Neither moves in this task, and
                GATES.md is not modified.

    target      F2, the arriving docstring
    method      grep -n 'frozen' over the exploratory script at 1b569851
    MEASURED    line 73 reads "for the frozen Wilson D"; lines 321, 353
                and 410 use the word for other objects. Line 73 is the
                only place it attaches to D.

    target      this specification under the landed P1 grammar
    method      the parser extracted VERBATIM from blob 1922fe88… and
                executed — not re-implemented
    MEASURED    one scope block; stated 7 additions, 0 modifications;
                the manifest lists seven paths and 'modify: []'
                contributes none; parse OK, counted equals stated.
