# Task specification — integrate the measure adjudication, and land it

Specification evidence base: `8108c29846adb3b69c4ea73ab66a1c04b66106dc`

    Repository         zetacheng/2-emergent-gravity
    Branch to create   science/integrate-det-01
    Cut from           authoritative main — refs/remotes/origin/main
    Source             science/det-01-measure-adjudication
                       fba1e0e1…

Classification: **MATERIAL**. Governed by Rule 15, Rule 18, and
**Amendments M–P and Rules 19–21.**

**This is the integration authorization AND the landing authorization.**
§6 carries the landing clause; **no second task is required.**

**NORMATIVE EXECUTION ORDER, stated once:**

    A3  environment conformance
    A1  repository identity and refs
    A2  review binding
    A4  onward

**Criterion numbering is not execution order.**

**One merge, measured clean.** Dry run: **no conflict**, merge-base
`8108c298…`.

    source contributes          4 additions, 0 modifications
    this task authors           3 — spec, review, report
    cumulative at merge (3)     6 additions
    cumulative at final (4)     7 additions

**`6` is CUMULATIVE, not the merge's contribution.** **Any conflict is
an immediate STOP.** **Nothing is modified.**

---

## 0. The verdict, and the rider that makes it actionable

    VERDICT     NOT DETERMINABLE

**The repository does not freeze the functional measure.** **`A5`'s
search is null on every term** — `jacobian` 0, `path integral` 0,
`functional` 0, `field-space` 0, `inner product` 0, `volume element` 0.
Of 21 `measure` hits, `:14` is the loop measure, `:22` is *measured in
units of the cutoff*, and the other 19 are governance text about
measurement discipline.

**The derivation is one line.** For a field-space metric `𝔊` the
covariant measure is `DA = (det 𝔊)^{1/2}∏dA`, and a quadratic action
gives `Γ = ½ log det K − ½ log det 𝔊`. Substituting the landed identity:

    Γ = ½ log det G1 + ½ log det(D1+m²) − ½ log det 𝔊        (★)

**The three pre-registered verdicts are three VALUES OF `𝔊`**, not three
rival physical claims: `𝔊 = G1` gives `OPERATOR-DETERMINANT`, `𝔊 = 𝟙`
gives `HESSIAN-DETERMINANT`, anything else gives `MEASURE-EXPLICIT`.
**They are images of one unspecified input, which is why comparing them
to each other could never settle anything.**

### THE RIDER, and it must land with the verdict

**`G1` is block-diagonal per site with block `√g g^{μν}`, and in four
dimensions `det[√g g⁻¹] = (√g)⁴/det g = det g`.** **Therefore**

    det G1 = ∏ₓ det g(x)     — ULTRALOCAL, and containing no m

**verified to `1.7e-14` against the full `1024×1024` matrix, and
independently confirmed algebraically by the Researcher.**

**So any two candidates differ by `Σₓ F(g(x))`, and two independent
reasons follow, either sufficient:**

- **an ultralocal functional has no derivatives of the metric and cannot
  contribute to `∫√g R`** — on the lattice it lands at `q⁰`, and
  `CONVENTIONS.md:20` defines `Z` as the `q²` coefficient;
- **it carries no `m`, so its `m² ln m²` coefficient is zero.**

> **`β_V` is invariant across the three candidates.**

**Write the landing statement in this form and do not compress it:**

> **`DET-01` verdict: `NOT DETERMINABLE`; the repository does not freeze
> the functional measure; the resulting ambiguity is ultralocal and
> mass-independent; therefore it does not affect `β_V` or the ratio
> target.**

**Do NOT write `NOT DETERMINABLE` as `RECON-01b remains blocked`.**

## 1. What is blocked and what is not

    BLOCKED       the absolute Γ_vector
                  the induced cosmological constant — exactly where the
                  ambiguity lands
                  any claim that a computed Γ is the effective action

    NOT BLOCKED   β_V
                  the β_V/β_B ratio target
                  RECON-01b's scan, as far as the MEASURE question goes

**`RECON-01b` must name the object it scans and record that the naming
is a STATED CHOICE, not a derived one.** **Report that requirement.**

**A separate and independent blocker survives**, and this task does not
dispose of it: **`C5` removes the transverse/longitudinal mixing from
`D1+m²`, so a scan of that object cannot exhibit a `T/L` mixing artefact
even if one is physically present** — and `CIRC-01` has already
established that the relevant observable lives in a two-momentum basis.

**That is a VALIDATION-BASIS question, not a determinant question.**
**Report the two separately and do not merge them into one blocker
again.**

## 2. What the verdict implies for work already landed

**`NOT DETERMINABLE` means the programme has been computing one-loop
determinants without a frozen functional measure**, and **that applies to
work already on `main`** — `P2-HK-01`, `CIRC-01`, every `β_s`.

**The same MECHANISM MAY insulate previously landed `β_s`
coefficients** — **where their measure ambiguity is likewise ultralocal
and mass-independent.**

**THIS INTEGRATION DOES NOT ESTABLISH THAT SPECIES-BY-SPECIES
GENERALISATION.** **`DET-01` proved the insulation for the VECTOR case
and for the specific ambiguity `det G1` carries.** **Whether each other
species' measure ambiguity has the same two properties is a
species-by-species question and was not asked.**

**An earlier draft wrote that those coefficients "are insulated for the
same reason the future one is", which extends a proved theorem to cases
it did not cover.** **Report the mechanism as POSSIBLY GENERALISING and
say it was not verified.**

**The source executor searched and found NO absolute-effective-action or
induced-cosmological-constant claim in the repository.** **Re-run that
search and report it** — `A8`. **If one exists, it is not insulated, and
that is a finding this landing must carry.**

## 3. Three defects arriving with it, none repaired here

**3a. `A9` of the `DET-01` specification cannot be satisfied as
written.** It required the artifact blob-identical at `3a`, `3b` and the
head; `§7` of the same specification required `3b` to append to that
same file. **With an appendix the blob must change.** **The internal
contradiction is the Researcher's.**

**The executor reported rather than choosing, and offered something
stronger than identity would have been**: the `3a` blob is a **BYTE-EXACT
PREFIX** of the `3b` blob — `sha256 7d5b375f…` for both the 23324-byte
`3a` blob and the first 23324 bytes of `3b`, zero differing bytes, 49
lines appended, none removed.

**Verify the prefix relation at the head** — `A9`. **A prefix is the
correct freeze evidence for an append-only artifact, and the
specification should have asked for it.**

**3b. A committed self-referential measurement error, not fixable.**
**The `A10` table first measured one hit for a search category — its own
row label matching its own pattern.** **The executor corrected the table,
and the correction deleted the spelling that caused the hit**, so **the
committed report now states `1` and points at a line whose committed
bytes contain `0`.**

**Rule 20 did not permit repairing the already-pushed `DET-01` tree**,
so the discrepancy stands in that history. **This integration records it
without rewriting that history** — **recording a correction here is not
the same as repairing there, and this report is where the authoritative
correction record lives.** **The measurement carrying the prohibition is
unaffected: the artifact is zero across all six categories, as are all
commit messages** — **verify that and report it separately.**

**This is the fourth self-referential search hazard in this session and
the first created by its own fix.**

**3c. Two measurement corrections in the `DET-01` specification.**
**`A12` asked for "the three `scripts/recon2026/` files" — only TWO live
there**, the third being `tests/test_recon2026_flat_limit.py`.
**And the `P2-BETAV-*` count re-measured to seven.** **Report both as
measured at this base.**

## 4. What this task must not do

- **Do not touch `main` until §6's landing.**
- **Do not modify any file**, including the arriving artifact and the
  three frozen construction files.
- **Do not settle the measure question**, and **do not promote the
  DeWitt metric — or any textbook convention — into a repository
  ruling.** **The source executor named this as the real temptation and
  declined it**: writing `OPERATOR-DETERMINANT` because the DeWitt
  metric genuinely is `√g g^{μν}` and the factors cancel neatly **would
  have read as derived while quietly making a textbook choice into a
  frozen one.**
- **Do not compute `Γ_k`, vary `k`, or compare anything to a ratio
  anchor.**
- **Do not write the `RECON-01b` specification.**
- **Do not repair the `A10` reporting error or the `A9` contradiction.**
- **Do not register a regression anchor.**
- **Do not adjudicate `r = 1`**, and do not touch `R1`–`R5`.
- **Do not add a register entry anywhere.**
- **Do not push any ref but `refs/heads/main` and this task's branch.**

## 5. Acceptance criteria

**A1 — Repository and refs.** Report the `origin` remote URL as measured,
**verbatim and not normalised**; confirm it identifies
`zetacheng/2-emergent-gravity`, accepting either URL form. Fetch, then
report `refs/remotes/origin/main` and confirm it is
`8108c29846adb3b69c4ea73ab66a1c04b66106dc`. **Report `refs/heads/main`
for contrast.** Report the source at `fba1e0e1…` and **that it is not an
ancestor of `main`.**

**A2 — This task's pre-execution review committed, unedited**, per Rule
18 and Amendment `N`, **carrying `reviewed specification SHA-256:`
filled in.** **Check the FIELD IS PRESENT before checking it matches.**

**A3 — Environment conformance, run FIRST.** Rule 13's diagnostic order
including Amendment D's step 0. **Report whether the clone is shallow
and its commit count.** **Any restoration in one line each, with
confirmation that no repository content was touched.**

**A4 — Merge parentage, three separately derived measurements**, parent
1 this task's review commit, parent 2 `fba1e0e1…`, merge-base the
evidence base. **Commit 1 must be an ancestor of parent 1.**

**A5 — No conflict.** Report the conflict list. **It must be empty.**

**A6 — The rider re-derived, not transcribed.** **Confirm
`det[√g g⁻¹] = det g` in four dimensions** — symbolically or
numerically, your choice, **and state which.** **Confirm `det G1` is a
product over sites and carries no `m`.**

**Then report the two consequences separately, and state each in its
own scope:**

- **being ultralocal in `g(x)`, it contributes at `q⁰`, not to the `q²`
  two-derivative coefficient identified here with the Einstein–Hilbert
  `Z`.** **State it that way and not as "cannot affect any
  curvature-related quantity"** — the second is larger than what is
  needed and larger than what was shown, **and the `q⁰` versus `q²`
  framing is the one that connects to `CIRC-01`'s two-momentum basis;**
- **it carries no `m`, so its `m² ln m²` coefficient vanishes.**

**Quote `CONVENTIONS.md:20`'s definition of `Z` with its line.**

**This is the result that changed the dependency graph.** **A landing
that carried the `NOT DETERMINABLE` verdict without the rider would
leave the programme believing `RECON-01b` is blocked when it is not.**

**A7 — The null convention search, re-run.** **Search
`CONVENTIONS.md` for `jacobian`, `path integral`, `functional`,
`field-space`, `inner product`, `volume element`.** **Report each term's
count.** **Report the `measure` hit count and how many are governance
text.** **Expected: null on all six.**

**A8 — The absolute-claim search, re-run.** **Search the repository for
any claim that a computed `Γ` is the effective action, and for any
induced-cosmological-constant claim.** **Report the search terms and the
result.** **The source found none; verify.** **If one exists, report it
as a finding — it would not be insulated by the rider.**

**A9 — The freeze, verified as a PREFIX.** **Report the `3a` and `3b`
blob object ids of `derivations/P2-BETAV-DET-01_measure-adjudication.md`
at the source branch**, the byte length of the `3a` blob, and **the
sha256 of the first that-many bytes of the `3b` blob.** **Confirm they
match and that no line was removed.**

**Report that `A9` of the `DET-01` specification asked for blob
identity, which its own `§7` made impossible.**

**A10 — §3b's committed error.** **Report what the committed report
states, what the committed bytes contain, and that Rule 20 did not
permit repairing the already-pushed `DET-01` tree** — **while this
integration report records the correction, which is a different act.** **Then verify independently that the artifact is
zero across all six search categories and that all commit messages are
too.** **Report both.**

**A11 — Scope, frozen manifest. Cumulative final: 7 additions, 0
modifications.**

    stated: 7 additions, 0 modifications
    append_only:
      DECISION_LOG.md
    authorised_gates: []
    base: 8108c29846adb3b69c4ea73ab66a1c04b66106dc
    head: <commit 4>
    mode: exact
    add:
      derivations/P2-BETAV-DET-01_measure-adjudication.md
      reports/2026-08-18T0142Z_det-01-measure-adjudication.md
      reports/2026-08-XXT{HHMM}Z_integrate-det-01.md
      reviews/chatgpt/2026-08-18T0142Z_det-01-measure-adjudication.md
      reviews/chatgpt/2026-08-XXT{HHMM}Z_integrate-det-01.md
      specs/2026-08-18T0142Z_det-01-measure-adjudication.md
      specs/2026-08-XXT{HHMM}Z_integrate-det-01.md
    modify: []
    forbidden_operations:
      delete, rename, copy, type_change, unmerged, unknown

**Seven paths.** **Four arrive, all additions; three authored here.**
**Report the ARRIVING PATH count and the ARRIVING ADDITION count
separately, and state whether they coincide** — **they do, at four.**

**Report the cumulative figure at each commit AND the source's own
contribution, separately labelled.** **Expected cumulative 1, 2, 6, 7;
source contribution 4.** **A reviewer of an earlier task read a
cumulative figure as a contribution and computed a total three higher.**

**`append_only: DECISION_LOG.md` is a checker-configuration declaration,
NOT an authorisation to write that file.**

**Measure the UTC time and use the value you measured.**

**A12 — Which merge case.** **The merge-base is the evidence base, so no
commit on `main` could have touched an arriving path** — report that,
**then** the four blob comparisons.

**A13 — Nothing existing changed.** Every path at the evidence base
blob-identical at the head. **Report the count compared**, and confirm
explicitly for `GATES.md`, `CONVENTIONS.md`, **every
`derivations/P2-BETAV-*` artifact — re-measure and report what you
measure**, all seven microspec artifacts, **the TWO
`scripts/recon2026/` files and `tests/test_recon2026_flat_limit.py` at
their frozen object ids**, both registers, and everything under
`results/`.

**Two files under `scripts/recon2026/`, not three** — §3c.

**A14 — Gate invariants and pins.** `^## P2-` count **14**;
`P2-PHASE-01` reads `Status: PROPOSED`; both prerequisites `SATISFIED`;
both pins match. **Report all four, read SCOPED.** **Also report:**
`P2-BETAV-RECON-01` `PROPOSED`, `P2-BETAV-CIRC-01` `RUN`, `P2-BETAV-01`
`PROPOSED (deferred)`, and **`Regression anchors` still `None yet
(proposed)`.**

**A15 — Superseded branches not merged, all six.**

    52f65117  ebd531ab  40168469  7146a093  10c260b9  d64cd912

**Six separate exit statuses**, before and after the advance.

**A16 — The checker over this task's own range**, base `8108c298…`, head
**commit 3**. Two runs, `RUN 1` observational and `RUN 2` naming only
this task's specification.

**Config for both runs:**

    append_only_paths          ["DECISION_LOG.md"]
    authorised_modified_gates  []
    prospectivity              boundary ce86b534…, both readings run
    register_path              docs/BRANCHING_POLICY.md

**Report `declared_source` for each** and **confirm no
`DECLARATION_CONFLICT`.** **`P7` must report fourteen sections.**
**`PASS` at zero is a STOP.** **RUN 2 is stop-governing.** **Both configs
and both JSON outputs verbatim.** **PARSE the output; do not grep it.**

**A16-final, post-report evidence:** re-run RUN 2 at commit 4, **before
the landing.**

**A17 — Validators, exit status 0.** **Expected 332 passed, 2
deselected** — the arriving task adds no code.

**A18 — Commit-message hygiene** on all four commits. **Rule 20 binds
this task.**

## 6. Commit order, evidence layering, and the landing clause

    commit 1  specs/2026-08-XXT{HHMM}Z_integrate-det-01.md
    commit 2  reviews/chatgpt/2026-08-XXT{HHMM}Z_integrate-det-01.md
    commit 3  --no-ff merge of fba1e0e1…
    commit 4  reports/2026-08-XXT{HHMM}Z_integrate-det-01.md
    then      fast-forward refs/heads/main to commit 4, and push

**Committed report — measured at commit 3:** A1–A15, A17 and A18 for
commits 1–3; **A16's two runs with both configs verbatim**; commit 1–3
SHAs and stored messages; commit 4's intended message; **A11's final
scope stated as INTENDED, with the measured cumulative 6 at commit 3.**

**Post-report evidence, NOT written back:** A11's final scope measured
base-to-commit-4; A16-final; A14 and A15 re-run after the advance; A18
for commit 4; the push; remote `main` read back; final ancestry
confirmation.

**Nothing in the committed report may claim to measure commit 4.**

**The landing.** **This task ends with authoritative `main` at its own
final report commit**, named as **commit 4**, not as a SHA. **The
advance is a fast-forward; `8108c298…` is the base of this branch.**
**Verify `--is-ancestor` before the push and report the exit status as a
measurement.** **If a fast-forward is not available, STOP.** **Push
without `--force` and without `--force-with-lease`.** **Push only
`refs/heads/main` and this task's branch — no session branch, no
`science/det-01-measure-adjudication`, no other ref.** **The source
branch is not deleted and does not move**; verify and report its tip.

## 7. Rule 16 assessment

**Rule 16 is operative.** State what the assembled set does NOT
establish, **naming the junction or reporting a search.**

**Five junctions, all five required in the report.**

**First.** **`NOT DETERMINABLE` is a finding about the repository, not a
failure of the task.** **The programme has been computing one-loop
determinants without a frozen functional measure, and that applies to
landed work.**

**Say that**, and **say that the rider is why the VECTOR case is
survivable — not diligence, and not luck.** **And say that whether the
same holds species by species was not established**, per §2. **A reader
who takes the vector result as covering every landed `β_s` would be
extending a theorem past its domain.**

**Second.** **The rider makes `β_V` invariant; it does not make the
measure question unimportant.** **The absolute `Γ_vector` and the
induced cosmological constant remain blocked, and the cosmological
constant is EXACTLY where an ultralocal `Σₓ F(g(x))` term lands.** **Say
that a programme claiming dark energy from this framework would meet
this ambiguity head-on.**

**Third.** **`RECON-01b` is unblocked as to the MEASURE and not as to
VALIDATION.** **`C5` removes the `T/L` mixing from the object a scan
would examine, so the scan cannot exhibit an artefact that may be
physically present.** **Report the two as separate blockers**, and **say
that merging them again would lose the second.**

**Fourth.** **None of the four sources the `DET-01` specification named
carried the verdict.** **The consequence came from `CONVENTIONS.md:20`,
which was not in the list of places to look.** **`SIGN-01` found the
convention most obviously labelled a convention was not the one carrying
the result; this goes one step further.** **Say that a specification's
list of sources is a hypothesis about where the answer lives, and this
one was wrong.**

**Fifth.** **Two of the three defects of §3 are the Researcher's** — the
`A9`/`§7` contradiction and the `scripts/recon2026/` miscount. **The
third is a committed reporting error the executor found in its own work
and could not repair.** **Say which is whose.**

## 8. Invariants and prohibitions

- Executor-writable: this specification, its review, and its report.
  **Everything arriving by merge is integrated exactly as reviewed.**
- **Modify nothing.**
- **Do not settle the measure, promote a textbook convention, compute
  `Γ_k`, or repair either defect.**
- **Do not adjust the config or this specification's declarations to
  make RUN 2 pass.**
- **No force-push and no branch deletion. No history rewrite except the
  narrowly permitted pre-push hygiene repair under Rule 20.**
- Merge commit only for the integration: no fast-forward there, no
  squash, no rebase. **The landing is a fast-forward or a stop.**
- Environment: `CONVENTIONS.md` Rule 13's diagnostic order applies, and
  **A3 requires it run FIRST and reported rather than assumed.** **Rule
  13 carries two such orders, a known open item; if no environment
  failure occurs, say neither was exercised rather than naming one.**
- If any instruction here is inconsistent with a repository rule or with
  another instruction, stop and report; do not decide which prevails.

## 9. Report contract

- everything in §6 under its correct layer, **each committed figure
  labelled MEASURED or INTENDED**;
- **A1's verbatim `origin` URL**;
- **A3's environment diagnosis in Rule 13's order, run FIRST**;
- **A4's three values, separately derived**;
- **A6's re-derivation with the method stated, and both consequences,
  with `CONVENTIONS.md:20` quoted**;
- **A7's six null counts and the `measure` breakdown**;
- **A8's search terms and result**;
- **A9's prefix verification with byte length and both digests**;
- **A10's committed-versus-actual statement and the independent
  six-category verification**;
- **A11's cumulative figures per commit AND the source's contribution,
  separately labelled**;
- **A12's merge case, stated BEFORE the blob comparisons**;
- **A13's path count, the `P2-BETAV-*` count re-measured, and the two
  `scripts/recon2026/` files**;
- **A14's four invariants, the three `BETAV` statuses, and the
  `Regression anchors` value**;
- **A15's six exit statuses, before and after**;
- **A16's two runs**, both configs verbatim, the section count `P7` saw,
  what `RUN 1` did, and confirmation the output was parsed not grepped;
- **A17's counts**;
- **the landing**: the pre-advance is-ancestor exit status, the exact
  push command, remote `main` read back, the source tip unchanged, and
  confirmation that no other ref was pushed;
- **§7's five Rule 16 junctions**;
- **whether landing an unfixed measure made you want to settle it, to
  write `OPERATOR-DETERMINANT` because the DeWitt metric fits, or to
  start `RECON-01b`.** **Say which and why, and confirm you did not** —
  **the source executor named the DeWitt temptation as the real one and
  said that keeping textbook labelled as textbook was the most useful
  thing the task produced**;
- a **Stops and clarifications** section using the five primary
  categories — `SPECIFICATION_DEFECT`, `ENVIRONMENT`,
  `OBSERVATION_METHOD_ERROR`, `REPOSITORY_DEFECT`,
  `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY` — one primary per stop,
  secondary findings separate, included even if there were none.

## 10. Pre-issue literal verification record

**Executed by the specification author before issue, per Amendment H and
Amendment M.**

    target      refs and the merge
    method      git fetch; git rev-parse; dry run from 8108c298 with two
                placeholder commits, then git merge --no-ff
    MEASURED    origin/main = 8108c29846adb3b69c4ea73ab66a1c04b66106dc;
                source = fba1e0e1…, NOT an ancestor of main. Merge
                CLEAN; merge-base = 8108c298. SOURCE CONTRIBUTES 4
                additions, 0 modifications. CUMULATIVE: 6 at the merge
                commit, 7 with a placeholder report. The landing
                fast-forward is available.

    target      the rider's central algebra
    method      build a random symmetric positive-definite 4x4 g,
                form √(det g) · g⁻¹, compare determinants, three trials
    MEASURED    det[√g g⁻¹] = det g in every trial, to floating
                precision. The rider's algebra holds independently of
                the executor's numerical check. A6 requires it
                re-derived at the head.

    target      the scripts/recon2026 file count
    method      git ls-tree over the evidence base
    MEASURED    TWO files under scripts/recon2026/ —
                flat_validation.py and proca_curved.py. The third
                construction file is tests/test_recon2026_flat_limit.py.
                The DET-01 specification said "the three
                scripts/recon2026/ files"; that was wrong and A13 states
                the correct locations.

    target      the P2-BETAV-* count
    method      git ls-tree over derivations/ at the evidence base
    MEASURED    SEVEN. A13 requires it re-measured rather than carried.

    target      the A9 / §7 contradiction in the DET-01 specification
    method      inspection of the issued specification
    MEASURED    A9 required the artifact blob-identical at 3a, 3b and
                the head; §7 required 3b to append to that same file.
                With an appendix the blob must change. The contradiction
                is the author's. The executor reported it rather than
                choosing, and supplied a byte-exact prefix relation
                instead. A9 of THIS specification asks for the prefix.

    target      THIS specification's own scope block
    method      parse this file and list its scope keys
    MEASURED    stated, append_only, authorised_gates, base, head, mode,
                add, modify, forbidden_operations.

    target      this specification under the landed P1 grammar
    method      the parser extracted VERBATIM from the checker at
                origin/main and executed — not re-implemented
    MEASURED    one scope block; stated 7 additions, 0 modifications;
                parse OK, counted equals stated per category.
