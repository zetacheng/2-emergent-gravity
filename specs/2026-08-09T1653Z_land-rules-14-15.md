# Task specification — land amendments A–D and new Rules 14 and 15

Specification evidence base: `f309f61c9c14b0e2c63e078f9c0d0809422742e7`

Classification: **MATERIAL**. Branch only; integration is a separate
authorization after result review.

**This lands Amendments A–D and Rule 14 from an earlier approved draft,
together with separately PI-approved Rule 15. None has previously been
executed.** It changes governance text only: no science, no gate, no
result.

**Why now.** The later amendment package (E–L, Rules 16 and 17)
**attaches Amendment E to Rule 14 — a rule `CONVENTIONS.md` does not yet
contain**,
whose highest numbered execution-discipline rule is 13. Landing that
package first would attach an amendment to nothing. **This specification
is the prerequisite.**

---

## 0. What lands, and the numbering

`CONVENTIONS.md` currently carries execution-discipline rules 1–13.
This task adds **14 and 15**, and refines four existing rules.

    Amendment A   -> Rule 7    mid-task authorizations reproduced
                               verbatim in the task report
    Amendment B   -> Rule 8    every task report carries a
                               "Stops and clarifications" section
    Amendment C   -> Rule 9    digest semantics and binary-safe
                               computation
    Amendment D   -> Rule 13   execution location, and the
                               process/harness layer
    New Rule 14                validator outcome and exception contract
    New Rule 15                governing artifacts are committed

**After this task, `CONVENTIONS.md` runs 1–15**, and the later package's
Amendment E (attached to Rule 14) and Rule 16 both become landable
without renumbering. **Do not renumber anything.**

## 1. The source, and one thing it does not contain

The approved package is
`/mnt/user-data/outputs/DRAFT_amendment_reporting_and_digests.md`,
SHA-256 `bef29c4d0ef3d391f2caa2f17ab6336e11677c73a3c37cbf4acb17b23e566822`,
supplied with this specification.

**It contains Amendments A–D and Rule 14. It does NOT contain Rule 15's
text.** §2 supplies it. **Do not attempt to extract Rule 15 from the
draft; it is not there, and that absence is a fact about the draft, not
a search failure.**

**Rule 15's provenance, stated without circularity.** Rule 15 was
approved by the PI prior to this execution and is **supplied
normatively by §2 of this specification**. **Its prior approval has no
separate repository artifact; this specification is the first durable
record of that approval.** It is not being presented as extracted from
an existing approved document, because it is not.

## 2. Rule 15, supplied here

> **15. Governing artifacts are committed.**
>
> **A specification, a pre-execution review, a task report, and any
> manifest supplied to a checker are governing artifacts and are
> committed to the repository.** An artifact that determines what a task
> was authorised to do, or that records what it did, is not evidence
> while it exists only in a conversation.
>
> **Placement.** Specifications under `specs/`; task reports under
> `reports/`; pre-execution reviews under `reviews/<function>/`, using
> the existing directory for the reviewing party. Supplied manifests are
> reproduced in the report that used them.
>
> **Timing.** A specification is committed as the task's first commit.
> **A pre-execution review is committed before the work it authorises
> proceeds.** A report is committed by the task that produced it.
>
> **Prospective only.** Records created before this rule are not
> retrospectively non-conforming, and are not to be back-filled.

**Including this task.** **This task is governed by the rules in force
at its evidence base. Rule 15 becomes operative only after integration,
and does not retroactively require this task's own pre-execution review
to be committed.** That is a matter of ordering, not an exemption — and
stating it prevents a later reader from finding the landing task in
breach of the rule it landed.

**Its origin, recorded so the rule is not read as bureaucracy.** A
read-only audit established that an integration task's repository state
was correct in every respect — **but that its scope manifest was never
preserved, no merge-guard output was ever committed, and neither
mid-task authorization appeared in the report verbatim. The result was
right; the authority under which it was produced could not be
reconstructed.**

**A second body of evidence has accumulated since.** Specifications have
been passed to the Reviewer as conversational text, and **on four
separate occasions in one day the Reviewer reviewed a superseded version
and raised issues already fixed** — each costing a full round trip. **A
committed artifact with a digest removes that failure mode**, because
the reviewer reads a path rather than a paste.

**What this rule asks of the Reviewer function, stated plainly.**
Pre-execution reviews would be committed under `reviews/chatgpt/` before
the work they authorise proceeds. **That is a change to how the Reviewer
works, not only to how the Executor does**, and it should be visible in
the landed text rather than discovered later.

## 3. Acceptance criteria

**A0 — Commit order and paths, frozen.**

    commit 1  specs/2026-08-09T{HHMM}Z_land-rules-14-15.md
    commit 2  CONVENTIONS.md, DECISION_LOG.md
    commit 3  reports/2026-08-09T{HHMM}Z_land-rules-14-15.md

`{HHMM}Z` is a UTC token fixed once by commit 1 and reused. **You choose
no path.** **No derivation note is required**: this task performs no
derivation.

**A1 — Pinned inputs**, verified before use; any mismatch is a STOP:

    CONVENTIONS.md
    ec8d23aa8b0596d4d68cb5df4ab4821428641e923980d0fe1b45c4770c0089e6

    AGENTS.md
    270c7ea4c38621cbafc9cee940c95c5ecf4cc65ff1e5d01e8029b1b12339d461

    the supplied amendment draft
    bef29c4d0ef3d391f2caa2f17ab6336e11677c73a3c37cbf4acb17b23e566822

**A2 — Current numbering confirmed.** `CONVENTIONS.md` at the evidence
base has execution-discipline rules **1 through 13, with no 14 or 15**.
Report the extracted headings. **If a 14 or 15 already exists, STOP** —
this task's premise is that they do not.

**A3 — Four refinements applied**, each attached to its stated rule,
reproducing the draft's substantive text.

**For each amendment report three things**, because "before and after"
alone admits two defensible readings — the whole rule, or only the
insertion point:

    1  the pre-existing rule text, unchanged
    2  the exact normative text the amendment adds
    3  the resulting rule section

**A4 — Rules 14 and 15 added**, 14 from the draft and 15 from §2,
**verbatim in substance**, as new sections in the file's existing
`### <n>. <title>` style, immediately after Rule 13.

**A5 — Rules 1–13 unchanged apart from the four authorised
insertions.** Their numbers and titles are identical, **and — after
removing only the four authorised inserted amendment blocks — the
remaining pre-existing text is byte-identical to the evidence-base
version.**

**Heading equality alone is a proxy, not the property.** It would not
detect a rule silently reworded under an unchanged heading. Report both
checks.

**A6 — `DECISION_LOG.md` entry** in the file's existing format,
recording the adoption, that the rules are prospective only, and
**distinguishing the two provenances**:

    Amendments A-D and Rule 14   from the previously approved draft,
                                 which was never executed
    Rule 15                      separately PI-approved, with no prior
                                 repository artifact; first durably
                                 recorded by this specification

**Do not write that Rule 15 came from the draft**, and do not imply a
prior artifact that does not exist. **Append-only: zero
deleted lines**, verified both base-to-head **and each commit against
its parent.**

**A7 — Nothing else touched.** `AGENTS.md`, `GATES.md`,
`pyproject.toml`, and **every path under `scripts/`, `results/`,
`tests/`, `derivations/` and `reviews/` THAT EXISTS AT THE EVIDENCE
BASE**: blob-identical to the evidence base. **Read from the objects.**

**No gate status changes.** **In particular, do not create any
`reviews/chatgpt/` record.** Rule 15 is prospective; **landing it does
not back-fill anything**, and creating a review record here would be the
back-filling the rule forbids.

**A8 — Scope**, two additions and two modifications:

    add:
      specs/2026-08-09T{HHMM}Z_land-rules-14-15.md
      reports/2026-08-09T{HHMM}Z_land-rules-14-15.md
    modify:
      CONVENTIONS.md
      DECISION_LOG.md
    forbidden_operations:
      delete, rename, copy, type_change, unmerged, unknown

**Final base-to-head scope: 2 additions and 2 modifications.** Report the
template, the resolved manifest, its SHA-256, and the scope-checker JSON
including `observed_operations`.

**A9 — Validators, exit status 0**, run individually with
`python -m pytest <path>`: `tests/test_repository_structure.py`,
`tests/test_si1_governance.py`, `tests/test_gate_anchors.py`,
`tests/test_governance_tools.py`. **A9-pre** at the pre-report head goes
in the report; **A9-final** at the pushed head is post-report evidence.

**If a validator asserts anything about `CONVENTIONS.md`'s structure or
rule count, report what it asserts** — a governance test that constrains
the file this task edits is worth knowing about.

**A10 — Branch only.** Verify `refs/remotes/origin/main` and remote
`refs/heads/main` both resolve to
`f309f61c9c14b0e2c63e078f9c0d0809422742e7`; create the branch from that
commit; move no `main` ref. **Local `main` is stale by design.** Report
all three. Push the task branch only. **Delete no branch.**

## 4. Evidence layering

**Committed report:** A1–A8, A9-pre, the earlier commit SHAs and
messages, the pre-report head, the intended final manifest, and the
intended report commit message with its authoring-time trailer
suppression.

**Post-report evidence, returned to the Reviewer and NOT written back:**
the final scope check at the pushed head, A9-final, the push, the report
commit's stored message read back from the object, and ancestry
confirmation.

## 5. Invariants and prohibitions

- Executor-writable: the four paths of A8 only.
- **Do not renumber, reword, or reorder rules 1–13** beyond the four
  stated refinements.
- **Do not land Amendments E–L or Rules 16 and 17.** They are a separate
  package and a separate authorization; **this task exists so that they
  become landable, not to land them.**
- **Do not create any `reviews/` record**, and do not back-fill any
  artifact under Rule 15.
- **Do not modify `AGENTS.md`.** Its research rules are a different
  numbering from `CONVENTIONS.md`'s execution-discipline rules, and this
  task touches only the latter.
- Commit-message hygiene: inspect the proposed message before each
  commit and the stored message after; permit no `Co-Authored-By`, no
  session identifier or URL, no tool attribution. **Report per commit
  whether any trailer was suppressed and which.**
- No merge into `main`, no PR, no force-push, no history rewrite.
- Branch naming: use `governance/land-rules-14-15`.
- Environment: rule 13's diagnostic order applies. **Do not install
  anything.**
- If any instruction here is inconsistent with a repository rule or with
  another instruction, stop and report; do not decide which prevails.

## 6. Report contract

- raw output for A1–A9, scope-checker JSON verbatim including
  `observed_operations`;
- the rule heading list **before and after**, showing 1–13 unchanged and
  14–15 added;
- the four refinements, each with its before and after;
- Rules 14 and 15 quoted in full as landed;
- the `DECISION_LOG.md` entry, with **both** append-only measures;
- **whether any existing rule now reads as contradicted by 14 or 15.**
  Fifteen rules written at different times can conflict, and **you are
  reading them together in a way nobody has**;
- **whether Rule 15 as written is executable by you.** It asks that
  specifications be committed as a task's first commit — **which every
  recent task already does** — but also that pre-execution reviews be
  committed before work proceeds, **which is not currently anyone's
  step.** If the rule as landed would be unenforceable in practice, that
  is worth more than a clean report;
- a **Stops and clarifications** section using the five primary
  categories — `SPECIFICATION_DEFECT`, `ENVIRONMENT`,
  `OBSERVATION_METHOD_ERROR`, `REPOSITORY_DEFECT`,
  `UNRESOLVED_GOVERNANCE_OR_EVIDENCE_AMBIGUITY` — one primary per stop,
  secondary findings separate, included even if there were none;
- anything ambiguous, unsatisfiable, or that you would have specified
  differently.

## 7. Pre-issue literal verification record

**Executed by the specification author before issue, per Amendment H.**
Re-run these; if any fails to reproduce, STOP and report which — do not
adjust a target to make it pass.

    targets     DRAFT_amendment_reporting_and_digests.md
                bef29c4d0ef3d391f2caa2f17ab6336e11677c73a3c37cbf4acb17b23e566822

                CONVENTIONS.md at the evidence base
                ec8d23aa8b0596d4d68cb5df4ab4821428641e923980d0fe1b45c4770c0089e6

    method      Python substring containment against each file's raw text
    check type  EXACT LITERAL SUBSTRING — no normalisation applied to
                either side; the literal itself is character-for-
                character identical

    PASS   ## Amendment A (attached to Rule 7)                    draft
    PASS   ## Amendment B (attached to Rule 8)                    draft
    PASS   ## Amendment C (attached to Rule 9)                    draft
    PASS   ## Amendment D (attached to Rule 13)                   draft
    PASS   ## New Rule 14                                         draft
    PASS   ### 7. Evidence precedence                             CONVENTIONS.md
    PASS   ### 8. Responsibility separation                       CONVENTIONS.md
    PASS   ### 9. Outcome-based task specification                CONVENTIONS.md
    PASS   ### 12. Acceptance criteria must be mechanically checkable
    PASS   ### 13. Execution environment                          CONVENTIONS.md

**No normalisation function applies to any check above** — the literals
are character-for-character identical, and stripping anything would make
them different checks.

**These heading checks establish the INSERTION TARGETS only.** Fidelity
of the amendments' substantive content is established against the
digest-pinned complete draft under A1 and A3. **§7 does not evidence
content fidelity**, and reading it as though it did would be the proxy
substitution Amendment H exists to prevent.

**Rule 15's text is NOT in this table**, because it does not exist in
the draft; §2 of this specification supplies it. **That absence is a
fact about the draft, not a failed lookup** — the distinction Amendment
E exists to protect.
